---
phase: 11-ui-redesign
plan: 06
type: execute
wave: 2
depends_on:
  - 11-03
files_modified:
  - web/components/layout/Sidebar.tsx
  - web/components/layout/SidebarItem.tsx
autonomous: true
requirements:
  - REQ-11-04

must_haves:
  truths:
    - "Sidebar displays 4 navigation categories: 拾忆, 发现, 创作, 我的"
    - "Active page has accent left bar indicator and background highlight"
    - "Collapsed sidebar shows icons only with tooltips on hover"
    - "New features have ✨ badge markers"
  artifacts:
    - path: "web/components/layout/Sidebar.tsx"
      provides: "Left navigation panel with categories and collapse toggle"
      contains: "SidebarProvider, useLayout"
    - path: "web/components/layout/SidebarItem.tsx"
      provides: "Nav item with active indicator, tooltip, badge"
      contains: "SidebarItem, isActive, badge"
  key_links:
    - from: "Sidebar.tsx"
      to: "SidebarItem.tsx"
      via: "import SidebarItem"
      pattern: "import.*SidebarItem"
    - from: "SidebarItem.tsx"
      to: "next/navigation usePathname"
      via: "active route detection"
      pattern: "usePathname"
---

<objective>
Create the left Sidebar navigation panel with 4 categorized navigation sections, active page indicator, collapse toggle, scroll-area, and ✨ badges for new features — matching the CONTEXT.md specification exactly.

Purpose: All navigation moves from the overloaded header into Sidebar. The 4-category structure (拾忆, 发现, 创作, 我的) provides clear information architecture. The collapse toggle saves space for power users.
Output: Sidebar.tsx (nav container + collapse logic) and SidebarItem.tsx (individual nav item with active state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ui-redesign/11-CONTEXT.md (section "3. 左侧 Sidebar 导航" — EXACT labels, categories, design details)
@.planning/phases/11-ui-redesign/11-UI-SPEC.md (section "Navigation Labels (Sidebar — Chinese)")
@web/app/providers/layout-provider.tsx (useLayout hook — sidebarCollapsed, toggleSidebar)
@web/components/ui/tooltip.tsx (new shadcn component for collapsed hover labels)
@web/components/ui/scroll-area.tsx (new shadcn component for overflow)
@web/components/ui/badge.tsx (new shadcn component for ✨ new feature markers)
@web/components/ui/separator.tsx (new shadcn component for section dividers)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SidebarItem component</name>
  <files>web/components/layout/SidebarItem.tsx</files>

  <read_first>
@.planning/phases/11-ui-redesign/11-CONTEXT.md (lines 127-153 — Sidebar nav items list, design details)
@web/components/ui/tooltip.tsx (Tooltip, TooltipTrigger, TooltipContent import names)
@web/components/ui/badge.tsx (Badge import name)
  </read_first>

  <action>
Create web/components/layout/SidebarItem.tsx:

```typescript
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { type LucideIcon } from 'lucide-react'
import { Tooltip, TooltipTrigger, TooltipContent } from '@/components/ui/tooltip'
import { Badge } from '@/components/ui/badge'
import { cn } from '@/lib/utils'

export interface SidebarItemProps {
  href: string
  label: string
  icon: LucideIcon
  collapsed?: boolean
  isNew?: boolean
  /** Exact route match (default: checks if pathname starts with href) */
  exact?: boolean
}

export function SidebarItem({ href, label, icon: Icon, collapsed = false, isNew = false, exact = false }: SidebarItemProps) {
  const pathname = usePathname()
  const isActive = exact ? pathname === href : pathname.startsWith(href)

  const linkContent = (
    <Link
      href={href}
      className={cn(
        'flex items-center gap-3 px-3 py-2 rounded-md text-sm transition-colors',
        'hover:bg-muted hover:text-foreground',
        isActive && 'bg-primary/10 text-primary font-medium',
        collapsed ? 'justify-center px-0 w-12 h-10 mx-auto' : 'w-full'
      )}
    >
      <Icon className={cn('h-4 w-4 flex-shrink-0', isActive ? 'text-primary' : 'text-muted-foreground')} />
      {!collapsed && (
        <span className="flex-1 truncate">{label}</span>
      )}
      {!collapsed && isNew && (
        <Badge variant="outline" className="text-[10px] px-1 py-0 h-4 border-amber-500/50 text-amber-600 dark:text-amber-400">
          ✨
        </Badge>
      )}
      {/* Left accent bar for active state */}
      {isActive && (
        <span className="absolute left-0 top-1/2 -translate-y-1/2 w-0.5 h-5 bg-primary rounded-r-full" />
      )}
    </Link>
  )

  // When collapsed, wrap in tooltip
  if (collapsed) {
    return (
      <Tooltip delayDuration={300}>
        <TooltipTrigger asChild>
          <div className="relative">{linkContent}</div>
        </TooltipTrigger>
        <TooltipContent side="right" className="text-xs">
          {label}
          {isNew && <span className="ml-1 text-amber-500">✨</span>}
        </TooltipContent>
      </Tooltip>
    )
  }

  return <div className="relative">{linkContent}</div>
}
```

Key behaviors:
- Active detection: `exact` mode checks `pathname === href`; default checks `pathname.startsWith(href)` (so "/memory/xyz" matches "/memory")
- Active state: blue left accent bar (2px, rounded-r-full) + bg-primary/10 background + text-primary color
- Collapsed mode: icon-only (48px square), centered, with Tooltip showing label on hover (300ms delay)
- New badge: subtle amber ✨ emoji badge, only visible when expanded (per CONTEXT.md: "新功能标记 ✨ 徽章")
- Icons: lucide-react LucideIcon type prop
  </action>

  <acceptance_criteria>
- Exports SidebarItem component with SidebarItemProps interface
- Uses usePathname() for active route detection
- Active state: left accent bar (bg-primary, 2px wide) + background highlight
- Collapsed mode: icon-only centered, tooltip on hover with delayDuration 300
- isNew prop: shows ✨ badge on right when true (expanded) / in tooltip (collapsed)
- exact prop: when true, requires exact pathname match (not startsWith)
- cn() used for conditional class merging
  </acceptance_criteria>

  <verify>
    <automated>grep -c "export function SidebarItem" web/components/layout/SidebarItem.tsx && grep -c "usePathname" web/components/layout/SidebarItem.tsx && grep -c "collapsed" web/components/layout/SidebarItem.tsx && grep -c "isActive" web/components/layout/SidebarItem.tsx && grep -c "Tooltip" web/components/layout/SidebarItem.tsx && grep -c "isNew" web/components/layout/SidebarItem.tsx</automated>
  </verify>

  <done>SidebarItem created with active indicator, collapsed tooltip, and new feature badge.</done>
</task>

<task type="auto">
  <name>Task 2: Create Sidebar navigation container</name>
  <files>web/components/layout/Sidebar.tsx</files>

  <read_first>
@web/components/layout/SidebarItem.tsx (created in Task 1 — understand SidebarItemProps interface)
@web/app/providers/layout-provider.tsx (useLayout — sidebarCollapsed, toggleSidebar)
@web/components/ui/scroll-area.tsx (ScrollArea component import name)
@web/components/ui/separator.tsx (Separator component import name)
@.planning/phases/11-ui-redesign/11-CONTEXT.md (lines 128-153 — EXACT navigation labels in Chinese)
  </read_first>

  <action>
Create web/components/layout/Sidebar.tsx:

```typescript
'use client'

import { useLayout } from '@/app/providers/layout-provider'
import { SidebarItem } from '@/components/layout/SidebarItem'
import { ScrollArea } from '@/components/ui/scroll-area'
import { Separator } from '@/components/ui/separator'
import {
  Clock,
  Sparkles,
  Compass,
  Tag,
  Smile,
  Bell,
  PenLine,
  Package2,
  User,
  ChevronLeft,
  ChevronRight,
} from 'lucide-react'
import { cn } from '@/lib/utils'

// Navigation categories with items — exact labels from CONTEXT.md section 3
const NAV_SECTIONS: {
  id: string
  label: string
  items: { href: string; label: string; icon: typeof Clock; isNew: boolean; exact?: boolean }[]
}[] = [
  {
    id: 'echoes',
    label: '拾忆',
    items: [
      { href: '/',                     label: '时间轴',   icon: Clock,     isNew: false, exact: true },
      { href: '/constellation',        label: '记忆星图', icon: Sparkles,  isNew: true },
      { href: '/explore',              label: '探索模式', icon: Compass,   isNew: true },
    ],
  },
  {
    id: 'discover',
    label: '发现',
    items: [
      { href: '/tags',                 label: '标签云',   icon: Tag,       isNew: false },
      { href: '/mood',                 label: '情绪日历', icon: Smile,     isNew: true },
      { href: '/daily-echo',           label: '每日回响', icon: Bell,      isNew: true },
    ],
  },
  {
    id: 'create',
    label: '创作',
    items: [
      { href: '/weave',                label: '记忆编织', icon: PenLine,   isNew: true },
      { href: '/capsules',             label: '时间胶囊', icon: Package2,  isNew: false },
    ],
  },
  {
    id: 'profile',
    label: '我的',
    items: [
      { href: '/profile',              label: '个人画像', icon: User,      isNew: true },
    ],
  },
]

export function Sidebar() {
  const { sidebarCollapsed, toggleSidebar } = useLayout()

  return (
    <nav
      className={cn(
        'flex flex-col h-full bg-sidebar/50 border-r border-border',
        sidebarCollapsed ? 'w-12' : 'w-[200px]',
        'transition-all duration-200 ease-out'
      )}
    >
      {/* Nav items with scroll area */}
      <ScrollArea className="flex-1">
        <div className="flex flex-col gap-1 px-2 py-3">
          {NAV_SECTIONS.map((section, sectionIdx) => (
            <div key={section.id}>
              {/* Section label (hidden when collapsed) */}
              {!sidebarCollapsed && (
                <div className="px-3 py-2 text-xs font-medium text-muted-foreground/60 uppercase tracking-wider">
                  {section.label}
                </div>
              )}
              {section.items.map((item) => (
                <SidebarItem
                  key={item.href}
                  href={item.href}
                  label={item.label}
                  icon={item.icon}
                  collapsed={sidebarCollapsed}
                  isNew={item.isNew}
                  exact={item.exact}
                />
              ))}
              {/* Separator between sections (except last) */}
              {sectionIdx < NAV_SECTIONS.length - 1 && !sidebarCollapsed && (
                <Separator className="my-2 mx-3 w-auto" />
              )}
            </div>
          ))}
        </div>
      </ScrollArea>

      {/* Collapse toggle at bottom */}
      <div className="border-t border-border p-2">
        <button
          onClick={toggleSidebar}
          className={cn(
            'flex items-center justify-center w-full h-8 rounded-md text-muted-foreground',
            'hover:bg-muted hover:text-foreground transition-colors',
            sidebarCollapsed ? 'w-8 mx-auto' : 'gap-2'
          )}
          aria-label={sidebarCollapsed ? '展开侧边栏' : '收起侧边栏'}
        >
          {sidebarCollapsed ? (
            <ChevronRight className="h-4 w-4" />
          ) : (
            <>
              <ChevronLeft className="h-4 w-4" />
              <span className="text-xs">收起</span>
            </>
          )}
        </button>
      </div>
    </nav>
  )
}
```

Navigation items MATCH CONTEXT.md section 3 EXACTLY:

**拾忆**: 时间轴 (/), 记忆星图 (/constellation) ✨, 探索模式 (/explore) ✨
**发现**: 标签云 (/tags), 情绪日历 (/mood) ✨, 每日回响 (/daily-echo) ✨
**创作**: 记忆编织 (/weave) ✨, 时间胶囊 (/capsules)
**我的**: 个人画像 (/profile) ✨

Icon mapping (all from lucide-react):
- Clock (时间轴), Sparkles (记忆星图), Compass (探索模式)
- Tag (标签云), Smile (情绪日历), Bell (每日回响)
- PenLine (记忆编织), Package2 (时间胶囊)
- User (个人画像)

Key behaviors:
- Sidebar width: 200px expanded, 48px (w-12) collapsed
- Collapse toggle at bottom with chevron icon
- Section labels ("拾忆", "发现", etc.) hidden when collapsed
- Separators between sections (hidden when collapsed)
- Scroll area for overflow when many items
- Transitions: 200ms ease-out for width changes (CSS class)
  </action>

  <acceptance_criteria>
- Exports Sidebar component
- 4 navigation categories: 拾忆 (3 items), 发现 (3 items), 创作 (2 items), 我的 (1 item)
- 9 nav items total, 5 marked as new (isNew: true): 记忆星图, 探索模式, 情绪日历, 每日回响, 记忆编织, 个人画像 — wait, that's 6. Let me recount: 记忆星图 ✨, 探索模式 ✨, 情绪日历 ✨, 每日回响 ✨, 记忆编织 ✨, 个人画像 ✨ = 6 new. 时间轴, 标签云, 时间胶囊 = 3 existing. Total 9.
- Uses useLayout() for sidebarCollapsed and toggleSidebar
- Collapse toggle button at bottom (chevron)
- Section labels hidden when collapsed
- Separators between sections hidden when collapsed
- Scroll area wraps nav items
- 200ms duration transition for width changes
  </acceptance_criteria>

  <verify>
    <automated>grep -c "export function Sidebar" web/components/layout/Sidebar.tsx && grep -c "NAV_SECTIONS" web/components/layout/Sidebar.tsx && grep -c "拾忆" web/components/layout/Sidebar.tsx && grep -c "发现" web/components/layout/Sidebar.tsx && grep -c "创作" web/components/layout/Sidebar.tsx && grep -c "我的" web/components/layout/Sidebar.tsx && grep -c "toggleSidebar" web/components/layout/Sidebar.tsx && grep -c "sidebarCollapsed" web/components/layout/Sidebar.tsx && grep -c "ScrollArea" web/components/layout/Sidebar.tsx</automated>
  </verify>

  <done>Sidebar created with 4-category navigation, 9 items (6 new with ✨ badges), collapse toggle, and responsive width.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Navigation-only component.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Information Disclosure | Sidebar links to future pages | accept | Placeholder pages for new routes (constellation, explore, etc.) created in Plan 12; until then, they simply 404 — no sensitive data exposed |
</threat_model>

<verification>
After all tasks complete:
1. Sidebar.tsx contains NAV_SECTIONS array with exactly 4 categories and 9 items
2. SidebarItem.tsx detects active route via usePathname()
3. Chinese labels match CONTEXT.md section 3 exactly
4. Collapsed mode shows icons only with tooltip
5. All icons are from lucide-react
</verification>

<success_criteria>
- [ ] Sidebar renders 4 categories: 拾忆, 发现, 创作, 我的
- [ ] All 9 nav items present with correct hrefs and icons
- [ ] Active page has left accent bar indicator
- [ ] Collapse toggle works (200px <-> 48px)
- [ ] Collapsed items show tooltip on hover
- [ ] New features have ✨ badges
- [ ] Section labels and separators hide when collapsed
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-redesign/11-06-SUMMARY.md`
</output>
