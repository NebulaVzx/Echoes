---
phase: 11-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/design-tokens/colors.json
  - shared/design-tokens/spacing.json
  - shared/design-tokens/breakpoints.json
  - shared/design-tokens/typography.json
  - shared/design-tokens/index.ts
  - shared/design-tokens/tailwind-preset.ts
  - web/package.json
autonomous: true
requirements:
  - REQ-11-12

must_haves:
  truths:
    - "Design tokens exist as JSON files in shared/design-tokens/"
    - "all new shadcn components and @serwist/next packages installed successfully"
  artifacts:
    - path: "shared/design-tokens/colors.json"
      provides: "Neutral palette + 5 accent presets in OKLCH format"
      contains: "neutral, blue, green, orange, violet"
    - path: "shared/design-tokens/spacing.json"
      provides: "8-point scale spacing tokens"
      contains: "xs, sm, md, lg, xl, 2xl, 3xl"
    - path: "shared/design-tokens/breakpoints.json"
      provides: "Responsive breakpoint definitions"
      contains: "mobile, tablet, desktop"
    - path: "shared/design-tokens/typography.json"
      provides: "Font sizes, weights, line heights"
      contains: "body, label, heading, display"
    - path: "shared/design-tokens/tailwind-preset.ts"
      provides: "Tailwind preset consuming design tokens"
      contains: "export const echoesPreset"
  key_links:
    - from: "tailwind.config.ts"
      to: "shared/design-tokens/tailwind-preset.ts"
      via: "presets array"
      pattern: "import.*echoesPreset.*from.*design-tokens"
---

<objective>
Install Phase 11 dependencies and extract design tokens into a shared format decoupled from Tailwind.

Purpose: New shadcn components (tooltip, dropdown-menu, command, etc.) are needed for the layout shell. @serwist/next enables PWA offline caching. Design tokens in shared/design-tokens/ serve as the single source of truth for colors, spacing, breakpoints, and typography — consumable by both the web (Tailwind preset) and future Expo builds (JSON import).
Output: Installed packages + shared/design-tokens/ directory with 4 JSON files + index + Tailwind preset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-redesign/11-RESEARCH.md
@.planning/phases/11-ui-redesign/11-UI-SPEC.md
@web/package.json
@web/components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install new shadcn components and npm packages</name>
  <files>web/package.json</files>

  <read_first>
@web/package.json (verify current versions)
@web/components.json (verify shadcn config paths)
  </read_first>

  <action>
Install the following new dependencies:

1. Install shadcn components (all from official @shadcn registry, base-nova preset):
```bash
cd web && npx shadcn@latest add tooltip dropdown-menu dialog command separator badge scroll-area switch avatar
```

2. Install PWA packages:
```bash
cd web && npm install @serwist/next@^9.5.10 serwist@^9.5.10
```

3. Verify all new components were created under web/components/ui/:
```
tooltip.tsx
dropdown-menu.tsx
dialog.tsx
command.tsx
separator.tsx
badge.tsx
scroll-area.tsx
switch.tsx
avatar.tsx
```

4. Verify package.json now contains @serwist/next and serwist in dependencies.

Do NOT install @tauri-apps/cli or @tauri-apps/api here — those go in apps/desktop/package.json in Plan 11.
  </action>

  <acceptance_criteria>
- All 9 new shadcn component files exist under web/components/ui/ (ls web/components/ui/tooltip.tsx web/components/ui/dropdown-menu.tsx web/components/ui/dialog.tsx web/components/ui/command.tsx web/components/ui/separator.tsx web/components/ui/badge.tsx web/components/ui/scroll-area.tsx web/components/ui/switch.tsx web/components/ui/avatar.tsx all succeed)
- package.json contains "@serwist/next" and "serwist" in dependencies
- `npx shadcn` completes without errors
- npm install completes without peer dependency errors (React 18.2 compatible)
  </acceptance_criteria>

  <verify>
    <automated>npm ls @serwist/next serwist 2>&1 | grep -E "^[├└].*(@serwist|serwist)" && echo "PACKAGES_OK" && ls web/components/ui/tooltip.tsx web/components/ui/dropdown-menu.tsx web/components/ui/dialog.tsx web/components/ui/command.tsx web/components/ui/separator.tsx web/components/ui/badge.tsx web/components/ui/scroll-area.tsx web/components/ui/switch.tsx web/components/ui/avatar.tsx 2>&1 | wc -l | grep 9</automated>
  </verify>

  <done>All new packages installed, all 9 shadcn components created, npm ls shows no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared/design-tokens JSON files</name>
  <files>
shared/design-tokens/colors.json
shared/design-tokens/spacing.json
shared/design-tokens/breakpoints.json
shared/design-tokens/typography.json
shared/design-tokens/index.ts
  </files>

  <read_first>
@.planning/phases/11-ui-redesign/11-UI-SPEC.md (sections: Spacing Scale, Typography, Color, Design Token Extraction)
  </read_first>

  <action>
Create the shared/design-tokens/ directory and populate with 4 JSON files and an index.ts. Use these EXACT values:

**shared/design-tokens/colors.json:**
```json
{
  "neutral": {
    "light": {
      "primary": "oklch(0.205 0 0)",
      "primaryForeground": "oklch(0.985 0 0)",
      "ring": "oklch(0.708 0 0)"
    },
    "dark": {
      "primary": "oklch(0.985 0 0)",
      "primaryForeground": "oklch(0.205 0 0)",
      "ring": "oklch(0.556 0 0)"
    }
  },
  "accents": {
    "blue": {
      "light": {
        "primary": "oklch(0.546 0.245 262.881)",
        "primaryForeground": "oklch(0.985 0 0)",
        "ring": "oklch(0.623 0.214 259.815)"
      },
      "dark": {
        "primary": "oklch(0.623 0.214 259.815)",
        "primaryForeground": "oklch(0.985 0 0)",
        "ring": "oklch(0.546 0.245 262.881)"
      }
    },
    "green": {
      "light": {
        "primary": "oklch(0.596 0.145 163.225)",
        "primaryForeground": "oklch(0.985 0 0)",
        "ring": "oklch(0.665 0.12 158.18)"
      },
      "dark": {
        "primary": "oklch(0.665 0.12 158.18)",
        "primaryForeground": "oklch(0.985 0 0)",
        "ring": "oklch(0.596 0.145 163.225)"
      }
    },
    "orange": {
      "light": {
        "primary": "oklch(0.646 0.192 41.116)",
        "primaryForeground": "oklch(0.985 0 0)",
        "ring": "oklch(0.704 0.18 48.54)"
      },
      "dark": {
        "primary": "oklch(0.704 0.18 48.54)",
        "primaryForeground": "oklch(0.985 0 0)",
        "ring": "oklch(0.646 0.192 41.116)"
      }
    },
    "violet": {
      "light": {
        "primary": "oklch(0.545 0.238 291.664)",
        "primaryForeground": "oklch(0.985 0 0)",
        "ring": "oklch(0.606 0.24 292.685)"
      },
      "dark": {
        "primary": "oklch(0.606 0.24 292.685)",
        "primaryForeground": "oklch(0.985 0 0)",
        "ring": "oklch(0.545 0.238 291.664)"
      }
    }
  },
  "presetNames": {
    "neutral": "石墨灰",
    "blue": "深海蓝",
    "green": "森林绿",
    "orange": "珊瑚橙",
    "violet": "紫罗兰"
  }
}
```

**shared/design-tokens/spacing.json:**
```json
{
  "scale": {
    "xs": "4px",
    "sm": "8px",
    "md": "16px",
    "lg": "24px",
    "xl": "32px",
    "2xl": "48px",
    "3xl": "64px"
  },
  "layout": {
    "sidebarWidth": "200px",
    "sidebarCollapsedWidth": "48px",
    "rightPanelWidth": "280px",
    "rightPanelMinWidth": "200px",
    "rightPanelMaxWidth": "400px",
    "headerHeight": "48px",
    "mobileDockHeight": "56px",
    "contentMaxWidthTimeline": "640px",
    "contentMaxWidthDetail": "720px",
    "contentMaxWidthFull": "100%"
  }
}
```

**shared/design-tokens/breakpoints.json:**
```json
{
  "mobile": { "max": "767px" },
  "tablet": { "min": "768px", "max": "1279px" },
  "desktop": { "min": "1280px" },
  "tailwind": {
    "mobile": "767",
    "tablet": "768",
    "desktop": "1280"
  }
}
```

**shared/design-tokens/typography.json:**
```json
{
  "body": { "size": "14px", "weight": "400", "lineHeight": "1.5", "tailwindClass": "text-sm" },
  "label": { "size": "12px", "weight": "400", "lineHeight": "1.25", "tailwindClass": "text-xs" },
  "heading": { "size": "20px", "weight": "600", "lineHeight": "1.2", "tailwindClass": "text-xl font-semibold" },
  "display": { "size": "28px", "weight": "600", "lineHeight": "1.1", "tailwindClass": "text-3xl font-semibold" },
  "fontFamily": {
    "sans": ["Inter", "system-ui", "-apple-system", "sans-serif"],
    "variable": ["Geist", "sans-serif"]
  }
}
```

**shared/design-tokens/index.ts:**
```typescript
export { default as colors } from './colors.json'
export { default as spacing } from './spacing.json'
export { default as breakpoints } from './breakpoints.json'
export { default as typographies } from './typography.json'

export type AccentPreset = 'neutral' | 'blue' | 'green' | 'orange' | 'violet'
export type DensityMode = 'compact' | 'comfortable' | 'loose'
```

Create the shared/design-tokens/ directory if it doesn't exist (mkdir -p shared/design-tokens).

**Important:** JSON files MUST use `.json` extension (not `.ts`), so they can be imported by both TypeScript and potential non-TS consumers (Expo, future tooling). The index.ts re-exports them.
  </action>

  <acceptance_criteria>
- shared/design-tokens/colors.json exists with neutral + 4 accent presets in OKLCH format
- shared/design-tokens/spacing.json exists with 7 scale tokens + 8 layout-specific values
- shared/design-tokens/breakpoints.json exists with mobile/tablet/desktop definitions
- shared/design-tokens/typography.json exists with body/label/heading/display specs
- shared/design-tokens/index.ts exists with named exports of all 4 JSON files + type exports
- All numeric values match UI-SPEC.md exactly
  </acceptance_criteria>

  <verify>
    <automated>grep -c '"blue"' shared/design-tokens/colors.json && grep -c '"sidebarWidth"' shared/design-tokens/spacing.json && grep -c '"desktop"' shared/design-tokens/breakpoints.json && grep -c '"body"' shared/design-tokens/typography.json && grep -c 'export' shared/design-tokens/index.ts</automated>
  </verify>

  <done>Design tokens extracted to shared/design-tokens/ with JSON files and TypeScript exports.</done>
</task>

<task type="auto">
  <name>Task 3: Create Tailwind preset from design tokens</name>
  <files>shared/design-tokens/tailwind-preset.ts</files>

  <read_first>
@shared/design-tokens/colors.json (created in Task 2)
@shared/design-tokens/spacing.json (created in Task 2)
@shared/design-tokens/breakpoints.json (created in Task 2)
@shared/design-tokens/typography.json (created in Task 2)
@web/tailwind.config.ts (existing config, understand current structure)
  </read_first>

  <action>
Create shared/design-tokens/tailwind-preset.ts as a Tailwind CSS preset that imports design token JSON files. This preset will be consumed by web/tailwind.config.ts.

```typescript
import type { Config } from 'tailwindcss'

const preset: Partial<Config> = {
  theme: {
    extend: {
      screens: {
        'mobile': { max: '767px' },
        'tablet': { min: '768px', max: '1279px' },
        'desktop': { min: '1280px' },
      },
      spacing: {
        'layout-sidebar': '200px',
        'layout-sidebar-collapsed': '48px',
        'layout-panel': '280px',
        'layout-header': '48px',
        'layout-dock': '56px',
        'content-timeline': '640px',
        'content-detail': '720px',
      },
      maxWidth: {
        'content-timeline': '640px',
        'content-detail': '720px',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
        display: ['Geist', 'sans-serif'],
      },
      fontSize: {
        'body': ['14px', { lineHeight: '1.5', fontWeight: '400' }],
        'label': ['12px', { lineHeight: '1.25', fontWeight: '400' }],
        'heading': ['20px', { lineHeight: '1.2', fontWeight: '600' }],
        'display': ['28px', { lineHeight: '1.1', fontWeight: '600' }],
      },
    },
  },
}

export default preset
```

Note: Do NOT use `require()` for JSON imports — the file is `.ts` and should use ES module syntax. To keep it simple, the values are inlined from the JSON files rather than importing JSON (which may require resolveJsonModule in tsconfig). If the project's tsconfig has `"resolveJsonModule": true`, you may import directly; otherwise, use the inlined values shown above.

The spacing tokens `layout-*` and `content-*` are new Tailwind spacing utilities that can be used like `w-layout-sidebar` or `max-w-content-timeline` in components.
  </action>

  <acceptance_criteria>
- shared/design-tokens/tailwind-preset.ts exists
- File exports a valid Tailwind `Config` partial
- Includes screen breakpoints matching UI-SPEC: desktop (1280px), tablet (768-1279px), mobile (<768px)
- Includes custom spacing tokens: layout-sidebar, layout-sidebar-collapsed, layout-panel, layout-header, layout-dock, content-timeline, content-detail
- Font sizes for body/label/heading/display match UI-SPEC
  </acceptance_criteria>

  <verify>
    <automated>grep -c "layout-sidebar" shared/design-tokens/tailwind-preset.ts && grep -c "content-timeline" shared/design-tokens/tailwind-preset.ts && grep -c "1280px" shared/design-tokens/tailwind-preset.ts</automated>
  </verify>

  <done>Tailwind preset created that consumes design tokens for spacing, breakpoints, and typography.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 11 is a frontend-only refactor. No new trust boundaries are introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | localStorage (accent/density/sidebar prefs) | accept | Layout preferences are non-security-critical cosmetic values; same-origin isolation via browser |
| T-11-02 | Information Disclosure | Right Panel widgets | mitigate | Panel content respects existing AuthProvider — useAuth() gate on all panel widgets |
| T-11-03 | Spoofing | Command Palette input | mitigate | Command palette is display-only navigation; actual memory access gate-checked by backend API auth |
</threat_model>

<verification>
After all tasks complete:
1. `ls shared/design-tokens/` shows colors.json, spacing.json, breakpoints.json, typography.json, index.ts, tailwind-preset.ts
2. `npm ls @serwist/next serwist` shows both packages installed
3. `ls web/components/ui/ | wc -l` shows >= 13 component files (4 existing + 9 new)
</verification>

<success_criteria>
- [ ] All 9 new shadcn components exist in web/components/ui/
- [ ] @serwist/next and serwist installed at ^9.5.10 in package.json
- [ ] shared/design-tokens/ contains colors.json, spacing.json, breakpoints.json, typography.json
- [ ] Design token values match UI-SPEC.md exactly
- [ ] Tailwind preset is importable from tailwind.config.ts
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-redesign/11-01-SUMMARY.md`
</output>
