---
phase: 11
name: ui-redesign
title: UI 架构重设计 — "工作台"
description: 从单栏信息流升级为三栏自适应工作台，解决功能增长后的导航和空间瓶颈，融入AI时代的交互特征
milestone: v1.3 "记忆的回响"
depends_on: []
---

# Phase 11 Context

## 问题诊断

### 当前布局现状

```
┌──────────────────────────────────────────┐
│ Header: Logo + Search + AI + Theme +     │  ← 拥挤，已塞不下更多入口
│         Capsule + Settings + User + Exit │
├──────────────────────────────────────────┤
│                                          │
│    max-w-3xl 窄栏居中                    │  ← 大屏幕上左右大量空白
│    (WarmthCards + CreateForm +           │
│     TagFilter + MemoryList)              │
│                                          │
└──────────────────────────────────────────┘
```

### 核心问题

1. **Header 过载**：已有 AI、主题、时间胶囊、设置、用户名、退出 —— 规划中的星图、探索、情绪、编织等无处安放
2. **空间浪费**：桌面端 1920px 宽屏，主内容只占 768px，左右各空 576px
3. **无全局导航**：每个页面各自为政，新用户找不到功能入口
4. **缺乏层级**：设置页和首页之间缺少"中间层"（个人资料、数据仪表盘）
5. **无快捷方式**：没有 Command Palette、快捷键体系，高级用户效率低

### 新增页面清单（v1.3 规划）

| 页面 | 当前入口 | 问题 |
|------|---------|------|
| 时间轴（首页） | Header Logo | ✅ 有入口 |
| 搜索 | Header 搜索框 | ✅ 有入口 |
| 记忆详情 | 点击卡片 | ✅ 有入口 |
| 标签管理 | Header "标签" | ✅ 有入口 |
| 时间胶囊 | Header "胶囊" | ⚠️ 拥挤 |
| 设置 | Header "设置" | ⚠️ 拥挤 |
| **记忆星图** | ❌ 无 | 没地方放 |
| **探索模式** | ❌ 无 | 没地方放 |
| **情绪日历** | ❌ 无 | 没地方放 |
| **记忆编织** | ❌ 无 | 没地方放 |
| **每日回响** | ❌ 无 | 没地方放 |
| **用户画像** | ❌ 无 | 没地方放 |

---

## 改造目标

**从"单栏信息流"升级为"三栏自适应工作台"**

| 维度 | 改造前 | 改造后 |
|------|--------|--------|
| 导航 | Header 按钮堆砌 | 左侧 Sidebar + 底部 Dock |
| 内容区 | max-w-3xl 固定 | 自适应三栏，按场景调整 |
| 空间利用 | 左右空白 | 右侧面板显示上下文 |
| AI 交互 | Chat 侧边栏独立 | 融入全局，支持命令面板 |
| 快捷方式 | 无 | Cmd+K 命令面板 + 快捷键 |

---

## 设计方案

### 1. 全局布局：三栏自适应

```
桌面端（≥1280px）
┌──────────────────────────────────────────────────────────────┐
│ Header: Logo + 全局搜索 + 用户头像菜单                        │
├─────────┬────────────────────────────────────┬───────────────┤
│         │                                    │               │
│ Sidebar │     主内容区（按场景自适应宽度）    │  Right Panel  │
│ (导航)  │                                    │  (上下文面板)  │
│  200px  │                                    │   280px       │
│         │                                    │               │
└─────────┴────────────────────────────────────┴───────────────┘

平板端（768px-1279px）
┌──────────────────────────────────────────┐
│ Header: Logo + 搜索 + 汉堡菜单 + 用户头像  │
├──────────────────────────────────────────┤
│                                          │
│  主内容区（隐藏 Sidebar 和 Right Panel）  │
│                                          │
└──────────────────────────────────────────┘

移动端（<768px）
┌──────────────────────────────────────────┐
│ Header: Logo + 搜索 + 用户头像             │
├──────────────────────────────────────────┤
│                                          │
│  主内容区                                 │
│                                          │
├──────────────────────────────────────────┤
│  [拾][星][胶][标][我]    Dock 导航        │
└──────────────────────────────────────────┘
```

### 2. Header 精简

**改造前**：Logo + Search + AI按钮 + Theme + Capsule + Settings + Username + Exit
**改造后**：
- 左侧：Logo（点击回首页）
- 中间：全局搜索（扩大，支持 AI 自然语言搜索）
- 右侧：用户头像（点击展开菜单：个人资料、设置、主题、退出）

```
Header
├─ Logo ── 全局搜索 ─────────────── 用户头像 ─┤
                                    └─ 下拉菜单
                                        ├─ 👤 我的画像
                                        ├─ ⚙️ 设置
                                        ├─ 🌙 暗黑/亮色
                                        └─ 🚪 退出
```

### 3. 左侧 Sidebar 导航

**一级导航（图标 + 文字）**：

```
拾忆
├── 时间轴        (默认页)
├── 记忆星图      ✨ 新
└── 探索模式      ✨ 新

发现
├── 标签云
├── 情绪日历      ✨ 新
└── 每日回响      ✨ 新

创作
├── 记忆编织      ✨ 新
└── 时间胶囊

我的
└── 个人画像      ✨ 新
```

**设计细节**：
- 当前页面高亮（背景色 + 左侧竖条指示器）
- 悬停显示 tooltip
- 可折叠（收起后只显示图标）
- 新功能标记 ✨ 徽章

### 4. 右侧面板（上下文面板）

根据当前页面动态显示：

| 当前页面 | 右侧面板内容 |
|---------|-------------|
| **时间轴** | 那年今日卡片、热门标签云、快捷操作（随机探索） |
| **记忆详情** | 相似记忆推荐、标签云、探索入口、来源信息 |
| **星图** | 筛选器（标签/时间范围/相似度阈值）、选中节点详情 |
| **探索** | 探索路径面包屑、收藏的记忆、快捷返回 |
| **编织** | 已选记忆列表、导出格式选择、预览 |
| **情绪** | 本月情绪摘要、情绪标签排行、AI 洞察 |
| **搜索** | 搜索历史、筛选器、相关标签 |

**右侧面板特性**：
- 可折叠（点击箭头隐藏）
- 可拖拽调整宽度（200px-400px）
- 为空时自动隐藏

### 5. 主内容区自适应宽度

| 场景 | 宽度 | 说明 |
|------|------|------|
| 时间轴列表 | 640px | 保持阅读舒适 |
| 记忆详情 | 720px | 稍宽，适合长文 |
| 记忆星图 | 100% | 全宽，最大化画布 |
| 探索模式 | 100% | 全宽，沉浸式 |
| 情绪日历 | 960px | 需要横向空间 |
| 记忆编织 | 720px | 编辑器需要适中宽度 |

### 6. Command Palette（命令面板）

**触发**：Cmd/Ctrl + K

**功能**：
```
搜索记忆...          (直接搜索)
跳转页面             (时间轴/星图/探索/标签...)
创建记忆             (快速弹出创建框)
打开设置             (直接跳设置)
切换主题             (暗黑/亮色)
随机探索             (进入探索模式)
查看画像             (打开个人画像)
```

**交互**：
- 输入 `/` 快速分类（/记忆 /标签 /设置）
- 输入 `>` 执行命令（>导出 >删除）
- 支持 emoji 搜索（输入 🔥 找到热门记忆）

### 7. AI 时代的 UI 特征融入

#### 生成式界面元素
- **动态 Placeholder**：输入框的 placeholder 根据时间/上下文变化
  - 早上："早上好，今天想记录什么？"
  - 深夜："夜深了，还有什么灵感没记下？"
  - 连续记录 7 天后："第 7 天！继续保持 🔥"
- **AI 搜索框**：搜索框内嵌 AI 助手图标，点击切换"语义搜索"和"AI 问答"
- **智能空状态**：空状态文案由 AI 根据用户历史生成（"你的知识库还缺 AI 相关的内容哦"）

#### 流式反馈
- **记忆封面生成**：骨架屏 + 渐进式加载（从模糊到清晰）
- **AI 建议**：打字机效果实时输出
- **向量搜索**：搜索过程中显示"正在检索 1,247 条记忆..."的实时状态
- **文件解析**：进度条 + 已提取字数实时更新

#### 智能密度调节
- **密度设置**：紧凑 / 舒适 / 宽松（用户偏好）
- **紧凑模式**：卡片更小（60px）、间距 8px、显示更多标签
- **舒适模式**：当前默认（卡片 80-120px、间距 16px）
- **宽松模式**：卡片更大（140px+）、间距 24px、更利于阅读

#### 沉浸式探索
- **全屏模式**：星图和探索模式支持 `F` 键全屏
- **手势支持**：移动端左右滑动切换主页面
- **聚焦模式**：记忆详情页按 `F` 隐藏所有导航，只显示内容

### 8. 趣味性与品味

#### 视觉彩蛋
- **里程碑庆祝**：记录达到 100/500/1000 条时，页面有 subtle 的粒子动画（confetti-lite，不打扰）
- **Streak 火焰**：连续记录天数在 Sidebar 用户头像旁显示火焰动画
- **时间胶囊解锁**：全屏渐变遮罩 + 记忆卡片从模糊到清晰的揭示动画
- **暗黑切换**：过渡动画（300ms 渐变，非瞬间切换）

#### 微交互
- **卡片悬停**：微妙的抬升（translateY -2px）+ 阴影加深
- **标签悬停**：颜色从点扩散到整个标签背景
- **星标点击**：弹跳动画（scale 1.2 → 1.0）+ 金色闪光
- **保存成功**：记忆匣图标闪烁 + "已存入" toast 从底部滑出

#### 个性化
- **主题色**：用户可选择强调色（不只是灰度）
  - 预设：石墨灰（默认）、深海蓝、森林绿、珊瑚橙、紫罗兰
- **字体选择**：Inter（默认）、系统默认、等宽（代码爱好者）
- **卡片样式**：简洁（无封面）/ 丰富（有封面+摘要）/ 极简（只标题）

---

## 响应式策略

| 断点 | 布局 | Sidebar | Right Panel | Dock |
|------|------|---------|-------------|------|
| ≥1280px | 三栏 | 固定 200px | 固定 280px | 隐藏 |
| 1024-1279px | 两栏 | 固定 200px | 隐藏 | 隐藏 |
| 768-1023px | 两栏 | 可折叠抽屉 | 隐藏 | 隐藏 |
| <768px | 单栏 | 隐藏 | 隐藏 | 底部固定 |

---

## 技术实现

### 新增组件

```
web/components/layout/
├── AppShell.tsx          # 三栏布局壳
├── Sidebar.tsx           # 左侧导航
├── SidebarItem.tsx       # 导航项
├── RightPanel.tsx        # 右侧面板
├── RightPanelWidget.tsx  # 面板小部件
├── MobileDock.tsx        # 移动端底部导航
├── Header.tsx            # 精简版 Header
├── UserMenu.tsx          # 用户头像下拉菜单
├── CommandPalette.tsx    # Cmd+K 命令面板
└── DensityToggle.tsx     # 密度切换

web/components/ui/
├── confetti-lite.tsx     # 轻量级庆祝动画
├── streak-flame.tsx      # 火焰动画
├── progress-radial.tsx   # 径向进度条
└── theme-color-picker.tsx # 主题色选择器
```

### 全局状态

```typescript
// 布局状态
interface LayoutState {
  sidebarCollapsed: boolean      // Sidebar 是否折叠
  rightPanelVisible: boolean     // 右侧面板是否显示
  rightPanelWidth: number        // 右侧面板宽度
  density: 'compact' | 'comfortable' | 'loose'
  themeColor: string             // 强调色
  fontFamily: 'inter' | 'system' | 'mono'
}

// 命令面板状态
interface CommandPaletteState {
  isOpen: boolean
  query: string
  results: CommandResult[]
}
```

---

## 验收标准

- [ ] 桌面端三栏布局稳定，无水平滚动条
- [ ] 移动端底部 Dock 导航可用，切换流畅
- [ ] Cmd+K 命令面板可搜索记忆、跳转页面、执行命令
- [ ] Header 不再拥挤，所有导航移入 Sidebar
- [ ] 右侧面板根据页面动态显示正确内容
- [ ] 密度切换实时生效，无刷新
- [ ] 主题色切换实时生效，无刷新
- [ ] 所有现有页面在新布局下正常显示
- [ ] 暗黑/亮色切换有过渡动画
- [ ] PWA 基础配置完成（manifest、icons、service worker）
- [ ] Tauri 桌面端最小可运行封装
- [ ] 设计 Token 提取为 `shared/design-tokens`，与 Tailwind 解耦
- [ ] 移动端（<768px）信息架构重组完成，非简单缩放

---

## 跨平台架构策略

> **原则：最多两套前端** —— Web + 桌面端共享一套代码，移动端独立一套（先 PWA，后 Expo）。

### 平台矩阵

| 平台 | 技术方案 | 与 Web 共享程度 | 优先级 |
|------|---------|----------------|--------|
| **Web** | Next.js 14（现有） | 100% | P0 |
| **桌面端** | Next.js + Tauri 2.0 封装 | 100%（同一套代码） | P1 |
| **移动端** | PWA（响应式 + Service Worker） | 100%（同一套代码） | P1 |
| **移动端（未来）** | Expo（React Native） | API + 设计 Token + 类型 | P2（v1.5+ 评估） |

### 为什么桌面端用 Tauri 而不是 Electron？

| 维度 | Tauri 2.0 | Electron |
|------|-----------|----------|
| 包体积 | ~3MB | ~150MB |
| 内存占用 | 系统 WebView | 自带 Chromium |
| 本地扩展 | Rust 侧可跑 SQLite/ONNX | Node.js 绑定 |
| 安全模型 | 显式 API allowlist | nodeIntegration 风险 |
| 未来扩展 | 与 Echoes 可能的 Rust 向量库天然兼容 | 额外依赖 |

桌面端本质是**无边框浏览器窗口 + 系统级能力**（全局快捷键、本地文件系统、离线优先）。Next.js 代码零改动，仅需 `tauri.conf.json` 配置窗口参数。

### 移动端：先 PWA，后 Expo

**Phase 11-12 先落地 PWA**：
- `manifest.json` + `icons` + `theme-color`
- Service Worker 缓存静态资源（Workbox）
- 响应式断点适配三栏 → 单栏折叠
- 这已经能解决 80% 的移动场景（快速记录、查看回响、搜索记忆）

**v1.5+ 再评估 Expo**：
- 只有在数据表明用户高频使用移动端时，才投入独立 App 开发
- 届时 API 层已稳定，只需重写 UI 层
- Expo 可复用 `shared/api`、`shared/types`、`shared/design-tokens`

### 共享层设计

所有平台共用以下资产，避免重复定义：

```
shared/
├── api/              # OpenAPI 规范或统一封装的 HTTP 客户端
├── design-tokens/    # 颜色、间距、字体、断点（JSON / Tailwind preset）
│   ├── colors.json
│   ├── spacing.json
│   └── breakpoints.json
├── types/            # TypeScript 类型定义（Memory、User、Tag 等）
└── i18n/             # 文案 JSON（支持未来多语言）
```

**桌面端和 Web 端**：直接 import `shared/design-tokens`，Tailwind 配置继承 preset。

**移动端（Expo 阶段）**：React Native 可 import JSON token 文件，用 StyleSheet 动态映射。

### 本地数据与离线策略

Echoes 的核心体验是"搜索记忆"，离线时搜不到 = 体验断裂。

| 平台 | 本地存储 | 角色 | 同步策略 |
|------|---------|------|---------|
| Web | IndexedDB（Dexie.js） | 缓存 | 在线时增量同步 |
| Desktop (Tauri) | SQLite（Rust 侧） | **主库** | 本地优先，后台同步云端 |
| Mobile (PWA) | IndexedDB | 缓存 | 同 Web |
| Mobile (Expo) | SQLite / WatermelonDB | 主库 | 同 Desktop |

**关键设计**：桌面端用 SQLite 作为本地主数据库，云端 PostgreSQL 作为备份/多设备同步。这让桌面端成为**体验最好的版本**（离线全功能、毫秒级搜索）。Web/PWA 阶段先保留 IndexedDB 缓存，未来再评估是否需要更复杂的同步协议。

### 三栏布局的跨平台自然适配

同一套组件代码，通过响应式状态机适配不同平台：

```
Desktop (≥1280px):
[Sidebar 200px] | [Main adaptive] | [Right Panel 280px]
    ↑                ↑                     ↑
  常驻导航        主内容区              上下文面板

Tablet (768-1279px):
[Sidebar 200px] | [Main 100%]
    ↑                ↑
  常驻导航        右侧面板隐藏

Mobile (<768px):
[Main 100%]
    ↑
  底部 Dock：[拾][星][胶][标][我]
  右侧面板 → 全屏 Modal / 新页面
```

**状态管理统一**：
- `sidebarCollapsed`、`rightPanelVisible`、`rightPanelWidth` 在同一套 React Context 中管理
- 桌面端默认 `rightPanelVisible = true`
- 移动端默认 `rightPanelVisible = false`（触发时全屏覆盖）
- 不需要为桌面端或移动端写分叉逻辑

### 对 Phase 11 实现的具体影响

| 任务 | 说明 |
|------|------|
| **提取设计 Token** | Tailwind 配置中的颜色、间距抽成 `shared/design-tokens/*.json`，供未来 Expo 复用 |
| **PWA 基础** | 新增 `manifest.json`、`icons/`、`service-worker.ts`，注册在 `app/layout.tsx` |
| **Tauri 配置** | 新建 `apps/desktop/` 目录，放置 `tauri.conf.json` 和最小 Rust 入口（~50 行） |
| **响应式断点策略** | 明确三档：Mobile `< 768px`、Tablet `768-1279px`、Desktop `≥ 1280px`，不是"缩小"而是"信息架构重组" |
| **离线缓存策略** | Service Worker 至少缓存：静态资源、最近 50 条记忆、用户设置 |

### 不在 Phase 11 做的事情（明确排除）

- ❌ **Expo 移动端 App** — 仅预留 `shared/` 结构，不实现
- ❌ **桌面端 SQLite 主库** — Tauri 侧先只做最小封装，SQLite 迁移放在 v1.4+ 或独立 Phase
- ❌ **跨设备实时同步协议** — 先依赖"刷新拉取"，CRDT/OT 是远期课题
- ❌ **原生推送通知** — PWA 的 Web Push 是未来选项，Phase 11 不实现

---
