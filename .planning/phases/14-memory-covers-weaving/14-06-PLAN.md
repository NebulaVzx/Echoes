---
phase: 14-memory-covers-weaving
plan: 06
type: execute
wave: 3
depends_on: ["14-02", "14-04"]
files_modified:
  - web/components/command/CommandPalette.tsx
  - web/components/constellation/ExplorePanel.tsx
  - web/lib/api.ts
autonomous: true
requirements:
  - WEA-01
  - D-04-04
  - D-04-05
must_haves:
  truths:
    - Command Palette 支持 /weave 命令
    - /weave 命令导航到编织页面
    - ExplorePanel 支持"编织选中记忆"按钮
    - api.ts 包含 weave 相关 API 方法
  artifacts:
    - path: web/components/command/CommandPalette.tsx
      provides: Weave command entry
      contains: "weave"
    - path: web/components/constellation/ExplorePanel.tsx
      provides: Weave button for selected nodes
    - path: web/lib/api.ts
      provides: Weave API client methods
      exports: [createWeave]
  key_links:
    - from: CommandPalette.tsx
      to: /weave
      via: router.push('/weave')
      pattern: "/weave"
    - from: ExplorePanel.tsx
      to: /weave
      via: sessionStorage + router.push
      pattern: "weave_source_ids"
---

<objective>
集成编织入口到 Command Palette 和 ExplorePanel。Command Palette 添加 `/weave` 命令，ExplorePanel 添加"编织选中记忆"按钮。

Purpose: 让用户可以从多个入口触发编织功能。
Output: Command Palette 和 ExplorePanel 都支持编织入口。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Key Interfaces (from codebase)

### CommandPalette (web/components/command/CommandPalette.tsx)
```typescript
interface CommandItem {
  id: string
  label: string
  icon: typeof Clock
  action: 'navigate' | 'toggle-theme' | 'random-explore'
  href?: string
  keywords: string[]
  group: string
}

const COMMANDS: CommandItem[] = [
  { id: 'weave', label: '记忆编织', icon: PenLine, action: 'navigate', href: '/weave', keywords: ['编织', 'weave'], group: '页面' },
  // ...
]
```

### ExplorePanel (web/components/constellation/ExplorePanel.tsx)
```typescript
interface ExplorePanelProps {
  memoryId: string
  onDrillDown?: (memoryId: string, label: string) => void
  breadcrumb?: ExploreState[]
  onNavigateBreadcrumb?: (index: number) => void
  onClearBreadcrumb?: () => void
}
```

### API client (web/lib/api.ts)
```typescript
// Need to add createWeave method
class ApiClient {
  // ... existing methods
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 api.ts 添加 weave API 方法</name>
  <files>web/lib/api.ts</files>
  <action>
per D-04-04, D-04-06:

1. 更新 Memory 类型的 content_type：
```typescript
export interface Memory {
  id: string
  user_id: string
  content_type: 'text' | 'link' | 'file' | 'weave'  // Add 'weave'
  // ...
}
```

2. 在 ApiClient 类中添加 createWeave 方法：
```typescript
async createWeave(data: {
  source_ids: string[]
  mode: string
  title?: string
}): Promise<ApiResponse<{ memory: Memory }>> {
  return this.request<{ memory: Memory }>('POST', '/api/v1/memories/weave', data)
}
```

3. 确保 Memory 接口包含 cover_url 字段（已存在，确认即可）。

注意：api.ts 当前 524 行，职责较重。按 ROADMAP v1.4 规划未来会拆分，但现在只添加必要方法。
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit lib/api.ts</automated>
  </verify>
  <done>
    - Memory.content_type 包含 'weave'
    - api.createWeave 方法存在
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 CommandPalette 添加 weave 命令</name>
  <files>web/components/command/CommandPalette.tsx</files>
  <action>
per D-04-04:

CommandPalette 已经包含 weave 命令（第 48 行）：
```typescript
{ id: 'weave', label: '记忆编织', icon: PenLine, action: 'navigate', href: '/weave', keywords: ['编织', 'weave'], group: '页面' },
```

但需要修改行为：直接导航到 `/weave` 会导致 source_ids 为空（因为 weave 页面需要从 sessionStorage 读取）。

修改方案：
1. 保持现有命令，但修改 `runCommand` 逻辑：
   - 当 weave 命令被触发时，检查是否有已选择的记忆
   - 如果有（从 sessionStorage 读取 weave_source_ids），直接导航到 `/weave`
   - 如果没有，显示提示"请先在时间轴选择记忆"

2. 或者更简单： weave 命令直接导航到 `/weave`，由 weave 页面处理空状态（已实现在 Plan 14-05 Task 2）。

当前实现已经正确，只需确认 weave 命令存在且 href='/weave'。无需修改。

但 per D-04-04，Command Palette 应该支持 `/weave` 前缀触发。当前过滤逻辑已支持（输入 "weave" 或 "编织" 即可匹配）。

确认现有代码即可。如果需要增强，可以添加 `/weave` 作为快捷前缀：
```typescript
// In filteredCommands logic, add special handling for "/weave"
const isWeaveCommand = query.toLowerCase() === '/weave'
if (isWeaveCommand) {
  // Directly navigate to weave page
  router.push('/weave')
  setOpen(false)
  return
}
```

实际上，保持简单：现有命令已足够。用户输入 "weave" 或 "编织" 即可找到命令。
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit components/command/CommandPalette.tsx</automated>
  </verify>
  <done>
    - CommandPalette 包含 weave 命令
    - 点击后导航到 /weave
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 ExplorePanel 添加编织按钮</name>
  <files>web/components/constellation/ExplorePanel.tsx</files>
  <action>
per D-04-05:

在 ExplorePanel 中添加"编织选中记忆"按钮。需要：

1. 扩展 ExplorePanelProps：
```typescript
interface ExplorePanelProps {
  memoryId: string
  onDrillDown?: (memoryId: string, label: string) => void
  breadcrumb?: ExploreState[]
  onNavigateBreadcrumb?: (index: number) => void
  onClearBreadcrumb?: () => void
  selectedNodeIds?: string[]  // NEW: selected constellation node IDs
  onWeaveSelected?: () => void  // NEW: callback to weave selected nodes
}
```

2. 在 ExplorePanel 头部或底部添加编织按钮：
```tsx
export function ExplorePanel({
  memoryId,
  onDrillDown,
  breadcrumb = [],
  onNavigateBreadcrumb,
  onClearBreadcrumb,
  selectedNodeIds = [],
  onWeaveSelected,
}: ExplorePanelProps) {
  // ... existing code

  return (
    <div className="h-full flex flex-col">
      {/* Breadcrumb */}
      <div className="px-4 py-3 border-b border-gray-200 dark:border-gray-700">
        <BreadcrumbTrail
          items={breadcrumbItems}
          onNavigate={onNavigateBreadcrumb}
          onClear={onClearBreadcrumb}
        />
      </div>

      {/* Weave button for selected nodes */}
      {selectedNodeIds.length >= 2 && onWeaveSelected && (
        <div className="px-4 py-2 border-b border-gray-200 dark:border-gray-700 bg-primary/5">
          <button
            onClick={onWeaveSelected}
            className="flex items-center gap-2 text-sm text-primary font-medium hover:text-primary/80 transition-colors"
          >
            <PenLine className="w-4 h-4" />
            编织选中的 {selectedNodeIds.length} 条记忆
          </button>
        </div>
      )}

      {/* Results */}
      <ScrollArea className="flex-1">
        {/* ... existing result cards */}
      </ScrollArea>
    </div>
  )
}
```

3. 导入 PenLine 图标：
```tsx
import { Sparkles, PenLine } from 'lucide-react'
```

注意：ExplorePanel 本身不管理选择状态（selectedNodeIds 由父组件 ConstellationGraph 提供）。
需要在 ConstellationGraph 或相关页面中添加节点选择逻辑，但这超出了本 Plan 的范围。

本 Plan 只负责 ExplorePanel 的 UI 入口。节点选择逻辑在 ConstellationGraph 中实现（如果尚未实现）。
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit components/constellation/ExplorePanel.tsx</automated>
  </verify>
  <done>
    - ExplorePanel 接收 selectedNodeIds 和 onWeaveSelected props
    - 当 selectedNodeIds >= 2 时显示"编织选中记忆"按钮
    - 按钮样式与主题一致
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI | Pure client-side navigation; no security boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-19 | Information Disclosure | sessionStorage weave_source_ids | accept | User-owned data; API validates ownership |
| T-14-20 | Tampering | ExplorePanel node selection | accept | Parent component manages selection state; this plan only adds UI button |
</threat_model>

<verification>
- Command Palette 输入 "weave" 或 "编织" 显示"记忆编织"命令
- 点击后导航到 /weave
- ExplorePanel 传入 selectedNodeIds >= 2 时显示编织按钮
- 点击编织按钮触发 onWeaveSelected callback
</verification>

<success_criteria>
- [ ] CommandPalette 支持 weave 命令导航
- [ ] api.ts 包含 createWeave 方法
- [ ] Memory.content_type 包含 'weave'
- [ ] ExplorePanel 显示编织选中记忆按钮（当 selectedNodeIds >= 2）
- [ ] npm run build 无错误
</success_criteria>

<output>
After completion, create `.planning/phases/14-memory-covers-weaving/14-06-SUMMARY.md`
</output>
