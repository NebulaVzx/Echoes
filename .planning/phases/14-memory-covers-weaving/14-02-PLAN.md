---
phase: 14-memory-covers-weaving
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/memory-service/internal/domain/memory.go
  - services/memory-service/internal/service/memory_service.go
  - services/memory-service/internal/transport/memory_handler.go
autonomous: true
requirements:
  - COV-01
  - WEA-02
  - WEA-03
must_haves:
  truths:
    - CreateMemoryRequest accepts content_type="weave"
    - TaskStatusUpdate accepts task_type="cover:generate"
    - POST /api/v1/memories/weave creates a weave memory with LLM-generated content
    - Weave memory metadata contains weave_source_ids
    - Weave content includes [^1], [^2] source citations
    - Four weave modes supported: article, story, summary, todo
  artifacts:
    - path: services/memory-service/internal/domain/memory.go
      provides: Updated binding validations
      contains: "oneof=text link file weave"
    - path: services/memory-service/internal/service/memory_service.go
      provides: Weave service method
      exports: [WeaveMemories]
    - path: services/memory-service/internal/transport/memory_handler.go
      provides: Weave HTTP endpoint
      exports: [Weave]
  key_links:
    - from: memory_handler.go
      to: memory_service.go
      via: POST /memories/weave -> WeaveMemories()
      pattern: "WeaveMemories"
    - from: memory_service.go
      to: domain.TaskStatusUpdate
      via: binding validation
      pattern: "cover:generate"
---

<objective>
实现 AI 编织 API。扩展 domain 验证支持 content_type="weave" 和 task_type="cover:generate"，新增 POST /api/v1/memories/weave 端点，支持四种编织模式（article/story/summary/todo），生成带来源标注的编织文章并持久化为新 memory。

Purpose: 让用户可以将多条记忆编织成连贯文章。
Output: Weave endpoint 可用，返回 weave memory ID。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Key Interfaces (from codebase)

### Memory domain (services/memory-service/internal/domain/memory.go)
```go
type CreateMemoryRequest struct {
    ContentType        string     `json:"content_type" binding:"required,oneof=text link file"`
    // ...
}

type TaskStatusUpdate struct {
    TaskType string                 `json:"task_type" binding:"required,oneof=link:fetch text:vectorize tag:generate suggestion:generate file:extract"`
    Status   string                 `json:"status" binding:"required,oneof=pending processing completed failed"`
    // ...
}
```

### MemoryService (services/memory-service/internal/service/memory_service.go)
```go
func (s *MemoryService) Create(ctx context.Context, userID uuid.UUID, req domain.CreateMemoryRequest) (*domain.Memory, string, error)
func (s *MemoryService) Get(ctx context.Context, memoryID, userID uuid.UUID) (*domain.Memory, error)
func (s *MemoryService) getUserLLMConfig(ctx context.Context, userID uuid.UUID) (map[string]interface{}, error)
func (s *MemoryService) extractContent(memory *domain.Memory) string
func (s *MemoryService) extractContentForReason(memory *domain.Memory) string
func (s *MemoryService) callOpenAIForReason(ctx context.Context, baseURL, apiKey, model string, temperature float64, prompt string) (string, error)
func (s *MemoryService) callAnthropicForReason(ctx context.Context, baseURL, apiKey, model string, temperature float64, prompt string) (string, error)
```

### MemoryHandler (services/memory-service/internal/transport/memory_handler.go)
```go
func (h *MemoryHandler) RegisterRoutes(router *gin.RouterGroup)
func (h *MemoryHandler) Create(c *gin.Context)
```

### Weave Prompts (from 14-RESEARCH.md)
```python
WEAVE_PROMPTS = {
    "article": "将以下记忆编织成一篇连贯的文章...使用 [^1], [^2] 等格式标注来源...",
    "story": "将以下记忆编织成一个连贯的故事...",
    "summary": "将以下记忆总结成一份精炼的摘要...",
    "todo": "基于以下记忆，生成一份可操作的任务清单...",
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 Domain 验证绑定</name>
  <files>services/memory-service/internal/domain/memory.go</files>
  <action>
per D-02-06, RESEARCH.md Pitfall 6, Pitfall 7:

1. 更新 `CreateMemoryRequest.ContentType` 的 binding tag：
   - 从 `oneof=text link file` 改为 `oneof=text link file weave`

2. 更新 `TaskStatusUpdate.TaskType` 的 binding tag：
   - 从 `oneof=link:fetch text:vectorize tag:generate suggestion:generate file:extract` 改为 `oneof=link:fetch text:vectorize tag:generate suggestion:generate file:extract cover:generate`

3. 添加 WeaveRequest domain 类型（可选放在 memory.go 或新建 weave.go）：
```go
type WeaveMode string

const (
    WeaveModeArticle WeaveMode = "article"
    WeaveModeStory   WeaveMode = "story"
    WeaveModeSummary WeaveMode = "summary"
    WeaveModeTodo    WeaveMode = "todo"
)

type WeaveRequest struct {
    SourceIDs []string  `json:"source_ids" binding:"required,min=2,max=20"`
    Mode      WeaveMode `json:"mode" binding:"required,oneof=article story summary todo"`
    Title     string    `json:"title" binding:"omitempty,max=200"`
}
```

注意：source_ids 最少 2 条（编织至少两条记忆），最多 20 条（防止 prompt 过长）。
  </action>
  <verify>
    <automated>cd services/memory-service && go build ./...</automated>
  </verify>
  <done>
    - CreateMemoryRequest binding 包含 "weave"
    - TaskStatusUpdate binding 包含 "cover:generate"
    - WeaveRequest 类型定义完整
    - go build 无错误
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 WeaveMemories 服务方法</name>
  <files>services/memory-service/internal/service/memory_service.go</files>
  <behavior>
    - Test 1: WeaveMemories 验证 source_ids 存在且属于当前用户
    - Test 2: 构建 LLM prompt 包含所有 source memories 的 content
    - Test 3: LLM 返回内容包含 [^1], [^2] 来源标注
    - Test 4: 解析 LLM 响应，提取正文和来源引用
    - Test 5: 创建 weave memory，content_type="weave"
    - Test 6: metadata 包含 weave_source_ids
    - Test 7: 底部自动附加来源列表（记忆标题+链接）
    - Test 8: 四种 mode 使用不同 prompt 模板
  </behavior>
  <action>
per D-05-01, D-05-02, D-05-03:

在 `memory_service.go` 中添加 `WeaveMemories` 方法：

```go
func (s *MemoryService) WeaveMemories(ctx context.Context, userID uuid.UUID, req domain.WeaveRequest) (*domain.Memory, error) {
    // 1. 验证并获取所有 source memories
    sourceMemories := make([]*domain.Memory, 0, len(req.SourceIDs))
    for _, idStr := range req.SourceIDs {
        id, err := uuid.Parse(idStr)
        if err != nil {
            return nil, fmt.Errorf("invalid source_id: %s", idStr)
        }
        mem, err := s.Get(ctx, id, userID)
        if err != nil {
            return nil, fmt.Errorf("source memory not found: %s", idStr)
        }
        sourceMemories = append(sourceMemories, mem)
    }

    // 2. 构建 LLM prompt
    llmConfig, _ := s.getUserLLMConfig(ctx, userID)
    prompt := s.buildWeavePrompt(sourceMemories, req.Mode, req.Title)

    // 3. 调用 LLM（复用 callOpenAIForReason 或新建 callLLMForWeave）
    content, err := s.callLLMForWeave(ctx, llmConfig, prompt)
    if err != nil {
        return nil, fmt.Errorf("weave generation failed: %w", err)
    }

    // 4. 创建 weave memory
    metadata := map[string]interface{}{
        "weave_source_ids": req.SourceIDs,
        "weave_mode":       req.Mode,
    }
    metaJSON, _ := json.Marshal(metadata)

    memory := &domain.Memory{
        ID:          uuid.New(),
        UserID:      userID,
        ContentType: "weave",
        TextContent: content,
        Tags:        pq.StringArray{}, // 可继承 source memories 的公共标签
        Metadata:    string(metaJSON),
        ProcessingStatus: "completed",
        Visibility:  "private",
    }

    if err := s.repo.Create(ctx, memory); err != nil {
        return nil, fmt.Errorf("failed to create weave memory: %w", err)
    }

    // 5. 发布 cover:generate 任务（weave memory 也需要封面）
    s.publishTasks(ctx, memory, llmConfig)

    return memory, nil
}
```

Prompt 模板设计（Claude's Discretion）：

```go
var weavePrompts = map[domain.WeaveMode]string{
    domain.WeaveModeArticle: `将以下记忆编织成一篇连贯的文章。保持原文的核心观点和关键信息，但重新组织结构使其流畅易读。
使用 [^1], [^2] 等格式标注来源。在文章末尾列出所有来源。

记忆内容：
%s

请输出完整的文章。`,
    domain.WeaveModeStory: `将以下记忆编织成一个连贯的故事。用叙事的方式串联这些记忆，保持情感温度。
使用 [^1], [^2] 等格式标注来源。在故事末尾列出所有来源。

记忆内容：
%s

请输出完整的故事。`,
    domain.WeaveModeSummary: `将以下记忆总结成一份精炼的摘要。提取每条记忆的核心要点，按主题归类。
使用 [^1], [^2] 等格式标注来源。在末尾列出所有来源。

记忆内容：
%s

请输出完整的摘要。`,
    domain.WeaveModeTodo: `基于以下记忆，生成一份可操作的任务清单。将每条记忆转化为具体的行动项。
使用 [^1], [^2] 等格式标注来源。在末尾列出所有来源。

记忆内容：
%s

请输出任务清单，格式为 Markdown 待办列表。`,
}
```

`buildWeavePrompt` 方法：
- 遍历 sourceMemories，为每条记忆生成编号内容块
- 文本类型：取 text_content 前 500 字
- 链接类型：取 link_title + link_summary
- 文件类型：取 text_content 前 500 字或 file_name
- 编织类型（weave）：取 text_content 前 500 字
- 格式：`[^N] 内容...`

`callLLMForWeave` 方法：
- 复用现有的 LLM 调用模式（callOpenAIForReason / callAnthropicForReason）
- 但使用更大的 max_tokens（2000）和更高的 temperature（0.7）
- 返回完整内容（包含来源标注和底部来源列表）

注意：LLM 输出可能已包含底部来源列表；如果不完整，WeaveMemories 方法应自动附加：
```

---

**来源：**
[^1] 记忆标题/内容摘要...
[^2] ...
```
  </action>
  <verify>
    <automated>cd services/memory-service && go test ./internal/service/... -run TestWeave -x -v</automated>
  </verify>
  <done>
    - WeaveMemories 方法编译通过
    - 单元测试通过（mock LLM 响应）
    - 生成的 weave memory 包含正确的 content_type="weave"
    - metadata 包含 weave_source_ids 和 weave_mode
    - 内容包含 [^N] 来源标注
  </done>
</task>

<task type="auto">
  <name>Task 3: 添加 Weave HTTP Handler 端点</name>
  <files>services/memory-service/internal/transport/memory_handler.go</files>
  <action>
per D-04-06:

1. 在 `RegisterRoutes` 中添加 weave 路由（放在 `/memories` 路由组内）：
```go
router.POST("/memories/weave", h.Weave)
```

2. 实现 `Weave` handler：
```go
func (h *MemoryHandler) Weave(c *gin.Context) {
    userID, ok := getUserID(c)
    if !ok {
        respondWithError(c, http.StatusUnauthorized, "UNAUTHORIZED", "User not authenticated")
        return
    }

    var req domain.WeaveRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        respondWithValidationError(c, err)
        return
    }

    memory, err := h.memoryService.WeaveMemories(c.Request.Context(), userID, req)
    if err != nil {
        zap.L().Error("weave failed", zap.Error(err), zap.String("user_id", userID.String()))
        respondWithError(c, http.StatusInternalServerError, "WEAVE_ERROR", err.Error())
        return
    }

    c.JSON(http.StatusCreated, gin.H{
        "success": true,
        "data": gin.H{
            "memory": memory.SafeResponse(),
        },
    })
}
```

3. 更新 `RetryTask` 的 validTypes map，添加 `cover:generate`：
```go
validTypes := map[string]bool{
    "link:fetch": true, "text:vectorize": true, "tag:generate": true,
    "suggestion:generate": true, "file:extract": true, "cover:generate": true,
}
```
  </action>
  <verify>
    <automated>cd services/memory-service && go build ./...</automated>
  </verify>
  <done>
    - POST /api/v1/memories/weave 端点注册
    - Weave handler 验证请求、调用服务、返回 201
    - RetryTask 包含 cover:generate
    - go build 无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Gateway -> Memory Service | JWT auth; X-User-ID header |
| Memory Service -> LLM API | API key auth; prompt contains only user's own memories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | Weave prompt injection | mitigate | Only memory content (title, summary, text) enters prompt; no user-controlled prompt template |
| T-14-08 | Information Disclosure | Source memory access | mitigate | WeaveMemories calls Get() which verifies userID ownership |
| T-14-09 | Denial of Service | Large source_ids array | mitigate | WeaveRequest.SourceIDs binding: max=20; each content truncated to 500 chars |
| T-14-10 | Tampering | content_type="weave" bypass | mitigate | Gateway JWT ensures userID; binding validates oneof constraint |
</threat_model>

<verification>
- POST /api/v1/memories/weave 返回 201 和 weave memory
- weave memory 的 content_type="weave"
- metadata 包含 weave_source_ids
- 内容包含 [^1] 来源标注
- cover:generate 任务在 weave memory 创建后自动发布
</verification>

<success_criteria>
- [ ] CreateMemoryRequest binding 包含 "weave"
- [ ] TaskStatusUpdate binding 包含 "cover:generate"
- [ ] WeaveRequest 类型定义（source_ids, mode, title）
- [ ] POST /api/v1/memories/weave 端点可用
- [ ] WeaveMemories 验证 source memories 所有权
- [ ] 四种 mode 使用不同 prompt 模板
- [ ] LLM 输出包含 [^N] 来源标注
- [ ] weave memory metadata 包含 weave_source_ids
- [ ] RetryTask 支持 cover:generate
- [ ] go build ./... 无错误
</success_criteria>

<output>
After completion, create `.planning/phases/14-memory-covers-weaving/14-02-SUMMARY.md`
</output>
