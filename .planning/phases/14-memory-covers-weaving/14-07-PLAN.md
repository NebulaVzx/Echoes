---
phase: 14-memory-covers-weaving
plan: 07
type: execute
wave: 4
depends_on: ["14-01", "14-02", "14-03", "14-04", "14-05", "14-06"]
files_modified:
  - services/memory-service/internal/service/memory_service.go
  - services/memory-service/internal/transport/memory_handler.go
  - services/memory-service/internal/domain/memory.go
  - docker-compose.yml
  - web/next.config.js
autonomous: true
requirements:
  - COV-01
  - COV-02
  - WEA-02
  - WEA-03
must_haves:
  truths:
    - 保存记忆时自动发布 cover:generate 任务
    - RetryTask 支持 cover:generate 重试
    - 所有服务编译通过
    - 前端构建通过
    - 端到端流程验证通过
  artifacts:
    - path: services/memory-service/internal/service/memory_service.go
      provides: publishTasks includes cover generation
      contains: "PublishCoverGenerate"
    - path: services/memory-service/internal/transport/memory_handler.go
      provides: RetryTask supports cover:generate
      contains: "cover:generate"
    - path: docker-compose.yml
      provides: Processor service env vars for cover consumer
  key_links:
    - from: memory_service.go
      to: redis_queue.go
      via: PublishCoverGenerate in publishTasks
      pattern: "PublishCoverGenerate"
    - from: memory_handler.go
      to: memory_service.go
      via: RetryTask handler
      pattern: "RetryTask"
---

<objective>
完成端到端集成。确保保存记忆时自动触发封面生成，重试任务支持 cover:generate，所有服务编译和构建验证通过。

Purpose: 确保封面生成和编织功能完整集成到现有流程中。
Output: 端到端流程可用，构建验证通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Key Interfaces (from codebase)

### publishTasks (services/memory-service/internal/service/memory_service.go)
```go
func (s *MemoryService) publishTasks(ctx context.Context, memory *domain.Memory, llmConfig map[string]interface{}) {
    // For link memories, publish link fetch task
    if memory.ContentType == "link" && memory.LinkURL != "" {
        _ = s.queue.PublishLinkFetch(ctx, memory.ID, memory.LinkURL, memory.Note, llmConfig)
    }
    // For file memories, publish file extraction task
    if memory.ContentType == "file" && memory.MediaURL != "" {
        _ = s.queue.PublishFileExtract(ctx, memory.ID, memory.FileName, memory.MediaURL, llmConfig)
        return
    }
    // For text/link memories, publish text vectorization directly
    content := s.extractContent(memory)
    if content != "" {
        _ = s.queue.PublishTextVectorize(ctx, memory.ID, content, llmConfig)
        _ = s.queue.PublishTagGenerate(ctx, memory.ID, content, memory.Note, llmConfig)
    }
}
```

### RetryTask (services/memory-service/internal/transport/memory_handler.go)
```go
validTypes := map[string]bool{
    "link:fetch": true, "text:vectorize": true, "tag:generate": true,
    "suggestion:generate": true, "file:extract": true,
}
```

### docker-compose.yml
Processor service environment variables need `ENABLE_COVER_CONSUMER=true`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完成 publishTasks 封面生成集成</name>
  <files>services/memory-service/internal/service/memory_service.go</files>
  <action>
per D-02-01:

在 `publishTasks` 方法中添加封面生成任务发布：

```go
func (s *MemoryService) publishTasks(ctx context.Context, memory *domain.Memory, llmConfig map[string]interface{}) {
    // Existing tasks...
    
    // Publish cover generation task for all content types
    coverContent := s.extractContent(memory)
    if coverContent != "" || memory.LinkURL != "" {
        _ = s.queue.PublishCoverGenerate(
            ctx,
            memory.ID,
            memory.ContentType,
            coverContent,
            memory.LinkURL,
            memory.LinkTitle,
            []string(memory.Tags),
            llmConfig,
        )
    }
}
```

注意：封面生成应该在所有记忆类型（text/link/file/weave）创建后发布。对于 file 类型，如果 text_content 尚未提取（初始为空），封面生成消费者需要处理空内容的情况（使用 file_name 作为 fallback）。

同时确保 `extractContent` 方法支持 "weave" 类型：
```go
func (s *MemoryService) extractContent(memory *domain.Memory) string {
    if memory.ContentType == "text" || memory.ContentType == "weave" {
        return memory.TextContent
    }
    // ... existing logic
}
```
  </action>
  <verify>
    <automated>cd services/memory-service && go build ./...</automated>
  </verify>
  <done>
    - publishTasks 调用 PublishCoverGenerate
    - extractContent 支持 "weave" 类型
    - go build 无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 docker-compose 和验证端到端构建</name>
  <files>
    docker-compose.yml
    web/next.config.js
  </files>
  <action>
per D-01-02:

1. 在 `docker-compose.yml` 的 processor-service 环境变量中添加：
```yaml
processor-service:
  environment:
    - ENABLE_COVER_CONSUMER=true
    - USE_POLLINATIONS_FALLBACK=true  # 开发环境默认启用
```

2. 确认前端构建配置支持新路由 `/weave` 和 `/weave/[id]`：
   - Next.js App Router 自动支持，无需额外配置
   - 确认 `next.config.js` 没有限制新路由

3. 执行完整构建验证：
   - 前端: `cd web && npm run build`
   - Go 后端: `cd services/memory-service && go build ./...`
   - Go Gateway: `cd services/gateway && go build ./...`
   - Python processor: `cd services/processor-service && python -c "from app.main import app; print('OK')"`

4. 检查是否有遗漏的文件修改：
   - 确认所有新文件已创建
   - 确认所有修改文件已保存
   - 确认 import 路径正确

注意：如果构建失败，修复问题后重新验证。
  </action>
  <verify>
    <automated>
      cd web && npm run build && \
      cd ../services/memory-service && go build ./... && \
      cd ../gateway && go build ./... && \
      cd ../processor-service && python -c "from app.main import app; print('OK')"
    </automated>
  </verify>
  <done>
    - docker-compose.yml 包含 ENABLE_COVER_CONSUMER
    - 前端 npm run build 通过
    - memory-service go build 通过
    - gateway go build 通过
    - processor-service Python 导入通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 端到端流程验证</name>
  <files>[]</files>
  <action>
执行端到端验证流程：

1. **封面生成流程验证：**
   - 启动所有服务: `make dev-start`
   - 创建一条 text 记忆
   - 检查 Redis: `redis-cli XREAD COUNT 1 STREAMS cover:generate 0`
   - 检查 processor 日志: 应显示 "Cover consumer started" 和封面生成日志
   - 检查 MinIO: `mc ls echoes-files/covers/`
   - 检查数据库: `SELECT cover_url FROM memories WHERE id = ...`

2. **编织流程验证：**
   - 在时间轴选择 2+ 条记忆
   - 点击"编织文章"
   - 选择模式（article）
   - 确认跳转到 /weave/{id}
   - 检查内容包含 [^1], [^2] 来源标注
   - 测试编辑和保存
   - 测试 Markdown 导出

3. **降级验证：**
   - 停止 DALL-E API key（或设置无效 key）
   - 创建新记忆
   - 确认 Pollinations.AI 降级工作
   - 再断开网络，确认 fallback cover 生成

4. **边界情况验证：**
   - 空标签记忆的 fallback 颜色
   - 单条记忆选择时编织按钮禁用
   - 移动端长按进入选择模式
   - 封面显示/隐藏切换

注意：如果某些验证需要外部服务（DALL-E API），使用 Pollinations 降级或 mock。
  </action>
  <verify>
    <automated>
      # Check all new files exist
      ls services/processor-service/app/consumers/cover_consumer.py && \
      ls services/processor-service/app/services/image_processor.py && \
      ls web/components/weave/weave-modal.tsx && \
      ls web/components/weave/weave-editor.tsx && \
      ls web/hooks/use-long-press.ts && \
      ls web/lib/tag-color.ts && \
      ls web/app/(main)/weave/page.tsx && \
      ls web/app/(main)/weave/[id]/page.tsx && \
      ls web/components/memory/selection-bar.tsx && \
      echo "All files present"
    </automated>
  </verify>
  <done>
    - 所有新文件存在
    - 封面生成端到端流程验证通过
    - 编织流程验证通过
    - 降级和边界情况验证通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| End-to-end system | Full flow from user action to persisted result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-21 | Denial of Service | Cover generation loop | mitigate | Consumer acks message even on failure; no infinite retry |
| T-14-22 | Information Disclosure | Build logs leak secrets | mitigate | No API keys in build output; env vars from docker-compose |
</threat_model>

<verification>
- 创建 text 记忆后 Redis 出现 cover:generate 消息
- CoverConsumer 处理消息并生成封面
- MinIO 中出现 covers/ 前缀对象
- 时间轴显示封面缩略图
- 选择 2+ 记忆后编织成功
- /weave/{id} 显示编织结果
- Markdown 导出文件内容正确
</verification>

<success_criteria>
- [ ] publishTasks 调用 PublishCoverGenerate
- [ ] docker-compose.yml 包含 ENABLE_COVER_CONSUMER
- [ ] 前端 npm run build 通过
- [ ] 所有 Go 服务编译通过
- [ ] Processor service Python 导入通过
- [ ] 所有新文件存在
- [ ] 封面生成端到端验证通过
- [ ] 编织流程端到端验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/14-memory-covers-weaving/14-07-SUMMARY.md`
</output>
