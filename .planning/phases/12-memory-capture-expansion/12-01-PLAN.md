---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: [10-warmth-of-memory]
files_modified:
  - shared/migrations/004_file_upload.sql
  - services/memory-service/internal/domain/memory.go
  - services/memory-service/internal/transport/memory_handler.go
  - services/memory-service/internal/service/memory_service.go
  - services/memory-service/internal/repository/memory_repository.go
  - services/memory-service/internal/service/redis_queue.go
  - services/processor-service/app/consumers/file_consumer.py
  - services/processor-service/app/services/file_extractor.py
  - services/processor-service/requirements.txt
  - web/components/memory/create-memory-form.tsx
  - web/components/memory/memory-card.tsx
  - web/lib/api.ts
autonomous: true
requirements:
  - FEAT-31
  - FEAT-32
  - FEAT-33
  - FEAT-34
  - FEAT-35
must_haves:
  truths:
    - "content_type supports 'text' | 'link' | 'file'"
    - "File upload uses multipart/form-data to MinIO via Memory Service"
    - "txt/md content is extracted directly; docx uses python-docx"
    - "Extracted text flows through existing text:vectorize queue"
    - "UI shows '记忆匣' branded header with quick templates"
    - "Starred memories are queryable via ?starred=true"
  artifacts:
    - path: "shared/migrations/004_file_upload.sql"
      provides: "Database schema for file support, source, is_starred, cover_url"
      contains: "ALTER TABLE memories ADD COLUMN source TEXT"
    - path: "services/memory-service/internal/domain/memory.go"
      provides: "Updated Memory struct with Source, IsStarred, CoverURL"
      exports: ["Source", "IsStarred", "CoverURL"]
    - path: "services/memory-service/internal/transport/memory_handler.go"
      provides: "POST /memories with multipart support for file uploads"
      contains: "c.Request.MultipartForm"
    - path: "services/processor-service/app/consumers/file_consumer.py"
      provides: "Redis Stream consumer for file:extract tasks"
      contains: "def process_file_extract_task"
    - path: "web/components/memory/create-memory-form.tsx"
      provides: "File upload UI with drag-and-drop, template selector, star toggle"
      contains: "记忆匣"
---

<objective>
Transform the memory capture experience from a bare text/link form into a rich "Memory Box" (记忆匣).
Support uploading .txt/.md/.docx files (extracted text goes through existing vectorization pipeline),
add quick templates (code snippet, reading notes, idea capture), source attribution, star/favorite,
and brand the capture area as "记忆匣".

Purpose: Users asked for richer capture capabilities — files, templates, branding. These are
all text-centric features aligned with Echoes' core semantic search mission.
Output: Working file upload, extraction, UI templates, star/source fields, branded capture form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-memory-capture-expansion/11-CONTEXT.md
</context>

## Task 1: Database migration for file support and new fields

<task>
  <id>12-01-01</id>
  <title>Add file support columns to memories table</title>
  <read_first>
    - shared/migrations/001_init.sql — Existing schema
    - shared/migrations/003_time_capsule.sql — Previous migration style
    - services/memory-service/internal/domain/memory.go — Current Memory struct
  </read_first>
  <action>
    Create `shared/migrations/004_file_upload.sql`:

    ```sql
    -- Migration 004: File upload support + capture enhancements
    ALTER TABLE memories ADD COLUMN IF NOT EXISTS source TEXT;
    ALTER TABLE memories ADD COLUMN IF NOT EXISTS is_starred BOOLEAN DEFAULT false;
    ALTER TABLE memories ADD COLUMN IF NOT EXISTS cover_url TEXT;
    ALTER TABLE memories ADD COLUMN IF NOT EXISTS file_name TEXT;
    ALTER TABLE memories ADD COLUMN IF NOT EXISTS file_size BIGINT;

    -- Index for starred queries
    CREATE INDEX IF NOT EXISTS idx_memories_starred ON memories(user_id, is_starred, created_at DESC);

    -- Note: content_type CHECK constraint updated in domain validation, not DB-level
    -- to avoid migration complexity with existing data
    ```
  </action>
  <acceptance_criteria>
    - `shared/migrations/004_file_upload.sql` exists with all 5 ALTER statements
    - File contains `CREATE INDEX IF NOT EXISTS idx_memories_starred`
    - Migration runs successfully without data loss
  </acceptance_criteria>
</task>

## Task 2: Update Memory domain model

<task>
  <id>12-01-02</id>
  <title>Add Source, IsStarred, CoverURL, FileName, FileSize to domain</title>
  <read_first>
    - services/memory-service/internal/domain/memory.go — Current struct
  </read_first>
  <action>
    Update `Memory` struct:
    1. Add fields:
       ```go
       Source     string     `gorm:"type:text" json:"source,omitempty"`
       IsStarred  bool       `gorm:"type:boolean;default:false" json:"is_starred"`
       CoverURL   string     `gorm:"type:text" json:"cover_url,omitempty"`
       FileName   string     `gorm:"type:text" json:"file_name,omitempty"`
       FileSize   int64      `gorm:"type:bigint" json:"file_size,omitempty"`
       ```
    2. Update `SafeResponse()` to include new fields
    3. Update `CreateMemoryRequest`:
       - Change `ContentType` binding from `oneof=text link` to `oneof=text link file`
       - Add `Source string` field
       - Add `IsStarred bool` field
    4. Update `UpdateMemoryRequest`:
       - Add `Tags`, `Note`, `Source`, `IsStarred` (keep backward compatible)
  </action>
  <acceptance_criteria>
    - Memory struct has Source, IsStarred, CoverURL, FileName, FileSize fields
    - SafeResponse() includes all new fields
    - CreateMemoryRequest validates content_type as "oneof=text link file"
    - Code compiles without error
  </acceptance_criteria>
</task>

## Task 3: Implement MinIO file upload in Memory Service

<task>
  <id>12-01-03</id>
  <title>Add multipart file upload handler and MinIO storage</title>
  <read_first>
    - services/memory-service/internal/transport/memory_handler.go — Current Create handler
    - services/memory-service/internal/service/memory_service.go — Create method
    - docker-compose.yml — MinIO configuration
  </read_first>
  <action>
    1. Add MinIO client initialization in Memory Service (reuse existing MinIO config)
    2. Modify `Create` handler in `memory_handler.go`:
       - Support `multipart/form-data` in addition to JSON
       - Extract file from form if `content_type=file`
       - Validate file type: .txt, .md, .docx
       - Validate file size <= 10MB
       - Upload to MinIO: `files/{user_id}/{memory_id}/{filename}`
       - Set `media_url` and `file_name` / `file_size`
    3. Set `text_content` to empty for file uploads (extracted asynchronously)
    4. Publish `file:extract` task to Redis Stream after creation
    5. Update `List` handler: support `?starred=true` query param
  </action>
  <acceptance_criteria>
    - POST /memories accepts multipart with file field
    - File uploaded to MinIO with correct path
    - Invalid file type returns 400 BAD_REQUEST
    - File > 10MB returns 400 BAD_REQUEST
    - `?starred=true` filters only starred memories
  </acceptance_criteria>
</task>

## Task 4: Create file extraction consumer in Processor Service

<task>
  <id>12-01-04</id>
  <title>Implement txt/md/docx text extraction consumer</title>
  <read_first>
    - services/processor-service/app/consumers/link_consumer.py — Existing consumer pattern
    - services/processor-service/app/consumers/tag_consumer.py — Existing consumer pattern
    - services/processor-service/requirements.txt — Current dependencies
  </read_first>
  <action>
    1. Add `python-docx` to `requirements.txt`
    2. Create `services/processor-service/app/services/file_extractor.py`:
       ```python
       def extract_text(file_path: str, file_type: str) -> str:
           # .txt: read utf-8
           # .md: read utf-8 (preserve markdown)
           # .docx: use python-docx to extract paragraphs
       ```
    3. Create `services/processor-service/app/consumers/file_consumer.py`:
       - Subscribe to `file:extract` queue
       - Download file from MinIO
       - Extract text using file_extractor
       - Call Memory Service internal API to update `text_content`
       - Publish `text:vectorize` task (reuse existing flow)
       - Publish `tag:generate` task (reuse existing flow)
    4. Register consumer in Processor Service main.py
  </action>
  <acceptance_criteria>
    - file_consumer.py subscribes to `file:extract` queue
    - txt/md content extracted correctly
    - docx content extracted via python-docx
    - After extraction, text:vectorize and tag:generate tasks are published
    - Failed extraction updates metadata with error status
  </acceptance_criteria>
</task>

## Task 5: Redesign capture form UI — "记忆匣"

<task>
  <id>12-01-05</id>
  <title>Redesign create form with file upload, templates, star, source</title>
  <read_first>
    - web/components/memory/create-memory-form.tsx — Current form
    - web/components/ui/ — shadcn/ui components available
  </read_first>
  <action>
    Update `create-memory-form.tsx`:
    1. Add branded header: `<h2 className="...">✦ 记忆匣 ✦</h2>`
    2. Content type toggle: add "文件" tab
       - File mode: drag-and-drop zone (dashed border)
       - Show selected file name + size
       - Show upload progress (if needed)
    3. Quick template selector (dropdown or chips):
       - "空白" (default)
       - "代码片段" → textarea placeholder: "```language\n// 粘贴代码\n```"
       - "读书笔记" → placeholder: "书名：《》\n章节：\n要点："
       - "灵感速记" → placeholder: "突然想到..."
       - "待办事项" → placeholder: "- [ ] 任务1\n- [ ] 任务2"
    4. Source input field (optional):
       - Placeholder: "来源（书名、文章、会议等）"
    5. Star toggle button (⭐) next to submit button
    6. Keep existing: tags, note, AI suggestion, time capsule
    7. Submit button: "存入记忆匣"
  </action>
  <acceptance_criteria>
    - UI shows "记忆匣" header
    - Three content type tabs: 文字 / 链接 / 文件
    - File drag-and-drop zone works
    - Template selector changes textarea placeholder
    - Source field visible and optional
    - Star toggle functional
    - Submit says "存入记忆匣"
  </acceptance_criteria>
</task>

## Task 6: Update API client and memory card display

<task>
  <id>12-01-06</id>
  <title>Update frontend API and card components for new fields</title>
  <read_first>
    - web/lib/api.ts — API client
    - web/components/memory/memory-card.tsx — Card display
  </read_first>
  <action>
    1. Update `web/lib/api.ts`:
       - `createMemory()` supports `FormData` (not just JSON)
       - Add `source?: string`, `is_starred?: boolean` to request type
       - Add `starred?: boolean` to list query params
    2. Update `memory-card.tsx`:
       - Show star icon (⭐) if `is_starred` is true
       - Show source attribution if present (small text below title)
       - Show file icon if content_type is 'file'
       - Show cover image if `cover_url` is present
    3. Update time轴 page to support starred filter toggle
  </action>
  <acceptance_criteria>
    - api.createMemory accepts FormData for file uploads
    - Memory cards show star icon for starred memories
    - Memory cards show source attribution
    - File memories show file icon
  </acceptance_criteria>
</task>

## Task 7: End-to-end verification

<task>
  <id>12-01-07</id>
  <title>Verify complete file upload flow end-to-end</title>
  <read_first>
    - All files modified in this plan
  </read_first>
  <action>
    Run verification sequence:
    1. Upload test.txt → verify text extracted → verify vectorized → verify searchable
    2. Upload test.md → verify markdown preserved → verify vectorized
    3. Upload test.docx → verify python-docx extracts text → verify vectorized
    4. Create text memory with template → verify template applied
    5. Create starred memory → verify ?starred=true returns it
    6. Create memory with source → verify card shows source
    7. Verify "记忆匣" branding visible in UI
  </action>
  <acceptance_criteria>
    - All 7 verification steps pass
    - File memories appear in timeline with correct icon
    - Starred memories filterable
    - Source shown on cards
    - No regression in existing text/link creation
  </acceptance_criteria>
</task>
