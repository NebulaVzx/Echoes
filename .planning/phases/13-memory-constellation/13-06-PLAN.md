---
phase: 13-memory-constellation
plan: 06
type: execute
wave: 4
depends_on: [13-05]
files_modified: []
autonomous: false
requirements:
  - CONST-01
  - CONST-02
  - CONST-03
  - CONST-04
  - EXPLORE-01
  - EXPLORE-02
  - EXPLORE-03
  - EXPLORE-04
must_haves:
  truths:
    - "End-to-end flow verified: create memory -> see it in constellation -> click to explore -> see related memories"
    - "All backend services compile and start successfully"
    - "Frontend builds without errors"
    - "Database migration applied successfully"
  artifacts:
    - path: "13-06-VERIFICATION.md"
      provides: "Recorded verification results"
      contains: ["pass", "fail", "notes"]
  key_links:
    - from: "Verification checklist"
      to: "All Phase 13 plans"
      via: "Manual testing of integrated features"
---

<objective>
Perform end-to-end verification of the complete Phase 13 constellation graph and explore mode feature.

Purpose: After all implementation plans complete, verify the integrated system works correctly from user perspective. This is a human-verified checkpoint, not an automated test run.
Output: Completed verification checklist with pass/fail status for each item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-memory-constellation/13-CONTEXT.md
@.planning/phases/13-memory-constellation/13-VALIDATION.md

## Prerequisites

All prior plans must be complete:
- 13-01: Database migration, domain types, repositories
- 13-02: Backend API (GET /constellation, GET /explore/:id)
- 13-03: Frontend graph component (ConstellationGraph, GraphControls)
- 13-04: Explore UI (ExplorePanel, BreadcrumbTrail, mobile page)
- 13-05: Integration hooks and page wiring

## Test Data Requirements

- At least 3 memories with different tags (for color clustering)
- At least 1 starred memory (for gold glow verification)
- Memories must have processing_status = 'completed' and vector IS NOT NULL
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: End-to-end verification checklist</name>
  <what-built>Complete Phase 13 constellation graph and explore mode: backend API, database migration, frontend graph visualization, and exploration UI</what-built>
  <how-to-verify>
1. Start all services: `make dev-start` or `docker-compose up -d`
2. Apply migration: `make migrate` (or run `shared/migrations/005_memory_relations.sql`)
3. Create at least 3 memories with different tags via web UI at http://localhost:3000/capture
4. Wait for vectorization to complete (check processing_status = 'completed')
5. Navigate to http://localhost:3000/constellation
6. Verify: Graph renders with nodes colored by tag, starred memories have gold glow
7. Verify: Hover highlights connected nodes and edges
8. Verify: Click a node (desktop) opens RightPanel with related memories
9. Verify: Click a related memory in RightPanel drills down to new center
10. Verify: Mobile view (< 768px) click navigates to /explore?id={memoryId}
11. Verify: "探索更远" button visible when has_more = true; clicking loads more nodes
12. Verify: Keyboard shortcuts: +/- zoom, 0 reset, f fit, Esc deselect
13. Test explore API directly: `curl -H "Authorization: Bearer $TOKEN" http://localhost:8088/api/v1/memories/{id}/explore`
14. Verify: Response includes results with similarity and reason fields (flat structure)
15. Verify: Backend compiles: `cd services/memory-service && go build ./...`
16. Verify: Frontend builds: `cd web && npm run build`
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | JWT token auth; all endpoints verify user_id |
| API -> DB | user_id filter on all queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-15 | Information Disclosure | E2E test data | accept | Test data is synthetic; no real user PII |
</threat_model>

<verification>
- [ ] All 16 verification steps completed
- [ ] Issues documented with reproduction steps
- [ ] Critical bugs fixed before phase sign-off
- [ ] 13-06-VERIFICATION.md created with results
</verification>

<success_criteria>
- End-to-end flow verified: create memory -> see in constellation -> explore related
- All backend services compile and start
- Frontend builds without errors
- Database migration applied
- No critical bugs blocking phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/13-memory-constellation/13-06-VERIFICATION.md` with verification results
</output>
