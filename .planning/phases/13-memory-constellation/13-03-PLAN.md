---
phase: 13-memory-constellation
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - web/package.json
  - web/lib/api.ts
  - web/types/constellation.ts
  - web/components/constellation/ConstellationGraph.tsx
  - web/components/constellation/GraphControls.tsx
  - web/components/constellation/GraphSkeleton.tsx
  - web/app/(main)/constellation/page.tsx
  - web/app/(main)/constellation/page-client.tsx
autonomous: true
requirements:
  - CONST-01
  - CONST-02
  - CONST-03
  - CONST-04
  - D-03
  - D-05
must_haves:
  truths:
    - "react-force-graph-2d is installed and dynamically imported with ssr:false"
    - "ConstellationGraph renders nodes colored by tag hash and content type borders"
    - "Starred memories have gold glow and larger size (D-05)"
    - "Graph supports hover, click, double-click, zoom, pan interactions"
    - "Graph controls (zoom in/out/reset) are visible and functional"
  artifacts:
    - path: "web/components/constellation/ConstellationGraph.tsx"
      provides: "Main force-directed graph component"
      contains: ["ForceGraph2D", "nodeCanvasObject", "nodeCanvasObjectMode"]
    - path: "web/components/constellation/GraphControls.tsx"
      provides: "Floating control bar"
      contains: ["onZoomIn", "onZoomOut", "onReset"]
    - path: "web/types/constellation.ts"
      provides: "GraphNode, GraphEdge, ExploreState types"
      exports: ["GraphNode", "GraphEdge"]
  key_links:
    - from: "ConstellationGraph"
      to: "react-force-graph-2d"
      via: "dynamic import ssr:false"
      pattern: "dynamic\(\(\) => import\('react-force-graph-2d'\)"
    - from: "ConstellationGraph"
      to: "GraphControls"
      via: "ref callback for zoom control"
      pattern: "fgRef"
---

<objective>
Build the core frontend constellation graph component with react-force-graph-2d, including custom node rendering, interactions, and controls.

Purpose: This is the visual centerpiece of Phase 13. Users see their memories as nodes in a force-directed graph, with colors by tag cluster and content type shapes. The graph must be interactive and performant.
Output: ConstellationGraph component, GraphControls, types, and updated constellation page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-memory-constellation/13-CONTEXT.md
@.planning/phases/13-memory-constellation/13-UI-SPEC.md
@.planning/phases/13-memory-constellation/13-RESEARCH.md

## Existing Code Patterns

From `web/lib/api.ts`:
- API client pattern: `api.request<T>(method, path)` with automatic token refresh
- Types use camelCase in TS (backend uses snake_case in JSON)
- Memory interface has `id`, `content_type`, `text_content`, `link_title`, `tags`, `is_starred`

From `13-UI-SPEC.md`:
- 16-color palette for tag clusters (hash: `tag.charCodeAt(0) % 16`)
- Content type shapes: text=circle, link=diamond, file=square
- Starred accent: gold glow `rgba(250, 204, 21, 0.3)`, +3px size
- Node label: max 20 chars, only when `globalScale > 1.2`
- Edge colors by similarity range (0.90-1.00, 0.80-0.89, 0.75-0.79)
- Selection state: 2px solid primary border, connected nodes opacity 1.0, others 0.3

From `13-RESEARCH.md`:
- MUST use `dynamic(() => import('react-force-graph-2d'), { ssr: false })`
- MUST add `'use client'` directive
- Performance props: `warmupTicks={50}`, `cooldownTicks={30}`, `cooldownTime={8000}`, `d3VelocityDecay={0.4}`
- Custom rendering via `nodeCanvasObject` + `nodeCanvasObjectMode={() => 'replace'}`

## Locked Decisions

- **D-03**: P0 uses react-force-graph-2d
- **D-05**: Tag hash coloring + content type borders + starred glow
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create types</name>
  <files>web/package.json, web/types/constellation.ts, web/lib/api.ts</files>
  <action>
1. Install dependencies in web/:
```bash
cd web && npm install react-force-graph-2d graphology
```

2. Create `web/types/constellation.ts`:

```typescript
// Graph node representing a memory in the constellation
export interface GraphNode {
  id: string
  label: string
  group: string
  val: number
  isStarred: boolean
  contentType: 'text' | 'link' | 'file'
  color: string
  x?: number
  y?: number
}

// Graph edge representing similarity connection
export interface GraphEdge {
  source: string
  target: string
  value: number
}

// Graph data structure for react-force-graph-2d
export interface GraphData {
  nodes: GraphNode[]
  links: GraphEdge[]
}

// Explore state for drill-down tracking
export interface ExploreState {
  memoryId: string
  label: string
}

// Constellation API response
export interface ConstellationResponse {
  nodes: Array<{
    id: string
    content_type: string
    text_content?: string
    link_title?: string
    tags: string[]
    is_starred: boolean
    created_at: string
  }>
  edges: Array<{
    source: string
    target: string
    similarity: number
  }>
  has_more: boolean
  total: number
}

// Explore API response
export interface ExploreResponse {
  memory_id: string
  results: Array<{
    id: string
    user_id: string
    content_type: 'text' | 'link' | 'file'
    text_content?: string
    link_url?: string
    link_title?: string
    link_summary?: string
    tags: string[]
    note?: string
    source?: string
    is_starred?: boolean
    cover_url?: string
    file_name?: string
    file_size?: number
    processing_status: string
    visibility: string
    sealed_until?: string
    created_at: string
    updated_at: string
    similarity: number
    reason: string
  }>
  breadcrumb: Array<{
    id: string
    label: string
  }>
}
```

3. Add API client methods to `web/lib/api.ts` (append inside ApiClient class, before closing brace):

```typescript
  // Constellation endpoints
  async getConstellation(): Promise<ApiResponse<ConstellationResponse>> {
    return this.request<ConstellationResponse>('GET', '/api/v1/constellation')
  }

  async exploreMemory(id: string): Promise<ApiResponse<ExploreResponse>> {
    return this.request<ExploreResponse>('GET', `/api/v1/memories/${id}/explore`)
  }
```

4. Add import for constellation types at top of `web/lib/api.ts`:
```typescript
import { ConstellationResponse, ExploreResponse } from '@/types/constellation'
```
  </action>
  <verify>
    <automated>cd web && grep -c "react-force-graph-2d" package.json && grep -c "graphology" package.json && grep -c "GraphNode" types/constellation.ts && grep -c "getConstellation" lib/api.ts</automated>
  </verify>
  <done>react-force-graph-2d and graphology installed; constellation types created; API methods added</done>
</task>

<task type="auto">
  <name>Task 2: Create ConstellationGraph component with custom rendering</name>
  <files>web/components/constellation/ConstellationGraph.tsx, web/components/constellation/GraphSkeleton.tsx</files>
  <action>
1. Create `web/components/constellation/GraphSkeleton.tsx`:

```tsx
export function GraphSkeleton() {
  return (
    <div className="flex items-center justify-center h-full w-full">
      <div className="text-center">
        <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-primary mx-auto mb-4" />
        <p className="text-sm text-muted-foreground">正在绘制星图...</p>
      </div>
    </div>
  )
}
```

2. Create `web/components/constellation/ConstellationGraph.tsx`:

```tsx
'use client'

import dynamic from 'next/dynamic'
import { useCallback, useRef, useState, useMemo } from 'react'
import type { GraphData, GraphNode, GraphEdge } from '@/types/constellation'

const ForceGraph2D = dynamic(
  () => import('react-force-graph-2d'),
  {
    ssr: false,
    loading: () => <GraphSkeleton />,
  }
)

// 16-color palette for tag clusters (light mode hex)
const TAG_COLORS = [
  '#0EA5E9', '#10B981', '#F59E0B', '#8B5CF6', '#F43F5E', '#06B6D4',
  '#EAB308', '#22C55E', '#F97316', '#3B82F6', '#14B8A6', '#84CC16',
  '#A855F7', '#64748B', '#EF4444', '#EC4899',
]

// Dark mode variants
const TAG_COLORS_DARK = [
  '#38BDF8', '#34D399', '#FBBF24', '#A78BFA', '#FB7185', '#22D3EE',
  '#FDE047', '#4ADE80', '#FB923C', '#60A5FA', '#2DD4BF', '#A3E635',
  '#C084FC', '#94A3B8', '#F87171', '#F472B6',
]

// Content type border colors (light/dark)
const BORDER_COLORS = {
  text: { light: '#9CA3AF', dark: '#6B7280' },
  link: { light: '#3B82F6', dark: '#60A5FA' },
  file: { light: '#10B981', dark: '#34D399' },
}

function getTagColor(tag: string, isDark: boolean): string {
  if (!tag) return isDark ? '#6B7280' : '#9CA3AF'
  const index = tag.charCodeAt(0) % 16
  return isDark ? TAG_COLORS_DARK[index] : TAG_COLORS[index]
}

function getNodeLabel(node: GraphNode): string {
  const text = node.label || '未命名'
  if (text.length <= 20) return text
  return text.slice(0, 20) + '...'
}

interface ConstellationGraphProps {
  data: GraphData
  onNodeClick?: (node: GraphNode) => void
  onNodeDoubleClick?: (node: GraphNode) => void
  maxNodes?: number
}

export function ConstellationGraph({
  data,
  onNodeClick,
  onNodeDoubleClick,
  maxNodes,
}: ConstellationGraphProps) {
  const fgRef = useRef<any>(null)
  const [highlightNodes, setHighlightNodes] = useState<Set<string>>(new Set())
  const [highlightLinks, setHighlightLinks] = useState<Set<number>>(new Set())
  const [selectedNode, setSelectedNode] = useState<string | null>(null)
  const [isDark, setIsDark] = useState(() => {
    if (typeof document !== 'undefined') {
      return document.documentElement.classList.contains('dark')
    }
    return false
  })

  // Listen for dark mode changes
  useState(() => {
    if (typeof window === 'undefined') return
    const observer = new MutationObserver(() => {
      setIsDark(document.documentElement.classList.contains('dark'))
    })
    observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] })
    return () => observer.disconnect()
  })

  // Limit nodes if specified (mobile degradation)
  const displayData = useMemo(() => {
    if (!maxNodes || data.nodes.length <= maxNodes) return data
    return {
      nodes: data.nodes.slice(0, maxNodes),
      links: data.links.filter(
        link => {
          const s = typeof link.source === 'object' ? link.source.id : link.source
          const t = typeof link.target === 'object' ? link.target.id : link.target
          const nodeIds = new Set(data.nodes.slice(0, maxNodes).map(n => n.id))
          return nodeIds.has(s) && nodeIds.has(t)
        }
      ),
    }
  }, [data, maxNodes])

  // Custom node rendering: tag color + content type shape + starred glow
  const nodeCanvasObject = useCallback((node: any, ctx: CanvasRenderingContext2D, globalScale: number) => {
    const size = (node.val || 5) as number
    const x = node.x || 0
    const y = node.y || 0
    const contentType = node.contentType as string
    const isStarred = node.isStarred as boolean
    const color = node.color as string

    // Starred memory: gold glow
    if (isStarred) {
      ctx.beginPath()
      ctx.arc(x, y, size + 6, 0, 2 * Math.PI)
      ctx.fillStyle = isDark ? 'rgba(250, 204, 21, 0.25)' : 'rgba(250, 204, 21, 0.3)'
      ctx.fill()
    }

    // Main shape based on content type
    ctx.fillStyle = color
    ctx.strokeStyle = BORDER_COLORS[contentType as keyof typeof BORDER_COLORS]?.[isDark ? 'dark' : 'light'] || BORDER_COLORS.text.light
    ctx.lineWidth = 1.5

    if (contentType === 'link') {
      // Diamond: rotate 45deg
      ctx.save()
      ctx.translate(x, y)
      ctx.rotate(Math.PI / 4)
      ctx.fillRect(-size, -size, size * 2, size * 2)
      ctx.strokeRect(-size, -size, size * 2, size * 2)
      ctx.restore()
    } else if (contentType === 'file') {
      // Square
      ctx.fillRect(x - size, y - size, size * 2, size * 2)
      ctx.strokeRect(x - size, y - size, size * 2, size * 2)
    } else {
      // Circle (default for text)
      ctx.beginPath()
      ctx.arc(x, y, size, 0, 2 * Math.PI)
      ctx.fill()
      ctx.stroke()
    }

    // Node label (only when zoomed in enough)
    if (globalScale > 1.2) {
      const label = getNodeLabel(node as GraphNode)
      ctx.font = `${Math.max(8, 12 / globalScale)}px sans-serif`
      ctx.fillStyle = isDark ? '#E5E7EB' : '#374151'
      ctx.textAlign = 'center'
      ctx.fillText(label, x, y + size + 10)
    }
  }, [isDark])

  // Hover: highlight connected nodes and edges
  const handleNodeHover = useCallback((node: any | null) => {
    if (!node) {
      setHighlightNodes(new Set())
      setHighlightLinks(new Set())
      return
    }

    const connectedNodes = new Set<string>([node.id as string])
    const connectedLinks = new Set<number>()

    displayData.links.forEach((link, i) => {
      const source = typeof link.source === 'object' ? (link.source as any).id : link.source
      const target = typeof link.target === 'object' ? (link.target as any).id : link.target
      if (source === node.id || target === node.id) {
        connectedLinks.add(i)
        connectedNodes.add(source as string)
        connectedNodes.add(target as string)
      }
    })

    setHighlightNodes(connectedNodes)
    setHighlightLinks(connectedLinks)
  }, [displayData.links])

  // Click: select node and trigger explore
  const handleNodeClick = useCallback((node: any) => {
    setSelectedNode(node.id as string)
    onNodeClick?.(node as GraphNode)
  }, [onNodeClick])

  // Double-click: zoom to node
  const handleNodeDoubleClick = useCallback((node: any) => {
    if (fgRef.current) {
      fgRef.current.centerAt(node.x, node.y, 300)
      fgRef.current.zoom(2.0, 300)
    }
    onNodeDoubleClick?.(node as GraphNode)
  }, [onNodeDoubleClick])

  // Link styling by similarity
  const linkColor = useCallback((link: any) => {
    const sim = link.value as number
    if (sim >= 0.90) return isDark ? '#D1D5DB' : '#374151'
    if (sim >= 0.80) return isDark ? '#9CA3AF' : '#6B7280'
    return isDark ? '#6B7280' : '#9CA3AF'
  }, [isDark])

  const linkWidth = useCallback((link: any) => {
    const idx = link.index as number
    if (highlightLinks.has(idx)) return 2.5
    const sim = link.value as number
    if (sim >= 0.90) return 2
    if (sim >= 0.80) return 1.5
    return 1
  }, [highlightLinks])

  const linkOpacity = useCallback((link: any) => {
    const idx = link.index as number
    if (highlightLinks.has(idx)) return 1
    if (selectedNode) return 0.1
    return 0.4
  }, [highlightLinks, selectedNode])

  const nodeOpacity = useCallback((node: any) => {
    if (highlightNodes.size === 0 && !selectedNode) return 1
    if (highlightNodes.has(node.id as string)) return 1
    if (selectedNode && node.id === selectedNode) return 1
    return 0.3
  }, [highlightNodes, selectedNode])

  return (
    <div className="relative h-full w-full">
      <ForceGraph2D
        ref={fgRef}
        graphData={displayData}
        nodeCanvasObject={nodeCanvasObject}
        nodeCanvasObjectMode={() => 'replace'}
        nodePointerAreaPaint={(node: any, color: string, ctx: CanvasRenderingContext2D) => {
          const size = (node.val || 5) + 2
          ctx.fillStyle = color
          ctx.beginPath()
          ctx.arc(node.x, node.y, size, 0, 2 * Math.PI)
          ctx.fill()
        }}
        onNodeHover={handleNodeHover}
        onNodeClick={handleNodeClick}
        onNodeDoubleClick={handleNodeDoubleClick}
        linkColor={linkColor}
        linkWidth={linkWidth}
        linkOpacity={linkOpacity}
        nodeOpacity={nodeOpacity}
        warmupTicks={50}
        cooldownTicks={30}
        cooldownTime={8000}
        d3VelocityDecay={0.4}
        enableZoomInteraction={true}
        enablePanInteraction={true}
        enablePointerInteraction={true}
        backgroundColor="transparent"
      />
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep -c "nodeCanvasObject" web/components/constellation/ConstellationGraph.tsx && grep -c "ForceGraph2D" web/components/constellation/ConstellationGraph.tsx && grep -c "ssr: false" web/components/constellation/ConstellationGraph.tsx && grep -c "getTagColor" web/components/constellation/ConstellationGraph.tsx</automated>
  </verify>
  <done>ConstellationGraph component created with custom nodeCanvasObject, tag coloring, content type shapes, starred glow, hover/click/double-click handlers, and performance props</done>
</task>

<task type="auto">
  <name>Task 3: Create GraphControls and update constellation page</name>
  <files>web/components/constellation/GraphControls.tsx, web/app/(main)/constellation/page.tsx, web/app/(main)/constellation/page-client.tsx</files>
  <action>
1. Create `web/components/constellation/GraphControls.tsx`:

```tsx
'use client'

import { ZoomIn, ZoomOut, RotateCcw, Search, Grid3X3 } from 'lucide-react'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'

interface GraphControlsProps {
  onZoomIn: () => void
  onZoomOut: () => void
  onReset: () => void
  onFilter?: (query: string) => void
  onToggleDensity?: () => void
  filterValue?: string
}

export function GraphControls({
  onZoomIn,
  onZoomOut,
  onReset,
  onFilter,
  onToggleDensity,
  filterValue = '',
}: GraphControlsProps) {
  return (
    <div className="absolute top-4 left-4 z-20 flex flex-col gap-2">
      <div className="flex items-center gap-1.5 bg-card/80 backdrop-blur-sm rounded-lg px-2 py-1.5 shadow-sm border">
        <Button
          variant="ghost"
          size="icon"
          className="h-8 w-8"
          onClick={onZoomIn}
          title="放大"
        >
          <ZoomIn className="h-4 w-4" />
        </Button>
        <Button
          variant="ghost"
          size="icon"
          className="h-8 w-8"
          onClick={onZoomOut}
          title="缩小"
        >
          <ZoomOut className="h-4 w-4" />
        </Button>
        <Button
          variant="ghost"
          size="icon"
          className="h-8 w-8"
          onClick={onReset}
          title="重置视图"
        >
          <RotateCcw className="h-4 w-4" />
        </Button>
        {onToggleDensity && (
          <Button
            variant="ghost"
            size="icon"
            className="h-8 w-8"
            onClick={onToggleDensity}
            title="切换密度"
          >
            <Grid3X3 className="h-4 w-4" />
          </Button>
        )}
      </div>
      {onFilter && (
        <div className="bg-card/80 backdrop-blur-sm rounded-lg px-2 py-1.5 shadow-sm border">
          <div className="relative">
            <Search className="absolute left-2 top-1/2 -translate-y-1/2 h-3.5 w-3.5 text-muted-foreground" />
            <Input
              placeholder="搜索记忆..."
              value={filterValue}
              onChange={(e) => onFilter(e.target.value)}
              className="h-8 w-48 pl-7 text-sm"
            />
          </div>
        </div>
      )}
    </div>
  )
}
```

2. Update `web/app/(main)/constellation/page.tsx`:

```tsx
import { type Metadata } from 'next'
import { ConstellationPageClient } from './page-client'

export const metadata: Metadata = {
  title: '记忆星图 - Echoes',
}

export default function ConstellationPage() {
  return <ConstellationPageClient />
}
```

3. Create `web/app/(main)/constellation/page-client.tsx`:

```tsx
'use client'

import { useEffect, useState, useCallback, useRef } from 'react'
import { ConstellationGraph } from '@/components/constellation/ConstellationGraph'
import { GraphControls } from '@/components/constellation/GraphControls'
import { GraphSkeleton } from '@/components/constellation/GraphSkeleton'
import { Button } from '@/components/ui/button'
import { Sparkles } from 'lucide-react'
import { api } from '@/lib/api'
import type { GraphData, GraphNode, ConstellationResponse } from '@/types/constellation'

function transformToGraphData(response: ConstellationResponse): GraphData {
  const nodes: GraphNode[] = response.nodes.map((node) => ({
    id: node.id,
    label: node.link_title || node.text_content?.slice(0, 30) || '未命名记忆',
    group: node.tags?.[0] || 'default',
    val: node.is_starred ? 8 : 5,
    isStarred: node.is_starred,
    contentType: node.content_type as 'text' | 'link' | 'file',
    color: '',
  }))

  const colors = [
    '#0EA5E9', '#10B981', '#F59E0B', '#8B5CF6', '#F43F5E', '#06B6D4',
    '#EAB308', '#22C55E', '#F97316', '#3B82F6', '#14B8A6', '#84CC16',
    '#A855F7', '#64748B', '#EF4444', '#EC4899',
  ]
  nodes.forEach((node) => {
    node.color = colors[node.group.charCodeAt(0) % 16] || '#9CA3AF'
  })

  const links = response.edges.map((edge) => ({
    source: edge.source,
    target: edge.target,
    value: edge.similarity,
  }))

  return { nodes, links }
}

export function ConstellationPageClient() {
  const [graphData, setGraphData] = useState<GraphData>({ nodes: [], links: [] })
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)
  const [hasMore, setHasMore] = useState(false)
  const [filterQuery, setFilterQuery] = useState('')
  const fgRef = useRef<any>(null)

  const fetchConstellation = useCallback(async () => {
    try {
      setLoading(true)
      const response = await api.getConstellation()
      if (response.success && response.data) {
        const data = transformToGraphData(response.data)
        setGraphData(data)
        setHasMore(response.data.has_more)
        setError(null)
      } else {
        setError('星图数据加载失败')
      }
    } catch (err) {
      setError('星图数据加载失败，请检查网络后重试')
    } finally {
      setLoading(false)
    }
  }, [])

  useEffect(() => {
    fetchConstellation()
  }, [fetchConstellation])

  const handleZoomIn = useCallback(() => {
    if (fgRef.current) {
      const currentZoom = fgRef.current.zoom()
      fgRef.current.zoom(currentZoom * 1.3, 300)
    }
  }, [])

  const handleZoomOut = useCallback(() => {
    if (fgRef.current) {
      const currentZoom = fgRef.current.zoom()
      fgRef.current.zoom(currentZoom / 1.3, 300)
    }
  }, [])

  const handleReset = useCallback(() => {
    if (fgRef.current) {
      fgRef.current.zoomToFit(300)
    }
  }, [])

  const handleNodeClick = useCallback((node: GraphNode) => {
    console.log('Node clicked:', node.id)
  }, [])

  const handleFilter = useCallback((query: string) => {
    setFilterQuery(query)
    if (!query) {
      fetchConstellation()
      return
    }
    const lower = query.toLowerCase()
    setGraphData((prev) => ({
      ...prev,
      nodes: prev.nodes.filter(
        (n) =>
          n.label.toLowerCase().includes(lower) ||
          n.group.toLowerCase().includes(lower)
      ),
    }))
  }, [fetchConstellation])

  if (!loading && graphData.nodes.length === 0 && !error) {
    return (
      <div className="flex items-center justify-center h-full">
        <div className="text-center">
          <Sparkles className="h-12 w-12 text-muted-foreground mx-auto mb-4" />
          <h2 className="text-xl font-semibold mb-2">星图还是一片空白</h2>
          <p className="text-muted-foreground mb-6">
            保存记忆后，它们之间的关联会自动在这里显现。试着保存第一条记忆吧。
          </p>
          <Button onClick={() => (window.location.href = '/capture')}>
            保存第一条记忆
          </Button>
        </div>
      </div>
    )
  }

  if (error) {
    return (
      <div className="flex items-center justify-center h-full">
        <div className="text-center">
          <p className="text-destructive mb-4">{error}</p>
          <Button onClick={fetchConstellation}>重试</Button>
        </div>
      </div>
    )
  }

  return (
    <div className="relative h-full w-full">
      {loading && graphData.nodes.length === 0 ? (
        <GraphSkeleton />
      ) : (
        <>
          <ConstellationGraph
            ref={fgRef}
            data={graphData}
            onNodeClick={handleNodeClick}
          />
          <GraphControls
            onZoomIn={handleZoomIn}
            onZoomOut={handleZoomOut}
            onReset={handleReset}
            onFilter={handleFilter}
            filterValue={filterQuery}
          />
          {hasMore && (
            <div className="absolute bottom-6 left-1/2 -translate-x-1/2 z-20">
              <Button
                variant="default"
                className="rounded-full shadow-lg"
                onClick={() => console.log('Load more')}
              >
                探索更远
              </Button>
            </div>
          )}
        </>
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>grep -c "GraphControls" web/components/constellation/GraphControls.tsx && grep -c "ConstellationPageClient" web/app/(main)/constellation/page-client.tsx && grep -c "记忆星图" web/app/(main)/constellation/page.tsx && grep -c "探索更远" web/app/(main)/constellation/page-client.tsx</automated>
  </verify>
  <done>GraphControls component with zoom/reset/filter; constellation page with empty state, loading state, error state, and "探索更远" button; data transformation from API to graph format</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | JWT token in Authorization header; all requests authenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Information Disclosure | Graph node data | accept | Nodes only contain public memory fields; no vector data exposed |
| T-13-09 | Denial of Service | Client-side filter | accept | Filter is client-side only; no backend impact |
</threat_model>

<verification>
- [ ] `npm install react-force-graph-2d graphology` succeeds
- [ ] `web/types/constellation.ts` exports GraphNode, GraphEdge, ExploreState
- [ ] `web/lib/api.ts` has getConstellation() and exploreMemory() methods
- [ ] ConstellationGraph uses dynamic import with ssr: false
- [ ] Custom nodeCanvasObject renders tag colors, content type shapes, starred glow
- [ ] GraphControls renders zoom in/out/reset buttons and filter input
- [ ] Constellation page shows empty state when no memories
- [ ] Constellation page shows loading skeleton during fetch
- [ ] `npm run build` in web/ compiles without errors
</verification>

<success_criteria>
- react-force-graph-2d and graphology installed
- ConstellationGraph renders with custom node colors, shapes, and starred glow
- GraphControls provides zoom and filter functionality
- Constellation page fetches data from API and displays graph
- Empty state, loading state, and error state all implemented
- Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-memory-constellation/13-03-SUMMARY.md`
</output>
