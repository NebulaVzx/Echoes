---
phase: 06-echo-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/migrations/002_chat_tables.sql
  - services/gateway/internal/chat/domain/conversation.go
autonomous: true
requirements:
  - CHAT-13
must_haves:
  truths:
    - "conversations table exists with UUID PK, user_id FK, title, timestamps"
    - "messages table exists with UUID PK, conversation_id FK, role, content, citations JSONB"
    - "Go domain models match SQL schema with GORM tags"
    - "Indexes exist for user_id + updated_at and conversation_id + created_at"
  artifacts:
    - path: "shared/migrations/002_chat_tables.sql"
      provides: "Chat database schema"
      contains: "CREATE TABLE conversations"
    - path: "services/gateway/internal/chat/domain/conversation.go"
      provides: "Go domain models for chat"
      contains: "type Conversation struct"
  key_links:
    - from: "conversation.go"
      to: "002_chat_tables.sql"
      via: "GORM model maps to SQL schema"
      pattern: "gorm:.*type:uuid"
---

<objective>
Create the database schema and Go domain models for chat conversations and messages.

Purpose: Persistent storage for chat history is the foundation of the Echo Assistant feature.
Output: SQL migration file + Go domain models with GORM tags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-echo-assistant/06-CONTEXT.md
@.planning/phases/06-echo-assistant/06-RESEARCH.md

# Existing patterns to follow
@shared/migrations/001_init.sql
@services/memory-service/internal/domain/memory.go

<interfaces>
From 001_init.sql:
- Uses `gen_random_uuid()` for UUID generation
- Uses `TIMESTAMP WITH TIME ZONE` for timestamps
- Has `update_updated_at_column()` trigger function (already defined)
- Uses `IF NOT EXISTS` for idempotent migrations
- Includes comments on tables and columns

From memory.go domain model:
```go
type Memory struct {
    ID        uuid.UUID `gorm:"type:uuid;primary_key;default:gen_random_uuid()" json:"id"`
    UserID    uuid.UUID `gorm:"type:uuid;not null;index" json:"user_id"`
    CreatedAt time.Time `json:"created_at"`
    UpdatedAt time.Time `json:"updated_at"`
}
func (Memory) TableName() string { return "memories" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chat tables migration</name>
  <files>shared/migrations/002_chat_tables.sql</files>
  <read_first>
    - shared/migrations/001_init.sql
    - .planning/phases/06-echo-assistant/06-RESEARCH.md (Database Schema section)
  </read_first>
  <action>
Create `shared/migrations/002_chat_tables.sql` with the following exact content:

1. `conversations` table:
   - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
   - `user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE`
   - `title VARCHAR(255) NOT NULL DEFAULT '新对话'`
   - `created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP`
   - `updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP`

2. `messages` table:
   - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
   - `conversation_id UUID NOT NULL REFERENCES conversations(id) ON DELETE CASCADE`
   - `role VARCHAR(20) NOT NULL CHECK (role IN ('user', 'assistant'))`
   - `content TEXT NOT NULL`
   - `citations JSONB DEFAULT '[]'` — array of objects with shape `{index: int, memory_id: string, title: string, similarity: float}`
   - `created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP`

3. Indexes:
   - `idx_conversations_user_id ON conversations(user_id)`
   - `idx_conversations_user_updated ON conversations(user_id, updated_at DESC)`
   - `idx_messages_conversation_id ON messages(conversation_id)`
   - `idx_messages_conversation_created ON messages(conversation_id, created_at ASC)`

4. Trigger for conversations updated_at (reuse existing `update_updated_at_column()` function):
   - Drop if exists, then create `update_conversations_updated_at` trigger

5. Comments on both tables for documentation.

Use `IF NOT EXISTS` for all CREATE statements. Do NOT redefine `update_updated_at_column()` — it already exists from 001_init.sql.
  </action>
  <verify>
    <automated>grep -n "CREATE TABLE.*conversations" shared/migrations/002_chat_tables.sql && grep -n "CREATE TABLE.*messages" shared/migrations/002_chat_tables.sql && grep -n "idx_conversations_user_updated" shared/migrations/002_chat_tables.sql</automated>
  </verify>
  <done>Migration file exists with conversations and messages tables, indexes, trigger, and comments.</done>
  <acceptance_criteria>
    - `shared/migrations/002_chat_tables.sql` exists
    - File contains `CREATE TABLE IF NOT EXISTS conversations`
    - File contains `CREATE TABLE IF NOT EXISTS messages`
    - File contains `role VARCHAR(20) NOT NULL CHECK (role IN ('user', 'assistant'))`
    - File contains `citations JSONB DEFAULT '[]'`
    - File contains `idx_conversations_user_updated` index
    - File contains `idx_messages_conversation_created` index
    - File contains `update_conversations_updated_at` trigger (using existing function)
    - File does NOT redefine `update_updated_at_column()` function
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Go domain models</name>
  <files>services/gateway/internal/chat/domain/conversation.go</files>
  <read_first>
    - services/memory-service/internal/domain/memory.go
    - .planning/phases/06-echo-assistant/06-RESEARCH.md
  </read_first>
  <action>
Create `services/gateway/internal/chat/domain/conversation.go` with Go domain models matching the SQL schema.

Define the following types:

1. `Conversation` struct with fields:
   - `ID uuid.UUID` with gorm tag `type:uuid;primary_key;default:gen_random_uuid()` and json tag `id`
   - `UserID uuid.UUID` with gorm tag `type:uuid;not null;index` and json tag `user_id`
   - `Title string` with gorm tag `type:varchar(255);not null;default:'新对话'` and json tag `title`
   - `CreatedAt time.Time` with json tag `created_at`
   - `UpdatedAt time.Time` with json tag `updated_at`
   - `TableName()` returns `"conversations"`

2. `Message` struct with fields:
   - `ID uuid.UUID` with gorm tag `type:uuid;primary_key;default:gen_random_uuid()` and json tag `id`
   - `ConversationID uuid.UUID` with gorm tag `type:uuid;not null;index"` and json tag `conversation_id`
   - `Role string` with gorm tag `type:varchar(20);not null"` and json tag `role`
   - `Content string` with gorm tag `type:text;not null"` and json tag `content`
   - `Citations datatypes.JSON` (from gorm.io/datatypes) with gorm tag `type:jsonb;default:'[]'"` and json tag `citations"`
   - `CreatedAt time.Time` with json tag `created_at"
   - `TableName()` returns `"messages"`

3. `Citation` struct (for JSONB array elements):
   - `Index int` json tag `index`
   - `MemoryID string` json tag `memory_id`
   - `Title string` json tag `title`
   - `Similarity float64` json tag `similarity`

4. `CreateConversationRequest` struct:
   - `Title string` with json tag `title" binding:"omitempty,max=255"`

5. `SendMessageRequest` struct:
   - `ConversationID string` with json tag `conversation_id" binding:"omitempty,uuid"`
   - `Content string` with json tag `content" binding:"required,max=10000"`

6. `ChatResponse` struct (for API responses):
   - `Message *Message` json tag `message"
   - `Citations []Citation` json tag `citations"

Package name: `domain`

Import needed packages: `time`, `github.com/google/uuid`, `gorm.io/datatypes`.
  </action>
  <verify>
    <automated>grep -n "type Conversation struct" services/gateway/internal/chat/domain/conversation.go && grep -n "type Message struct" services/gateway/internal/chat/domain/conversation.go && grep -n "type Citation struct" services/gateway/internal/chat/domain/conversation.go</automated>
  </verify>
  <done>Domain models file exists with Conversation, Message, Citation structs and request/response DTOs.</done>
  <acceptance_criteria>
    - `services/gateway/internal/chat/domain/conversation.go` exists
    - File contains `package domain`
    - File contains `type Conversation struct` with `TableName()` returning `"conversations"`
    - File contains `type Message struct` with `TableName()` returning `"messages"`
    - File contains `type Citation struct` with fields `Index`, `MemoryID`, `Title`, `Similarity`
    - File contains `type SendMessageRequest` with `Content` field having `binding:"required"`
    - File imports `github.com/google/uuid` and `gorm.io/datatypes`
    - `Message.Citations` uses `datatypes.JSON` type
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database | PostgreSQL stores conversation data scoped to user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | conversations table | mitigate | user_id FK with ON DELETE CASCADE ensures user deletion cleans up chat data |
| T-06-02 | Tampering | messages.citations JSONB | accept | Citations are LLM-generated metadata; validation happens at application layer |
</threat_model>

<verification>
- Migration file can be applied: `psql $DATABASE_URL -f shared/migrations/002_chat_tables.sql` exits 0
- Go file compiles: `cd services/gateway && go build ./internal/chat/domain/...` exits 0
</verification>

<success_criteria>
- SQL migration creates conversations and messages tables with correct columns, constraints, and indexes
- Go domain models compile and match the SQL schema exactly
- All tables use UUID primary keys with gen_random_uuid() default
- Foreign keys have ON DELETE CASCADE
</success_criteria>

<output>
After completion, create `.planning/phases/06-echo-assistant/06-01-SUMMARY.md`
</output>
