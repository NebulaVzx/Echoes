---
phase: 07-bug-fixes-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/gateway/internal/router/router.go
  - services/gateway/internal/middleware/auth.go
  - services/gateway/internal/router/router_test.go
  - services/gateway/internal/middleware/auth_test.go
autonomous: true
requirements:
  - BUG-03
  - BUG-04
  - BUG-05
must_haves:
  truths:
    - Gateway /health 返回下游服务（User Service、Memory Service）健康状态
    - 下游服务不可用时返回 HTTP 503 + 具体服务名称
    - 反向代理有显式连接超时配置（非无限阻塞）
    - isPublicRoute 使用 filepath.Clean 防止路径遍历绕过（CR-02）
  artifacts:
    - path: services/gateway/internal/router/router.go
      provides: "聚合健康检查 + 反向代理超时"
      contains:
        - "healthCheckHandler"
        - "checkServiceHealth"
        - "proxy.Transport"
        - "DialContext"
    - path: services/gateway/internal/middleware/auth.go
      provides: "路径遍历修复"
      contains:
        - "filepath.Clean"
    - path: services/gateway/internal/router/router_test.go
      provides: "健康检查测试"
    - path: services/gateway/internal/middleware/auth_test.go
      provides: "auth 中间件测试"
  key_links:
    - from: "/health"
      to: "User Service /api/v1/auth/me"
      via: "HTTP HEAD 探测"
    - from: "/health"
      to: "Memory Service /api/v1/memories"
      via: "HTTP HEAD 探测"
    - from: "isPublicRoute"
      to: "filepath.Clean"
      via: "路径规范化"
---

<objective>
增强 Gateway 健康检查：聚合下游服务状态、返回 503 降级响应、为反向代理添加超时配置。同时修复 auth 中间件路径遍历漏洞（CR-02）。

Purpose: 提升系统可观测性和安全性，防止路径遍历绕过认证
Output: 增强版 /health 端点、带超时的反向代理、安全的 isPublicRoute
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-bug-fixes-quality/07-CONTEXT.md
@services/gateway/internal/router/router.go
@services/gateway/internal/middleware/auth.go

## 现有代码关键片段

### router.go /health（lines 42-49）
```go
router.GET("/health", func(c *gin.Context) {
    c.JSON(200, gin.H{
        "status":  "ok",
        "service": "gateway",
        "version": "0.2.0",
    })
})
```

### router.go newReverseProxy（lines 103-132）
```go
func newReverseProxy(envKey, defaultURL string) *httputil.ReverseProxy {
    // ... parse target URL ...
    proxy := httputil.NewSingleHostReverseProxy(target)
    proxy.Transport = otelhttp.NewTransport(&http.Transport{
        MaxIdleConns:        100,
        MaxIdleConnsPerHost: 10,
        IdleConnTimeout:     90 * time.Second,
    })
    // ... Director ...
    return proxy
}
```

### auth.go isPublicRoute（lines 86-103）
```go
func isPublicRoute(path string) bool {
    publicPaths := []string{
        "/api/v1/auth/register",
        "/api/v1/auth/login",
        // ...
    }
    for _, p := range publicPaths {
        if path == p || strings.HasPrefix(path, p+"/") {
            return true
        }
    }
    return false
}
```
漏洞：`/health/../api/v1/memories` 不会被识别为需要认证的路径。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强 Gateway /health 端点 + 反向代理超时</name>
  <files>services/gateway/internal/router/router.go</files>
  <read_first>
    - services/gateway/internal/router/router.go (完整文件)
    - services/gateway/internal/middleware/auth.go (isPublicRoute 函数，用于理解 public paths)
  </read_first>
  <action>
    修改 `services/gateway/internal/router/router.go`，实现以下增强（per D-02）：

    1. **替换 /health handler**：将现有的静态 /health handler 替换为聚合式健康检查：
       ```go
       router.GET("/health", healthCheckHandler)
       ```

    2. **新增 healthCheckHandler 函数**：
       ```go
       func healthCheckHandler(c *gin.Context) {
           ctx, cancel := context.WithTimeout(c.Request.Context(), 2*time.Second)
           defer cancel()

           services := map[string]string{
               "user":   checkServiceHealth(ctx, os.Getenv("USER_SERVICE_URL"), "http://user-service:8001", "/api/v1/auth/me"),
               "memory": checkServiceHealth(ctx, os.Getenv("MEMORY_SERVICE_URL"), "http://memory-service:8002", "/api/v1/memories"),
           }

           overallStatus := "healthy"
           httpStatus := http.StatusOK
           for _, status := range services {
               if status != "ok" {
                   overallStatus = "degraded"
                   httpStatus = http.StatusServiceUnavailable
                   break
               }
           }

           c.JSON(httpStatus, gin.H{
               "status":   overallStatus,
               "gateway":  "ok",
               "services": services,
               "version":  "0.2.0",
           })
       }
       ```

    3. **新增 checkServiceHealth 函数**：
       ```go
       func checkServiceHealth(ctx context.Context, envURL, defaultURL, path string) string {
           targetURL := envURL
           if targetURL == "" {
               targetURL = defaultURL
           }
           req, err := http.NewRequestWithContext(ctx, http.MethodHead, targetURL+path, nil)
           if err != nil {
               return "unreachable"
           }
           client := &http.Client{Timeout: 2 * time.Second}
           resp, err := client.Do(req)
           if err != nil {
               return "unreachable"
           }
           defer resp.Body.Close()
           if resp.StatusCode >= 200 && resp.StatusCode < 500 {
               return "ok"
           }
           return "unreachable"
       }
       ```
       注意：使用 HEAD 请求减少负载；2 秒超时通过 `context.WithTimeout` 控制。

    4. **增强反向代理超时配置**：在 `newReverseProxy` 中，为 `http.Transport` 添加显式连接超时：
       ```go
       proxy.Transport = otelhttp.NewTransport(&http.Transport{
           MaxIdleConns:        100,
           MaxIdleConnsPerHost: 10,
           IdleConnTimeout:     90 * time.Second,
           // 新增超时配置
           DialContext: (&net.Dialer{
               Timeout:   5 * time.Second,
               KeepAlive: 30 * time.Second,
           }).DialContext,
           TLSHandshakeTimeout:   5 * time.Second,
           ResponseHeaderTimeout: 10 * time.Second,
           ExpectContinueTimeout: 1 * time.Second,
       })
       ```
       需要添加 `net` 包导入。

    5. **添加 context 包导入**（如尚未导入）。

    关键约束：
    - 健康检查不触发数据库查询（防止雪崩）
    - 使用 HEAD 请求减少下游负载
    - 每个下游检查 2 秒超时
    - 任一服务不可用时返回 HTTP 503
  </action>
  <verify>
    <automated>cd services/gateway && go build ./...</automated>
  </verify>
  <done>
    - router.go 编译通过
    - healthCheckHandler 函数存在，使用 context.WithTimeout(2*time.Second)
    - checkServiceHealth 函数存在，使用 http.MethodHead
    - 反向代理 Transport 包含 DialContext、TLSHandshakeTimeout、ResponseHeaderTimeout
    - /health 返回格式包含 status、gateway、services、version 字段
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复 isPublicRoute 路径遍历漏洞（CR-02）</name>
  <files>services/gateway/internal/middleware/auth.go</files>
  <read_first>
    - services/gateway/internal/middleware/auth.go (完整文件，关注 lines 85-103)
  </read_first>
  <action>
    修改 `services/gateway/internal/middleware/auth.go` 中的 `isPublicRoute` 函数（per D-05）：

    1. **添加 `path/filepath` 导入**（如尚未导入）。

    2. **修改 isPublicRoute 函数**：在检查前使用 `filepath.Clean` 规范化路径：
       ```go
       func isPublicRoute(path string) bool {
           cleanPath := filepath.Clean(path)
           publicPaths := []string{
               "/api/v1/auth/register",
               "/api/v1/auth/login",
               "/api/v1/auth/providers",
               "/api/v1/auth/github",
               "/api/v1/auth/github/callback",
               "/api/v1/auth/refresh",
               "/health",
           }
           for _, p := range publicPaths {
               if cleanPath == p || strings.HasPrefix(cleanPath, p+"/") {
                   return true
               }
           }
           return false
       }
       ```

    3. **验证修复效果**：`filepath.Clean("/health/../api/v1/memories")` 返回 `/api/v1/memories`，不会被误判为 public route。

    注意：保持 `strings` 包导入不变。
  </action>
  <verify>
    <automated>cd services/gateway && go build ./...</automated>
  </verify>
  <done>
    - auth.go 编译通过
    - isPublicRoute 函数第一行是 `cleanPath := filepath.Clean(path)`
    - filepath 包已导入
    - 路径遍历测试用例 `/health/../api/v1/memories` 返回 false
  </done>
</task>

<task type="auto">
  <name>Task 3: Gateway 中间件和路由测试</name>
  <files>
    services/gateway/internal/middleware/auth_test.go
    services/gateway/internal/router/router_test.go
  </files>
  <read_first>
    - services/gateway/internal/middleware/auth.go (isPublicRoute 修改后)
    - services/gateway/internal/router/router.go (healthCheckHandler 修改后)
  </read_first>
  <action>
    创建两个测试文件：

    ### 文件 1: services/gateway/internal/middleware/auth_test.go

    测试 `isPublicRoute` 函数：

    1. **TestIsPublicRoute_ExactMatch**：
       - 测试 `/health`、`/api/v1/auth/login`、`/api/v1/auth/register` 返回 true

    2. **TestIsPublicRoute_PrefixMatch**：
       - 测试 `/api/v1/auth/github/callback?code=xxx` 返回 true（HasPrefix 匹配）

    3. **TestIsPublicRoute_PathTraversal**（CR-02 修复验证）：
       - `"/health/../api/v1/memories"` → false
       - `"/api/v1/auth/login/../../memories"` → false
       - `"/api/v1/auth/login/.."` → false（Clean 后为 `/api/v1/auth`）
       - `"/../api/v1/memories"` → false（Clean 后为 `/api/v1/memories`）

    4. **TestIsPublicRoute_ProtectedPaths**：
       - `/api/v1/memories` → false
       - `/api/v1/search` → false
       - `/api/v1/chat/messages` → false

    由于 `isPublicRoute` 是私有函数，测试文件放在同一包 `package middleware` 中。

    ### 文件 2: services/gateway/internal/router/router_test.go

    测试健康检查响应格式：

    1. **TestHealthCheckHandler**：
       - 使用 `httptest.NewRecorder()` 和 `gin.CreateTestContext()` 创建测试上下文
       - 调用 `healthCheckHandler`
       - 验证响应 HTTP 状态码（200 或 503，取决于下游服务是否可达）
       - 验证响应 JSON 包含 `status`、`gateway`、`services`、`version` 字段
       - 验证 `services` 包含 `user` 和 `memory` 键

    2. **TestHealthCheckResponseStructure**：
       - 验证 JSON 结构符合预期：
         ```json
         {
           "status": "healthy|degraded",
           "gateway": "ok",
           "services": {
             "user": "ok|unreachable",
             "memory": "ok|unreachable"
           },
           "version": "0.2.0"
         }
         ```

    注意：健康检查测试可能需要 mock 下游服务或接受实际服务不可达时的 503 响应。由于测试环境可能无下游服务，重点验证响应结构正确，状态码可以是 503。
  </action>
  <verify>
    <automated>cd services/gateway && go test ./internal/middleware/ -run TestIsPublicRoute -v && go test ./internal/router/ -run TestHealthCheck -v</automated>
  </verify>
  <done>
    - auth_test.go 所有测试通过，包括路径遍历用例
    - router_test.go 测试通过，验证响应结构
    - 路径遍历测试 `/health/../api/v1/memories` 明确返回 false
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Gateway /health | 公开端点，无认证，返回服务拓扑信息 |
| Gateway → Downstream services | 内部网络，HEAD 探测 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Elevation of Privilege | isPublicRoute | mitigate | filepath.Clean 规范化路径，防止遍历绕过 |
| T-07-02-02 | Information Disclosure | /health response | accept | 仅返回服务名称和可达状态，无敏感数据 |
| T-07-02-03 | Denial of Service | health check probes | mitigate | HEAD 请求 + 2s 超时，避免雪崩 |
</threat_model>

<verification>
## 验证清单

1. **编译检查**：`cd services/gateway && go build ./...` 返回 0
2. **Auth 测试**：`cd services/gateway && go test ./internal/middleware/ -v` 所有测试 PASS
3. **Router 测试**：`cd services/gateway && go test ./internal/router/ -v` 所有测试 PASS
4. **代码审查**：
   - `grep -n "filepath.Clean" services/gateway/internal/middleware/auth.go` 能找到
   - `grep -n "healthCheckHandler\|checkServiceHealth" services/gateway/internal/router/router.go` 均能找到
   - `grep -n "DialContext\|ResponseHeaderTimeout" services/gateway/internal/router/router.go` 均能找到
5. **路径遍历验证**：确认 `TestIsPublicRoute_PathTraversal` 测试中 `/health/../api/v1/memories` 返回 false
</verification>

<success_criteria>
- Gateway /health 返回聚合健康状态（gateway + user + memory）
- 下游服务不可用时返回 HTTP 503，status 为 "degraded"
- 反向代理 Transport 配置包含 DialContext(5s)、TLSHandshakeTimeout(5s)、ResponseHeaderTimeout(10s)
- isPublicRoute 使用 filepath.Clean 防止路径遍历绕过
- 所有新增测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-bug-fixes-quality/07-02-SUMMARY.md`
</output>
