---
phase: 07-bug-fixes-quality
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - services/memory-service/internal/transport/memory_handler.go
  - services/memory-service/internal/service/memory_service.go
  - services/memory-service/internal/domain/memory.go
  - services/user-service/internal/domain/auth.go
  - services/user-service/internal/service/auth_service.go
  - services/user-service/internal/transport/auth_handler.go
  - web/lib/api.ts
  - web/components/memory/memory-list.tsx
  - web/app/(main)/page.tsx
  - web/app/(main)/settings/page.tsx
  - web/components/ui/pagination.tsx
autonomous: true
requirements:
  - BUG-06
  - BUG-07
  - BUG-08
must_haves:
  truths:
    - 时间轴页面支持双模式分页："加载更多"（默认）和"页码组件"
    - 用户可在 Settings 页面切换分页模式
    - 分页偏好持久化到 users.settings JSONB
    - 切换页码时保留滚动位置或平滑滚动到顶部
    - 移动端分页显示简洁（上一页/页码/下一页）
  artifacts:
    - path: services/memory-service/internal/domain/memory.go
      provides: "API 分页响应格式"
      contains:
        - "has_more"
        - "pagination"
    - path: services/memory-service/internal/service/memory_service.go
      provides: "HasMore 计算逻辑"
      contains:
        - "hasMore := int64(page*limit) < total"
    - path: services/user-service/internal/domain/auth.go
      provides: "UserSettings 分页字段"
      contains:
        - "PaginationMode"
    - path: services/user-service/internal/service/auth_service.go
      provides: "设置更新逻辑"
      contains:
        - "req.Pagination"
    - path: services/user-service/internal/transport/auth_handler.go
      provides: "HTTP handler 解析 pagination"
      contains:
        - "UpdateSettingsRequest"
    - path: web/components/ui/pagination.tsx
      provides: "页码组件"
    - path: web/components/memory/memory-list.tsx
      provides: "加载更多按钮"
    - path: web/app/(main)/page.tsx
      provides: "分页状态管理"
    - path: web/app/(main)/settings/page.tsx
      provides: "分页模式切换开关"
  key_links:
    - from: "page.tsx"
      to: "api.listMemories"
      via: "page + limit 参数"
    - from: "settings/page.tsx"
      to: "api.updateSettings"
      via: "pagination_mode 字段"
    - from: "Memory Service List handler"
      to: "ListMemoriesResponse"
      via: "新增 has_more 字段"
    - from: "auth_handler.go UpdateSettings"
      to: "UpdateSettingsRequest.Pagination"
      via: "HTTP JSON 解析"
---

<objective>
为时间轴页面添加双模式分页支持：默认"加载更多"模式，可在 Settings 切换为页码组件模式。分页偏好持久化到用户设置。

Purpose: 解决时间轴无分页导致的大量记忆加载问题，同时保留 Notion-like 极简交互
Output: 双模式分页 UI + API 分页增强 + 设置持久化
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-bug-fixes-quality/07-CONTEXT.md
@services/memory-service/internal/transport/memory_handler.go
@services/memory-service/internal/domain/memory.go
@web/lib/api.ts
@web/components/memory/memory-list.tsx
@web/app/(main)/page.tsx
@web/app/(main)/settings/page.tsx

## 现有代码关键片段

### memory_handler.go List (lines 135-166)
```go
func (h *MemoryHandler) List(c *gin.Context) {
    // ... parse page, limit, tag ...
    resp, err := h.memoryService.List(c.Request.Context(), userID, page, limit, tag)
    // ...
    c.JSON(http.StatusOK, gin.H{"success": true, "data": resp})
}
```

### domain/memory.go ListMemoriesResponse (lines 73-79)
```go
type ListMemoriesResponse struct {
    Memories []map[string]interface{} `json:"memories"`
    Total    int64                    `json:"total"`
    Page     int                      `json:"page"`
    Limit    int                      `json:"limit"`
}
```

### web/lib/api.ts listMemories (lines 300-307)
```typescript
async listMemories(params?: { page?: number; limit?: number; tag?: string }): Promise<ApiResponse<ListMemoriesResponse>> {
    const searchParams = new URLSearchParams()
    if (params?.page) searchParams.set('page', String(params.page))
    if (params?.limit) searchParams.set('limit', String(params.limit))
    if (params?.tag) searchParams.set('tag', params.tag)
    const query = searchParams.toString()
    return this.request<ListMemoriesResponse>('GET', `/api/v1/memories${query ? '?' + query : ''}`)
}
```

### web/app/(main)/page.tsx 当前加载逻辑 (lines 74-86)
```typescript
const loadMemories = useCallback(async () => {
    try {
      setIsLoading(true)
      const response = await api.listMemories({ page: 1, limit: 20 })
      if (response.success && response.data) {
        setMemories(response.data.memories)
      }
    } catch (err) {
      showToast(err instanceof Error ? err.message : '加载失败', 'error')
    } finally {
      setIsLoading(false)
    }
  }, [])
```

### settings/page.tsx 当前 schema (line 12-22)
```typescript
const settingsSchema = z.object({
  llm_provider: z.string().min(1).max(50),
  llm_protocol: z.enum(['openai', 'anthropic']),
  // ...
  similarity_threshold: z.number().min(0).max(1),
  rag_memory_limit: z.number().min(1).max(20),
})
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory Service 分页 API 增强</name>
  <files>
    services/memory-service/internal/domain/memory.go
    services/memory-service/internal/service/memory_service.go
    services/memory-service/internal/transport/memory_handler.go
  </files>
  <read_first>
    - services/memory-service/internal/domain/memory.go (ListMemoriesResponse 定义)
    - services/memory-service/internal/service/memory_service.go (List 方法 lines 219-244)
    - services/memory-service/internal/transport/memory_handler.go (List handler)
  </read_first>
  <action>
    1. **修改 ListMemoriesResponse**（`services/memory-service/internal/domain/memory.go`）：
       在现有字段基础上新增 `HasMore` 字段：
       ```go
       type ListMemoriesResponse struct {
           Memories []map[string]interface{} `json:"memories"`
           Total    int64                    `json:"total"`
           Page     int                      `json:"page"`
           Limit    int                      `json:"limit"`
           HasMore  bool                     `json:"has_more"`  // 新增
       }
       ```

    2. **修改 MemoryService.List**（`services/memory-service/internal/service/memory_service.go` lines 219-244）：
       在返回 `ListMemoriesResponse` 时计算 `HasMore`：
       ```go
       hasMore := int64(page*limit) < total
       return &domain.ListMemoriesResponse{
           Memories: items,
           Total:    total,
           Page:     page,
           Limit:    limit,
           HasMore:  hasMore,
       }, nil
       ```

    3. **验证 Memory Handler**（`services/memory-service/internal/transport/memory_handler.go`）：
       确认 List handler 的响应格式自动包含 `has_more`（因为 `ListMemoriesResponse` 已更新，handler 直接返回 `resp`）。
  </action>
  <verify>
    <automated>cd services/memory-service && go build ./...</automated>
  </verify>
  <done>
    - ListMemoriesResponse 包含 HasMore 字段
    - MemoryService.List 计算 hasMore = page*limit < total
    - Memory handler 编译通过，响应自动包含 has_more
  </done>
</task>

<task type="auto">
  <name>Task 2: User Service 设置 schema 扩展 + Handler 验证</name>
  <files>
    services/user-service/internal/domain/auth.go
    services/user-service/internal/service/auth_service.go
    services/user-service/internal/transport/auth_handler.go
  </files>
  <read_first>
    - services/user-service/internal/domain/auth.go (UserSettings, UpdateSettingsRequest)
    - services/user-service/internal/service/auth_service.go (UpdateUserSettings lines 155-224)
    - services/user-service/internal/transport/auth_handler.go (UpdateSettings handler)
  </read_first>
  <action>
    1. **扩展 UserSettings**（`services/user-service/internal/domain/auth.go`）：
       在 `UserSettings` struct 中新增分页偏好字段：
       ```go
       type UserSettings struct {
           LLMSettings
           SearchSimilarityThreshold float64 `json:"search_similarity_threshold,omitempty" binding:"omitempty,gte=0,lte=1"`
           RAGMemoryLimit            int     `json:"rag_memory_limit,omitempty" binding:"omitempty,gte=1,lte=20"`
           PaginationMode            string  `json:"pagination_mode,omitempty" binding:"omitempty,oneof=load_more page_numbers"`  // 新增
       }
       ```

    2. **扩展 UpdateSettingsRequest**（`services/user-service/internal/domain/auth.go`）：
       新增 PaginationSettings：
       ```go
       type PaginationSettings struct {
           Mode string `json:"mode,omitempty" binding:"omitempty,oneof=load_more page_numbers"`
       }
       ```
       并在 `UpdateSettingsRequest` 中添加：
       ```go
       type UpdateSettingsRequest struct {
           LLM        *LLMSettings        `json:"llm,omitempty"`
           Search     *SearchSettings     `json:"search,omitempty"`
           RAG        *RAGSettings        `json:"rag,omitempty"`
           Pagination *PaginationSettings `json:"pagination,omitempty"`  // 新增
       }
       ```

    3. **修改 AuthService.UpdateUserSettings**（`services/user-service/internal/service/auth_service.go` lines 155-224）：
       在更新逻辑中处理 pagination 设置：
       ```go
       // Update pagination settings if provided
       if req.Pagination != nil && req.Pagination.Mode != "" {
           existing.PaginationMode = req.Pagination.Mode
       }
       ```
       添加到现有 LLM/Search/RAG 更新逻辑之后。

    4. **验证 auth_handler.go 的 UpdateSettings 端点**（`services/user-service/internal/transport/auth_handler.go`）：
       确认 handler 的 `UpdateSettings` 方法正确解析并传递 `UpdateSettingsRequest` 到 service 层。
       - 检查 handler 是否使用 `c.ShouldBindJSON(&req)` 或类似方式解析请求体
       - 确认解析后的 `req` 直接传递给 `authService.UpdateUserSettings(ctx, userID, req)`
       - 如果 handler 中有字段过滤/白名单逻辑，确保 `pagination` 字段未被过滤掉
       - 关键验证：`grep -n "UpdateSettingsRequest\|UpdateUserSettings" services/user-service/internal/transport/auth_handler.go`
  </action>
  <verify>
    <automated>cd services/user-service && go build ./...</automated>
  </verify>
  <done>
    - UserSettings 包含 PaginationMode 字段，binding 为 oneof=load_more page_numbers
    - UpdateSettingsRequest 包含 Pagination 字段
    - AuthService.UpdateUserSettings 处理 req.Pagination
    - auth_handler.go 的 UpdateSettings 端点解析并传递 pagination 字段到 service 层
    - 两个服务编译通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 前端分页组件 + API 客户端更新</name>
  <files>
    web/lib/api.ts
    web/components/ui/pagination.tsx
    web/components/memory/memory-list.tsx
  </files>
  <read_first>
    - web/lib/api.ts (ListMemoriesResponse 接口, listMemories 方法)
    - web/components/memory/memory-list.tsx (当前实现)
  </read_first>
  <action>
    1. **更新 web/lib/api.ts**：
       - 在 `ListMemoriesResponse` 接口添加 `has_more`：
         ```typescript
         export interface ListMemoriesResponse {
           memories: Memory[]
           total: number
           page: number
           limit: number
           has_more: boolean  // 新增
         }
         ```
       - 在 `UserSettings` 接口添加 `pagination_mode`：
         ```typescript
         export interface UserSettings extends LLMSettings {
           search_similarity_threshold?: number
           rag_memory_limit?: number
           pagination_mode?: 'load_more' | 'page_numbers'  // 新增
         }
         ```
       - 在 `UpdateSettingsRequest` 接口添加 `pagination`：
         ```typescript
         export interface UpdateSettingsRequest {
           llm?: LLMSettings
           search?: SearchSettings
           rag?: RAGSettings
           pagination?: { mode?: 'load_more' | 'page_numbers' }  // 新增
         }
         ```

    2. **创建 web/components/ui/pagination.tsx**（页码组件）：
       设计一个简洁的页码组件，支持：
       - 显示当前页码、总页数
       - 上一页/下一页按钮
       - 页码快速跳转（显示最多 5 个页码：首页 + 当前页附近 + 末页）
       - 移动端适配：简化显示，仅显示 上一页 / 当前页/总页数 / 下一页
       - Props 接口：
         ```typescript
         interface PaginationProps {
           currentPage: number
           totalPages: number
           onPageChange: (page: number) => void
         }
         ```
       样式使用 Tailwind，与现有设计系统一致（圆角、灰色边框、hover 效果）。

    3. **修改 web/components/memory/memory-list.tsx**：
       扩展 `MemoryListProps` 以支持"加载更多"模式：
       ```typescript
       interface MemoryListProps {
         memories: Memory[]
         hasMore?: boolean
         onLoadMore?: () => void
         isLoadingMore?: boolean
       }
       ```
       在组件底部添加"加载更多"按钮（当 `hasMore` 为 true 时）：
       ```tsx
       {hasMore && onLoadMore && (
         <div className="flex justify-center py-4">
           <button
             onClick={onLoadMore}
             disabled={isLoadingMore}
             className="px-4 py-2 text-sm text-gray-600 dark:text-gray-400 border border-gray-200 dark:border-gray-600 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors disabled:opacity-50"
           >
             {isLoadingMore ? '加载中...' : '加载更多'}
           </button>
         </div>
       )}
       ```
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <done>
    - api.ts 中 ListMemoriesResponse 包含 has_more
    - api.ts 中 UserSettings 包含 pagination_mode
    - pagination.tsx 组件存在，导出 Pagination 组件
    - memory-list.tsx 支持 hasMore/onLoadMore/isLoadingMore props
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 4: 时间轴页面分页集成 + 设置页面切换开关</name>
  <files>
    web/app/(main)/page.tsx
    web/app/(main)/settings/page.tsx
  </files>
  <read_first>
    - web/app/(main)/page.tsx (当前 loadMemories 和状态管理)
    - web/app/(main)/settings/page.tsx (当前设置表单结构)
    - web/lib/api.ts (更新后的接口)
    - web/components/ui/pagination.tsx (新组件)
    - web/components/memory/memory-list.tsx (更新后的 props)
  </read_first>
  <action>
    1. **修改 web/app/(main)/page.tsx**：
       添加分页状态管理：
       ```typescript
       const [page, setPage] = useState(1)
       const [limit] = useState(20)
       const [hasMore, setHasMore] = useState(false)
       const [total, setTotal] = useState(0)
       const [isLoadingMore, setIsLoadingMore] = useState(false)
       const [paginationMode, setPaginationMode] = useState<'load_more' | 'page_numbers'>('load_more')
       ```

       修改 `loadMemories` 以支持分页参数和模式：
       ```typescript
       const loadMemories = useCallback(async (targetPage: number = 1, append: boolean = false) => {
         try {
           if (targetPage === 1) setIsLoading(true)
           else setIsLoadingMore(true)

           const response = await api.listMemories({ page: targetPage, limit })
           if (response.success && response.data) {
             if (append) {
               setMemories(prev => [...prev, ...response.data.memories])
             } else {
               setMemories(response.data.memories)
             }
             setHasMore(response.data.has_more)
             setTotal(response.data.total)
             setPage(targetPage)
           }
         } catch (err) {
           showToast(err instanceof Error ? err.message : '加载失败', 'error')
         } finally {
           setIsLoading(false)
           setIsLoadingMore(false)
         }
       }, [limit])
       ```

       添加加载更多回调：
       ```typescript
       const handleLoadMore = useCallback(() => {
         if (!hasMore || isLoadingMore) return
         loadMemories(page + 1, true)
       }, [hasMore, isLoadingMore, page, loadMemories])
       ```

       添加页码切换回调（BUG-08：平滑滚动到顶部）：
       ```typescript
       const handlePageChange = useCallback((newPage: number) => {
         loadMemories(newPage, false)
         window.scrollTo({ top: 0, behavior: 'smooth' })
       }, [loadMemories])
       ```

       在组件挂载时加载用户设置以获取分页模式：
       ```typescript
       useEffect(() => {
         const loadSettings = async () => {
           try {
             const response = await api.getSettings()
             if (response.success && response.data?.pagination_mode) {
               setPaginationMode(response.data.pagination_mode)
             }
           } catch {
             // 忽略设置加载失败，使用默认 load_more
           }
         }
         loadSettings()
       }, [])
       ```

       修改渲染部分：
       - 时间轴标题区域显示总数量：`{total} 条记忆`
       - 传递分页 props 给 MemoryList：
         ```tsx
         <MemoryList
           memories={memories}
           hasMore={paginationMode === 'load_more' ? hasMore : undefined}
           onLoadMore={paginationMode === 'load_more' ? handleLoadMore : undefined}
           isLoadingMore={isLoadingMore}
         />
         ```
       - 在"页码组件"模式下显示 Pagination：
         ```tsx
         {paginationMode === 'page_numbers' && total > limit && (
           <div className="mt-6">
             <Pagination
               currentPage={page}
               totalPages={Math.ceil(total / limit)}
               onPageChange={handlePageChange}
             />
           </div>
         )}
         ```

    2. **修改 web/app/(main)/settings/page.tsx**：
       在"搜索偏好"部分之后新增"界面偏好"部分：

       ```tsx
       {/* Section 4: UI Preferences */}
       <section className="bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 p-6">
         <h2 className="text-lg font-semibold text-gray-900 dark:text-gray-50 mb-1">
           界面偏好
         </h2>
         <p className="text-sm text-gray-500 dark:text-gray-400 mb-6">
           自定义时间轴的交互方式
         </p>

         <div className="space-y-5">
           {/* Pagination Mode */}
           <div>
             <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
               分页模式
             </label>
             <div className="flex gap-3">
               <label className="flex items-center gap-2 px-4 py-2 border border-gray-200 dark:border-gray-600 rounded-lg cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
                 <input
                   type="radio"
                   {...register('pagination_mode')}
                   value="load_more"
                   className="text-gray-900 dark:text-gray-100"
                 />
                 <span className="text-sm text-gray-700 dark:text-gray-300">加载更多</span>
               </label>
               <label className="flex items-center gap-2 px-4 py-2 border border-gray-200 dark:border-gray-600 rounded-lg cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
                 <input
                   type="radio"
                   {...register('pagination_mode')}
                   value="page_numbers"
                   className="text-gray-900 dark:text-gray-100"
                 />
                 <span className="text-sm text-gray-700 dark:text-gray-300">页码组件</span>
               </label>
             </div>
             <p className="mt-1 text-xs text-gray-500 dark:text-gray-500">
               "加载更多"适合浏览，"页码组件"适合快速跳转
             </p>
           </div>

           <div className="pt-2">
             <button
               type="button"
               onClick={onSaveUIPreferences}
               disabled={isSavingUI}
               className="px-4 py-2 bg-gray-900 dark:bg-gray-100 text-white dark:text-gray-900 rounded-md text-sm font-medium hover:bg-gray-800 dark:hover:bg-gray-200 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
             >
               {isSavingUI ? '保存中...' : '保存界面偏好'}
             </button>
           </div>
         </div>
       </section>
       ```

       在 schema 中添加 `pagination_mode` 字段（已在 Task 3 中完成，确认 settingsSchema 包含）：
       ```typescript
       pagination_mode: z.enum(['load_more', 'page_numbers']).optional()
       ```

       在 defaultValues 中添加：
       ```typescript
       pagination_mode: 'load_more'
       ```

       在 loadSettings 的 reset 中添加：
       ```typescript
       pagination_mode: (data.pagination_mode as 'load_more' | 'page_numbers') || 'load_more'
       ```

       新增保存回调 `onSaveUIPreferences`：
       ```typescript
       const [isSavingUI, setIsSavingUI] = useState(false)
       const onSaveUIPreferences = async () => {
         setIsSavingUI(true)
         dismissToast()
         const data = getValues()
         const payload = {
           pagination: {
             mode: data.pagination_mode || 'load_more'
           }
         }
         try {
           const response = await api.updateSettings(payload)
           if (response.success) {
             showToast('界面偏好已保存', 'success')
           } else {
             showToast(response.error?.message || '保存失败', 'error')
           }
         } catch (err) {
           showToast(err instanceof Error ? err.message : '保存失败', 'error')
         } finally {
           setIsSavingUI(false)
         }
       }
       ```
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit</automated>
  </verify>
  <done>
    - page.tsx 支持 page/limit/hasMore/total/paginationMode 状态
    - loadMemories 支持 targetPage 和 append 参数
    - handleLoadMore 加载下一页并追加到列表
    - handlePageChange 切换页码并 smooth scroll 到顶部
    - settings/page.tsx 有"界面偏好"部分，包含分页模式单选
    - settings/page.tsx 有 onSaveUIPreferences 保存回调
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API pagination params | page/limit 为整数，服务端已校验范围 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Denial of Service | page/limit params | mitigate | Memory Service handler 已校验 limit <= 100，page >= 1 |
</threat_model>

<verification>
## 验证清单

1. **TypeScript 编译**：`cd web && npx tsc --noEmit` 返回 0
2. **Go 编译**：`cd services/memory-service && go build ./... && cd ../user-service && go build ./...`
3. **代码审查**：
   - `grep -n "has_more" services/memory-service/internal/domain/memory.go` 能找到
   - `grep -n "PaginationMode\|pagination_mode" services/user-service/internal/domain/auth.go` 能找到
   - `grep -n "has_more" web/lib/api.ts` 能找到
   - `grep -n "Pagination" web/components/ui/pagination.tsx` 能找到
   - `grep -n "paginationMode\|handleLoadMore\|handlePageChange" web/app/(main)/page.tsx` 均能找到
   - `grep -n "界面偏好\|pagination_mode" web/app/(main)/settings/page.tsx` 均能找到
4. **Handler 验证**：`grep -n "UpdateSettingsRequest\|UpdateUserSettings" services/user-service/internal/transport/auth_handler.go` 确认 pagination 字段被传递
5. **响应格式验证**：确认 ListMemoriesResponse 包含 has_more 字段
</verification>

<success_criteria>
- 时间轴默认显示"加载更多"按钮，点击加载下一页并追加
- Settings 页面有"界面偏好"部分，可切换分页模式
- 切换为"页码组件"后时间轴显示页码导航
- 切换页码时页面 smooth scroll 到顶部
- 分页偏好保存到 users.settings JSONB，刷新后保持
- API 响应包含 has_more 字段
- 移动端页码显示简洁（上一页/当前页/总页数/下一页）
</success_criteria>

<output>
After completion, create `.planning/phases/07-bug-fixes-quality/07-03-SUMMARY.md`
</output>
