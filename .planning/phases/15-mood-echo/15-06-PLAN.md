---
phase: 15-mood-echo
plan: 06
type: execute
wave: 3
depends_on: [15-05]
files_modified:
  - web/components/warmth/daily-review-card.tsx
  - web/components/echo/echo-card.tsx
  - web/components/echo/echo-style-selector.tsx
  - web/components/echo/echo-message.tsx
  - web/app/(main)/page.tsx
autonomous: true
requirements:
  - MOOD-11
  - MOOD-12
must_haves:
  truths:
    - "DailyReviewCard shows echo section when expanded"
    - "Echo style selector allows switching between 4 styles"
    - "Style preference persists in localStorage"
    - "Echo message can be regenerated on demand"
  artifacts:
    - path: "web/components/echo/echo-card.tsx"
      provides: "Extended DailyReview with echo support"
      exports: ["EchoCard"]
    - path: "web/components/echo/echo-style-selector.tsx"
      provides: "Style picker component"
      exports: ["EchoStyleSelector"]
    - path: "web/components/echo/echo-message.tsx"
      provides: "Echo text display with animation"
      exports: ["EchoMessage"]
  key_links:
    - from: "echo-card.tsx"
      to: "lib/api.ts getDailyReview"
      via: "api.getDailyReview(style)"
      pattern: "getDailyReview"
    - from: "echo-style-selector.tsx"
      to: "localStorage"
      via: "echo_style_preference"
      pattern: "localStorage.setItem.*echo_style"
---

<objective>
扩展 DailyReview 卡片支持回响功能。创建 EchoCard、风格选择器、回响消息展示组件，并集成到首页。

Purpose: 让用户每天收到一条温暖的旧记忆回响
Output: Echo 组件集 + DailyReviewCard 扩展 + 首页集成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-mood-echo/15-UI-SPEC.md
@.planning/phases/15-mood-echo/15-RESEARCH.md

# Key interfaces from codebase

From web/components/warmth/daily-review-card.tsx (existing component):
```tsx
interface DailyReviewData {
  today_count: number
  top_tags: string[]
  worth_reviewing?: Memory
}

export default function DailyReviewCard() {
  const [data, setData] = useState<DailyReviewData | null>(null)
  const [collapsed, setCollapsed] = useState(() => {
    if (typeof window === 'undefined') return false
    return localStorage.getItem('daily_review_collapsed') === 'true'
  })
  // ...
}
```

From 15-UI-SPEC.md (EchoCard props):
```typescript
interface EchoCardProps {
  today_count: number
  top_tags: string[]
  worth_reviewing?: Memory
  echo_message?: string
  echo_style?: 'warm' | 'humorous' | 'concise' | 'poetic'
  onGenerateEcho?: () => void
  onStyleChange?: (style: string) => void
}
```

From 15-UI-SPEC.md (EchoStyleSelector):
```typescript
interface EchoStyleSelectorProps {
  value: string
  onChange: (value: string) => void
  options: { value: string; label: string; icon: LucideIcon }[]
}
```
Options:
| Value | Label | Icon |
|-------|-------|------|
| warm | 温暖安慰 | Heart |
| humorous | 幽默调侃 | Laugh |
| concise | 简洁洞察 | Lightbulb |
| poetic | 诗意文艺 | Feather |

From 15-UI-SPEC.md (Animation):
```typescript
// Echo text reveal
initial={{ opacity: 0, y: 8 }}
animate={{ opacity: 1, y: 0 }}
transition={{ duration: 0.3, delay: paragraphIndex * 0.05 }}
```

From web/lib/api.ts (DailyReviewWithEcho):
```typescript
export interface DailyReviewWithEcho {
  today_count: number
  top_tags: string[]
  worth_reviewing?: Memory
  echo_message?: string
  echo_style?: string
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Echo 风格选择器和回响消息展示组件</name>
  <files>
    web/components/echo/echo-style-selector.tsx
    web/components/echo/echo-message.tsx
  </files>
  <read_first>
    - 15-UI-SPEC.md §EchoStyleSelector (详细规格)
    - 15-UI-SPEC.md §EchoCard (echo section 规格)
    - web/components/ui/button.tsx (Button 组件)
  </read_first>
  <action>
    1. 创建 `web/components/echo/echo-style-selector.tsx`：
    ```tsx
    'use client'

    import { Heart, Laugh, Lightbulb, Feather } from 'lucide-react'
    import { Button } from '@/components/ui/button'
    import { motion } from 'framer-motion'

    interface EchoStyleOption {
      value: string
      label: string
      icon: React.ComponentType<{ className?: string }>
    }

    const options: EchoStyleOption[] = [
      { value: 'warm', label: '温暖安慰', icon: Heart },
      { value: 'humorous', label: '幽默调侃', icon: Laugh },
      { value: 'concise', label: '简洁洞察', icon: Lightbulb },
      { value: 'poetic', label: '诗意文艺', icon: Feather },
    ]

    interface EchoStyleSelectorProps {
      value: string
      onChange: (value: string) => void
    }

    export default function EchoStyleSelector({ value, onChange }: EchoStyleSelectorProps) {
      return (
        <div className="flex flex-wrap gap-1" role="radiogroup" aria-label="回响风格">
          {options.map((option) => {
            const Icon = option.icon
            const isSelected = value === option.value
            return (
              <Button
                key={option.value}
                variant={isSelected ? 'secondary' : 'ghost'}
                size="sm"
                className={`text-xs gap-1 ${isSelected ? 'border-l-2 border-primary' : 'text-muted-foreground'}`}
                onClick={() => onChange(option.value)}
                role="radio"
                aria-checked={isSelected}
              >
                <Icon className="w-3.5 h-3.5" />
                <span>{option.label}</span>
              </Button>
            )
          })}
        </div>
      )
    }
    ```

    2. 创建 `web/components/echo/echo-message.tsx`：
    ```tsx
    'use client'

    import { motion } from 'framer-motion'

    interface EchoMessageProps {
      message: string
      isLoading?: boolean
    }

    export default function EchoMessage({ message, isLoading }: EchoMessageProps) {
      if (isLoading) {
        return (
          <div className="bg-muted/50 rounded-lg p-4 space-y-2">
            <div className="h-4 bg-muted rounded animate-pulse w-full" />
            <div className="h-4 bg-muted rounded animate-pulse w-3/4" />
            <div className="h-4 bg-muted rounded animate-pulse w-1/2" />
          </div>
        )
      }

      const paragraphs = message.split('\n').filter((p) => p.trim())

      return (
        <div className="bg-muted/50 rounded-lg p-4 relative">
          <span className="absolute top-2 left-3 text-2xl text-muted-foreground font-serif leading-none">
            &ldquo;
          </span>
          <div className="pl-4 space-y-2">
            {paragraphs.map((paragraph, index) => (
              <motion.p
                key={index}
                initial={{ opacity: 0, y: 8 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.3, delay: index * 0.05 }}
                className="text-sm leading-relaxed italic text-foreground"
              >
                {paragraph}
              </motion.p>
            ))}
          </div>
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>grep -c "export default function EchoStyleSelector" web/components/echo/echo-style-selector.tsx && grep -c "export default function EchoMessage" web/components/echo/echo-message.tsx</automated>
  </verify>
  <done>Echo 风格选择器和消息展示组件创建完成</done>
  <acceptance_criteria>
    - `web/components/echo/echo-style-selector.tsx` 存在，导出默认组件
    - 包含 4 个选项：warm(Heart), humorous(Laugh), concise(Lightbulb), poetic(Feather)
    - 使用 `role="radiogroup"` 和 `role="radio"` 无障碍属性
    - 选中项使用 `secondary` variant，未选中使用 `ghost` variant
    - `web/components/echo/echo-message.tsx` 存在，导出默认组件
    - loading 状态显示 3 行 skeleton pulse
    - 正常状态显示引号装饰 + 段落动画（stagger 50ms）
    - 文本使用 italic 样式
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 创建 EchoCard 组件并扩展 DailyReviewCard</name>
  <files>
    web/components/echo/echo-card.tsx
    web/components/warmth/daily-review-card.tsx
  </files>
  <read_first>
    - web/components/warmth/daily-review-card.tsx (完整现有实现)
    - web/lib/api.ts (DailyReviewWithEcho 接口)
    - 15-UI-SPEC.md §EchoCard (详细规格)
  </read_first>
  <action>
    1. 创建 `web/components/echo/echo-card.tsx`：
    ```tsx
    'use client'

    import { useState, useEffect, useCallback } from 'react'
    import { motion, AnimatePresence } from 'framer-motion'
import { ChevronDown, ChevronUp, Sparkles, RefreshCw } from 'lucide-react'
    import { Button } from '@/components/ui/button'
    import { Separator } from '@/components/ui/separator'
    import { api, Memory, DailyReviewWithEcho } from '@/lib/api'
    import EchoStyleSelector from './echo-style-selector'
    import EchoMessage from './echo-message'
    import Link from 'next/link'

    export default function EchoCard() {
      const [data, setData] = useState<DailyReviewWithEcho | null>(null)
      const [collapsed, setCollapsed] = useState(() => {
        if (typeof window === 'undefined') return true
        return localStorage.getItem('daily_review_collapsed') !== 'false'
      })
      const [style, setStyle] = useState(() => {
        if (typeof window === 'undefined') return 'warm'
        return localStorage.getItem('echo_style_preference') || 'warm'
      })
      const [echoLoading, setEchoLoading] = useState(false)

      const fetchData = useCallback(async (selectedStyle?: string) => {
        try {
          const res = await api.getDailyReview(selectedStyle || style)
          if (res.success && res.data) {
            setData(res.data)
          }
        } catch (err) {
          console.error('Failed to fetch daily review:', err)
        }
      }, [style])

      useEffect(() => {
        fetchData()
      }, [fetchData])

      const toggleCollapse = () => {
        const next = !collapsed
        setCollapsed(next)
        localStorage.setItem('daily_review_collapsed', String(next))
      }

      const handleStyleChange = (newStyle: string) => {
        setStyle(newStyle)
        localStorage.setItem('echo_style_preference', newStyle)
        setEchoLoading(true)
        fetchData(newStyle).finally(() => setEchoLoading(false))
      }

      const handleRegenerate = () => {
        setEchoLoading(true)
        fetchData(style).finally(() => setEchoLoading(false))
      }

      if (!data) return null

      return (
        <div className="mb-5 rounded-xl border border-gray-100 dark:border-gray-700 bg-white dark:bg-gray-800 overflow-hidden">
          <button
            onClick={toggleCollapse}
            className="w-full flex items-center justify-between p-3 text-sm"
            aria-expanded={!collapsed}
          >
            <div className="flex items-center gap-2">
              <Sparkles className="w-4 h-4 text-sky-500" />
              <span className="font-medium text-gray-800 dark:text-gray-200">今日拾忆</span>
            </div>
            {collapsed ? (
              <ChevronDown className="w-4 h-4 text-gray-400" />
            ) : (
              <ChevronUp className="w-4 h-4 text-gray-400" />
            )}
          </button>

          <AnimatePresence>
            {!collapsed && (
              <motion.div
                initial={{ height: 0, opacity: 0 }}
                animate={{ height: 'auto', opacity: 1 }}
                exit={{ height: 0, opacity: 0 }}
                transition={{ duration: 0.25, ease: 'easeOut' }}
                className="overflow-hidden"
              >
                <div className="px-3 pb-3 space-y-3">
                  {/* Existing content */}
                  {data.today_count > 0 ? (
                    <>
                      <p className="text-sm text-gray-600 dark:text-gray-400">
                        今天保存了 {data.today_count} 条记忆
                        {data.top_tags && data.top_tags.length > 0 && (
                          <>，标签：{data.top_tags.join('、')}</>
                        )}
                      </p>
                    </>
                  ) : (
                    <p className="text-sm text-gray-600 dark:text-gray-400">
                      今天还没有保存记忆
                    </p>
                  )}

                  {data.worth_reviewing && (
                    <div className="bg-gray-50 dark:bg-gray-700/50 rounded-lg p-3">
                      <p className="text-xs text-gray-500 dark:text-gray-400 mb-1">
                        值得回顾
                      </p>
                      <Link
                        href={`/memory/${data.worth_reviewing.id}`}
                        className="text-sm text-sky-600 dark:text-sky-400 hover:underline line-clamp-2"
                      >
                        {data.worth_reviewing.text_content || data.worth_reviewing.link_title || '无标题记忆'}
                      </Link>
                    </div>
                  )}

                  {/* Echo Section */}
                  <Separator />

                  <div className="space-y-3">
                    <div className="flex items-center justify-between">
                      <EchoStyleSelector value={style} onChange={handleStyleChange} />
                      {data.echo_message && (
                        <Button
                          variant="ghost"
                          size="sm"
                          className="text-xs gap-1"
                          onClick={handleRegenerate}
                          disabled={echoLoading}
                        >
                          <RefreshCw className={`w-3 h-3 ${echoLoading ? 'animate-spin' : ''}`} />
                          重新生成
                        </Button>
                      )}
                    </div>

                    {data.echo_message ? (
                      <EchoMessage message={data.echo_message} isLoading={echoLoading} />
                    ) : echoLoading ? (
                      <EchoMessage message="" isLoading={true} />
                    ) : (
                      <p className="text-sm text-muted-foreground text-center py-4">
                        今天还没有回响，点击生成一条温暖的记忆回响吧
                      </p>
                    )}
                  </div>
                </div>
              </motion.div>
            )}
          </AnimatePresence>
        </div>
      )
    }
    ```

    2. 修改 `web/components/warmth/daily-review-card.tsx`，将其内容替换为对 EchoCard 的 re-export（保持向后兼容）：
    ```tsx
    'use client'

    import EchoCard from '@/components/echo/echo-card'

    export default function DailyReviewCard() {
      return <EchoCard />
    }
    ```
    或者直接在 `web/app/(main)/page.tsx` 中将 `DailyReviewCard` 的 import 替换为 `EchoCard`。
  </action>
  <verify>
    <automated>grep -c "export default function EchoCard" web/components/echo/echo-card.tsx && grep -c "EchoCard" web/components/warmth/daily-review-card.tsx</automated>
  </verify>
  <done>EchoCard 创建完成，DailyReviewCard 向后兼容或已替换</done>
  <acceptance_criteria>
    - `web/components/echo/echo-card.tsx` 存在，导出默认组件
    - 默认折叠（localStorage `daily_review_collapsed` 默认 'true'）
    - 展开时显示原有内容（today_count, top_tags, worth_reviewing）
    - 展开时显示 EchoStyleSelector 和 EchoMessage
    - 切换风格时重新调用 `api.getDailyReview(newStyle)`
    - 风格偏好持久化到 localStorage `echo_style_preference`
    - 有 "重新生成" 按钮，点击后重新获取回响
    - loading 状态显示 skeleton
    - 使用 Framer Motion AnimatePresence 展开/折叠动画
    - `web/components/warmth/daily-review-card.tsx` 导入并渲染 EchoCard（向后兼容）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 在首页集成 EchoCard，添加导航入口</name>
  <files>
    web/app/(main)/page.tsx
    web/components/layout/sidebar.tsx
  </files>
  <read_first>
    - web/app/(main)/page.tsx (现有 DailyReviewCard 导入位置)
    - web/components/layout/sidebar.tsx (导航菜单)
  </read_first>
  <action>
    1. 修改 `web/app/(main)/page.tsx`，将 `DailyReviewCard` 的 import 替换为 `EchoCard`：
    ```tsx
    // 替换这一行：
    // import DailyReviewCard from '@/components/warmth/daily-review-card'
    import EchoCard from '@/components/echo/echo-card'
    ```
    在 JSX 中将 `<DailyReviewCard />` 替换为 `<EchoCard />`。

    2. 在 `web/components/layout/sidebar.tsx`（或导航组件）中添加 `/mood` 导航入口。
    查找现有导航项（如 `/explore` 或 `/weave`），在其旁边添加：
    ```tsx
    {
      href: '/mood',
      label: '情绪日历',
      icon: Heart, // 或 CalendarHeart
    }
    ```
    如果导航组件使用不同的结构，遵循相同模式添加即可。
  </action>
  <verify>
    <automated>grep -c "EchoCard" web/app/(main)/page.tsx && grep -c "/mood" web/components/layout/sidebar.tsx</automated>
  </verify>
  <done>首页使用 EchoCard，导航添加情绪日历入口</done>
  <acceptance_criteria>
    - `web/app/(main)/page.tsx` 导入 `EchoCard` 而非 `DailyReviewCard`
    - 页面渲染 `<EchoCard />`
    - `web/components/layout/sidebar.tsx` 包含 `/mood` 导航链接
    - 导航标签为 "情绪日历"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Next.js -> API | JWT Bearer token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-13 | Information Disclosure | localStorage echo_style_preference | accept | 风格偏好非敏感信息 |
| T-15-14 | Denial of Service | Rapid style switching | mitigate | 前端 loading 状态阻止重复点击 |
</threat_model>

<verification>
1. TypeScript 类型检查：`cd web && npx tsc --noEmit`
2. 构建验证：`cd web && npm run build`
3. 组件渲染检查：EchoCard 展开/折叠、风格切换、重新生成
</verification>

<success_criteria>
- [ ] `web/components/echo/echo-style-selector.tsx` 创建完成
- [ ] `web/components/echo/echo-message.tsx` 创建完成
- [ ] `web/components/echo/echo-card.tsx` 创建完成
- [ ] `web/components/warmth/daily-review-card.tsx` 向后兼容或已替换
- [ ] 首页使用 EchoCard
- [ ] 导航添加 `/mood` 入口
- [ ] `tsc --noEmit` 通过
- [ ] `npm run build` 通过
</success_criteria>

<output>
After completion, create `.planning/phases/15-mood-echo/15-06-SUMMARY.md`
</output>
