---
phase: 15-mood-echo
plan: 07
type: execute
wave: 4
depends_on: [15-03, 15-06]
files_modified:
  - scripts/backfill_mood.py
  - scripts/backfill_mood.ps1
  - scripts/backfill_mood.sh
  - docker-compose.yml
  - services/processor-service/Dockerfile
  - services/memory-service/Dockerfile
autonomous: false
requirements:
  - MOOD-05
must_haves:
  truths:
    - "Batch backfill script can analyze all existing memories"
    - "Script publishes mood:generate tasks in paginated batches"
    - "Docker Compose includes mood consumer configuration"
  artifacts:
    - path: "scripts/backfill_mood.py"
      provides: "Batch backfill script for existing memories"
      exports: ["main"]
    - path: "scripts/backfill_mood.sh"
      provides: "Unix wrapper script"
      exports: ["run"]
    - path: "scripts/backfill_mood.ps1"
      provides: "Windows wrapper script"
      exports: ["run"]
  key_links:
    - from: "backfill_mood.py"
      to: "Redis Stream mood:generate"
      via: "redis.xadd"
      pattern: "mood:generate"
---

<objective>
创建批量回溯脚本，为所有已有记忆生成情绪分析任务。同时更新 Docker 配置确保 mood consumer 在生产环境正确启动。

Purpose: 让已有记忆也能获得情绪分析（D-01, D-02 全量回溯）
Output: 批量回溯脚本（Python + sh + ps1）+ Docker 配置更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-mood-echo/15-CONTEXT.md
@.planning/phases/15-mood-echo/15-RESEARCH.md

# Key interfaces from codebase

From services/memory-service/internal/service/redis_queue.go (PublishTask pattern):
```go
func (q *RedisTaskQueue) PublishTask(ctx context.Context, stream string, fields map[string]interface{}) error
```

From services/processor-service/app/config.py:
```python
enable_mood_consumer: bool = True
```

From RESEARCH.md (Pitfall 2):
```
Process in batches of 50 memories. Add delay between batches.
```

From docker-compose.yml (existing service definitions):
```yaml
processor-service:
  environment:
    - ENABLE_LINK_CONSUMER=true
    - ENABLE_TAG_CONSUMER=true
    - ENABLE_SUGGESTION_CONSUMER=true
    - ENABLE_FILE_CONSUMER=true
    - ENABLE_COVER_CONSUMER=true
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建批量回溯脚本</name>
  <files>
    scripts/backfill_mood.py
    scripts/backfill_mood.sh
    scripts/backfill_mood.ps1
  </files>
  <read_first>
    - services/processor-service/app/consumers/tag_consumer.py (_create_llm 模式，了解字段格式)
    - services/memory-service/internal/service/redis_queue.go (PublishTask 字段格式)
    - RESEARCH.md §Common Pitfalls (batch backfill 建议)
  </read_first>
  <action>
    1. 创建 `scripts/backfill_mood.py`：
    ```python
    #!/usr/bin/env python3
    """
    Echoes Mood Backfill Script
    Publishes mood:generate tasks for all existing memories.
    Run after deploying Phase 15 to analyze emotions for existing memories.

    Usage:
        python scripts/backfill_mood.py [--batch-size 50] [--delay 1]
    """

    import argparse
    import os
    import time
    import sys

    import psycopg2
    import redis


    def get_db_connection():
        """Get PostgreSQL connection from environment or defaults."""
        dsn = os.getenv("DATABASE_URL", "postgres://echoes_user:echoes_password@localhost:5432/echoes")
        return psycopg2.connect(dsn)


    def get_redis_client():
        """Get Redis client from environment or defaults."""
        redis_url = os.getenv("REDIS_URL", "redis://localhost:6379/0")
        return redis.Redis.from_url(redis_url, decode_responses=True)


    def fetch_memories(conn, offset, limit):
        """Fetch memories paginated."""
        with conn.cursor() as cur:
            cur.execute("""
                SELECT id, user_id, content_type, text_content, link_url, link_title, link_summary, note
                FROM memories
                ORDER BY created_at DESC
                LIMIT %s OFFSET %s
            """, (limit, offset))
            return cur.fetchall()


    def count_memories(conn):
        """Count total memories."""
        with conn.cursor() as cur:
            cur.execute("SELECT COUNT(*) FROM memories")
            return cur.fetchone()[0]


    def build_content(memory):
        """Build content for mood analysis based on content type."""
        content_type = memory[2]
        text_content = memory[3] or ""
        link_title = memory[5] or ""
        link_summary = memory[6] or ""
        note = memory[7] or ""

        if content_type == "link" and link_title:
            content = link_title
            if link_summary:
                content += "\n" + link_summary
            return content

        if content_type == "file":
            return text_content

        if content_type == "weave":
            return text_content

        # text or fallback
        content = text_content
        if note and content_type == "text":
            content += "\n\n备注: " + note
        return content


    def publish_mood_task(redis_client, memory_id, content_type, content, note=""):
        """Publish a mood:generate task to Redis Stream."""
        fields = {
            "memory_id": str(memory_id),
            "content_type": content_type,
            "content": content[:3000],  # Limit content length
        }
        if note:
            fields["note"] = note[:1000]

        redis_client.xadd("mood:generate", fields, maxlen=5000, approximate=True)


    def main():
        parser = argparse.ArgumentParser(description="Backfill mood analysis for existing memories")
        parser.add_argument("--batch-size", type=int, default=50, help="Memories per batch")
        parser.add_argument("--delay", type=float, default=1.0, help="Delay between batches (seconds)")
        parser.add_argument("--dry-run", action="store_true", help="Print what would be done without publishing")
        args = parser.parse_args()

        conn = get_db_connection()
        redis_client = get_redis_client()

        total = count_memories(conn)
        print(f"Total memories to process: {total}")

        if total == 0:
            print("No memories found.")
            return

        processed = 0
        offset = 0
        batch = 0

        while True:
            memories = fetch_memories(conn, offset, args.batch_size)
            if not memories:
                break

            batch += 1
            print(f"Batch {batch}: Processing {len(memories)} memories (offset {offset})...")

            for memory in memories:
                memory_id = memory[0]
                content_type = memory[2]
                content = build_content(memory)
                note = memory[7] or ""

                if not content.strip():
                    print(f"  Skipping {memory_id}: no content")
                    continue

                if args.dry_run:
                    print(f"  Would publish mood:generate for {memory_id} ({content_type}, {len(content)} chars)")
                else:
                    publish_mood_task(redis_client, memory_id, content_type, content, note)

                processed += 1

            offset += len(memories)

            if memories and not args.dry_run:
                print(f"  Published {processed}/{total} tasks. Sleeping {args.delay}s...")
                time.sleep(args.delay)

        print(f"Done! Published {processed} mood:generate tasks.")
        conn.close()


    if __name__ == "__main__":
        main()
    ```

    2. 创建 `scripts/backfill_mood.sh`：
    ```bash
    #!/bin/bash
    # Echoes Mood Backfill Script (Unix wrapper)
    set -e

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    cd "$SCRIPT_DIR/.."

    echo "Starting mood backfill..."
    python3 scripts/backfill_mood.py "$@"
    echo "Backfill complete."
    ```

    3. 创建 `scripts/backfill_mood.ps1`：
    ```powershell
    # Echoes Mood Backfill Script (Windows wrapper)
    $scriptDir = Split-Path -Parent $MyInvocation.MyCommand.Path
    $projectDir = Split-Path -Parent $scriptDir

    Set-Location $projectDir

    Write-Host "Starting mood backfill..."
    python scripts/backfill_mood.py @args
    Write-Host "Backfill complete."
    ```

    4. 确保脚本可执行：`chmod +x scripts/backfill_mood.sh scripts/backfill_mood.py`
  </action>
  <verify>
    <automated>ls scripts/backfill_mood.* | wc -l</automated>
  </verify>
  <done>批量回溯脚本创建完成（Python + sh + ps1）</done>
  <acceptance_criteria>
    - `scripts/backfill_mood.py` 存在，包含 argparse 参数解析
    - 支持 `--batch-size`（默认 50）、`--delay`（默认 1s）、`--dry-run`
    - 从 PostgreSQL 查询 memories 表，分页处理
    - 根据 content_type 构建分析内容（text/link/file/weave）
    - 发布到 Redis Stream `mood:generate`，maxlen=5000
    - `scripts/backfill_mood.sh` 存在，调用 Python 脚本
    - `scripts/backfill_mood.ps1` 存在，调用 Python 脚本
    - Python 脚本包含 shebang `#!/usr/bin/env python3`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 Docker Compose 和 Dockerfile 配置</name>
  <files>
    docker-compose.yml
    services/processor-service/Dockerfile
    services/memory-service/Dockerfile
  </files>
  <read_first>
    - docker-compose.yml (现有 processor-service 环境变量)
    - services/processor-service/Dockerfile (现有构建配置)
    - services/memory-service/Dockerfile (现有构建配置)
  </read_first>
  <action>
    1. 在 `docker-compose.yml` 的 `processor-service` 环境变量部分添加：
    ```yaml
    environment:
      - ENABLE_MOOD_CONSUMER=true
    ```
    查找现有的 `ENABLE_*_CONSUMER` 变量，在其下方添加 `ENABLE_MOOD_CONSUMER=true`。

    2. 检查 `services/processor-service/Dockerfile` 是否需要更新（通常不需要，因为 consumer 启动由 `main.py` 的 `lifespan` 控制，而 `config.py` 已经读取环境变量）。

    3. 检查 `services/memory-service/Dockerfile` 是否需要更新（通常不需要，因为新代码会随构建自动包含）。

    4. 如果 `docker-compose.yml` 中有 `.env` 文件引用，确保 `ENABLE_MOOD_CONSUMER=true` 也在 `.env.example` 中添加（如果存在）。
  </action>
  <verify>
    <automated>grep -c "ENABLE_MOOD_CONSUMER" docker-compose.yml</automated>
  </verify>
  <done>Docker Compose 添加 mood consumer 环境变量</done>
  <acceptance_criteria>
    - `docker-compose.yml` 的 `processor-service` 环境变量包含 `ENABLE_MOOD_CONSUMER=true`
    - 环境变量位置与其他 `ENABLE_*_CONSUMER` 变量相邻
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: 端到端验证</name>
  <what-built>
    Phase 15 全部后端和前端组件：
    1. 数据库迁移（memory_emotions 表）
    2. MoodConsumer（Redis Stream 消费者）
    3. LLM Provider 扩展（analyze_sentiment, generate_echo）
    4. 后端 API（GET /mood/calendar, GET /mood/insight, 扩展 DailyReview）
    5. 前端组件（MoodCalendar, EchoCard, EchoStyleSelector 等）
    6. /mood 页面
    7. 批量回溯脚本
  </what-built>
  <how-to-verify>
    1. 启动所有服务：`make dev-start` 或 `docker-compose up -d`
    2. 运行迁移：`make migrate`（或手动执行 006_memory_emotions.sql）
    3. 创建一条新记忆，检查 processor-service 日志中是否有 mood:generate 处理
    4. 访问 `/mood` 页面，检查日历是否渲染
    5. 在首页检查 DailyReview 卡片，展开后查看回响功能
    6. 运行批量回溯脚本（dry-run 先）：`python scripts/backfill_mood.py --dry-run`
    7. 检查构建：前端 `npm run build`，后端 `go build ./...`
  </how-to-verify>
  <resume-signal>验证完成后输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backfill script -> Redis | 本地网络，需 Redis 访问权限 |
| Backfill script -> PostgreSQL | 本地网络，需 DB 访问权限 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | Denial of Service | Backfill overwhelms queue | mitigate | Batch size 50, delay 1s between batches, maxlen=5000 trim |
| T-15-16 | Information Disclosure | Backfill script DB access | accept | 脚本需要 DB 访问权限，仅限运维使用 |
</threat_model>

<verification>
1. 脚本语法检查：`python -m py_compile scripts/backfill_mood.py`
2. Docker Compose 语法检查：`docker-compose config`
3. 端到端验证（人工检查点）
</verification>

<success_criteria>
- [ ] `scripts/backfill_mood.py` 创建完成
- [ ] `scripts/backfill_mood.sh` 和 `scripts/backfill_mood.ps1` 创建完成
- [ ] `docker-compose.yml` 添加 `ENABLE_MOOD_CONSUMER=true`
- [ ] 端到端验证通过（人工确认）
</success_criteria>

<output>
After completion, create `.planning/phases/15-mood-echo/15-07-SUMMARY.md`
</output>
