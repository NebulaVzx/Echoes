---
phase: 15-mood-echo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/processor-service/app/services/llm/base.py
  - services/processor-service/app/services/llm/openai_provider.py
  - services/processor-service/app/services/llm/anthropic_provider.py
  - services/processor-service/app/services/llm/prompts/sentiment_prompts.py
  - services/processor-service/app/services/llm/prompts/echo_prompts.py
  - services/processor-service/app/consumers/mood_consumer.py
  - services/processor-service/app/config.py
  - services/processor-service/app/main.py
autonomous: true
requirements:
  - MOOD-02
  - MOOD-03
  - MOOD-09
must_haves:
  truths:
    - "LLM providers can analyze sentiment and return structured JSON"
    - "MoodConsumer processes mood:generate messages from Redis Stream"
    - "Echo prompts support 4 distinct styles (warm/humorous/concise/poetic)"
  artifacts:
    - path: "services/processor-service/app/services/llm/base.py"
      provides: "LLMProvider abstract class with analyze_sentiment and generate_echo"
      exports: ["analyze_sentiment", "generate_echo"]
    - path: "services/processor-service/app/consumers/mood_consumer.py"
      provides: "MoodConsumer extends RedisStreamConsumer"
      exports: ["MoodConsumer"]
    - path: "services/processor-service/app/services/llm/prompts/sentiment_prompts.py"
      provides: "Sentiment analysis prompt templates"
      exports: ["build_sentiment_prompt"]
    - path: "services/processor-service/app/services/llm/prompts/echo_prompts.py"
      provides: "Echo generation prompt templates (4 styles)"
      exports: ["build_echo_prompt", "ECHO_STYLE_PERSONAS"]
  key_links:
    - from: "mood_consumer.py process_message"
      to: "llm.analyze_sentiment"
      via: "LLMFactory.create -> analyze_sentiment"
      pattern: "llm.analyze_sentiment"
    - from: "mood_consumer.py"
      to: "memory_client.update_task_status"
      via: "PATCH /internal/memories/:id/tasks"
      pattern: "update_task_status.*mood:generate"
---

<objective>
构建情绪分析的 AI 处理层。扩展 LLM Provider 支持情绪分析和回响生成，创建 MoodConsumer 处理 Redis Stream 任务，并提供情绪分析和回响生成的 prompt 模板。

Purpose: 让 processor-service 能够异步分析记忆情绪并生成回响语
Output: LLM 方法扩展 + 2 个 prompt 模块 + MoodConsumer + 配置和启动集成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-mood-echo/15-CONTEXT.md
@.planning/phases/15-mood-echo/15-RESEARCH.md

# Key interfaces from codebase

From services/processor-service/app/services/llm/base.py:
```python
class LLMProvider(ABC):
    def __init__(self, model: str, temperature: float = 0.7):
        self.model = model
        self.temperature = temperature

    @abstractmethod
    async def generate(self, prompt: str, temperature: float = None, max_tokens: int = 500, timeout: float = None) -> str:
        pass

    @abstractmethod
    async def generate_tags(self, content: str) -> List[str]:
        pass

    @abstractmethod
    async def chat(self, messages: List[LLMMessage], temperature: float = None, max_tokens: int = 500) -> str:
        pass

    @abstractmethod
    async def generate_suggestion(self, prompt: str, temperature: float = None, max_tokens: int = 200, timeout: float = None) -> str:
        pass
```

From services/processor-service/app/consumers/tag_consumer.py (consumer pattern):
```python
class TagConsumer(RedisStreamConsumer):
    def __init__(self, redis_client: redis.Redis, memory_client: MemoryServiceClient):
        super().__init__(
            redis_client=redis_client,
            stream="tag:generate",
            group="processor-group",
            consumer="processor-tag-1",
            memory_client=memory_client,
            max_retries=3,
        )

    async def process_message(self, msg_id: str, fields: dict):
        memory_id = fields.get("memory_id", "")
        content = fields.get("content", "")
        llm = _create_llm(fields)
        tags = await llm.generate_tags(content)
        result = {"tags": tags}
        await self.memory_client.update_task_status(
            memory_id, "tag:generate", "completed", result=result
        )
```

From services/processor-service/app/config.py:
```python
class Settings(BaseSettings):
    enable_link_consumer: bool = True
    enable_tag_consumer: bool = True
    enable_suggestion_consumer: bool = True
    enable_file_consumer: bool = True
    enable_cover_consumer: bool = True
```

From services/processor-service/app/main.py (consumer startup):
```python
if settings.enable_tag_consumer:
    tag_consumer = TagConsumer(redis_client, memory_client)
    await tag_consumer.start()
    consumers.append(tag_consumer)
```

From RESEARCH.md (prompt templates):
```python
SENTIMENT_SYSTEM_PROMPT = """你是一位情绪分析专家..."""
ECHO_STYLE_PERSONAS = {
    "warm": "你是一位温柔体贴的老朋友...",
    "humorous": "你是一位幽默风趣的伙伴...",
    "concise": "你是一位洞察深刻的智者...",
    "poetic": "你是一位诗意文艺的观察者...",
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 LLM Provider 基类和实现类，添加 analyze_sentiment 和 generate_echo</name>
  <files>
    services/processor-service/app/services/llm/base.py
    services/processor-service/app/services/llm/openai_provider.py
    services/processor-service/app/services/llm/anthropic_provider.py
  </files>
  <read_first>
    - services/processor-service/app/services/llm/base.py (现有抽象方法)
    - services/processor-service/app/services/llm/openai_provider.py (generate, generate_tags, chat, generate_suggestion 实现模式)
    - services/processor-service/app/services/llm/anthropic_provider.py (相同模式)
  </read_first>
  <action>
    1. 在 `services/processor-service/app/services/llm/base.py` 的 `LLMProvider` 类中添加两个抽象方法：
    ```python
    @abstractmethod
    async def analyze_sentiment(self, content: str, content_type: str = "text") -> dict:
        """Analyze sentiment of content.
        Returns: {"sentiment": "positive|neutral|negative", "score": 1-10, "reason": str}
        """
        pass

    @abstractmethod
    async def generate_echo(self, memory_content: str, style: str, years_ago: int) -> str:
        """Generate an echo message for a memory.
        Returns: echo message string (80-150 Chinese characters)
        """
        pass
    ```

    2. 在 `services/processor-service/app/services/llm/openai_provider.py` 的 `OpenAIProvider` 类中实现这两个方法：
    ```python
    async def analyze_sentiment(self, content: str, content_type: str = "text") -> dict:
        from .prompts.sentiment_prompts import build_sentiment_prompt
        prompt = build_sentiment_prompt(content, content_type)
        result = await self.generate(prompt, temperature=0.3, max_tokens=200, timeout=15.0)
        return self._parse_sentiment_result(result)

    async def generate_echo(self, memory_content: str, style: str, years_ago: int) -> str:
        from .prompts.echo_prompts import build_echo_prompt
        prompt = build_echo_prompt(memory_content, style, years_ago)
        return await self.generate(prompt, temperature=0.8, max_tokens=300, timeout=15.0)

    def _parse_sentiment_result(self, result: str) -> dict:
        """Parse sentiment analysis result from LLM output.
        Handles both JSON and plain text formats with fallback.
        """
        import json
        import re

        # Try to extract JSON from the response
        try:
            # Look for JSON object in the response
            json_match = re.search(r'\{[^}]*"sentiment"[^}]*\}', result)
            if json_match:
                data = json.loads(json_match.group())
                sentiment = data.get("sentiment", "neutral")
                score = int(data.get("score", 5))
                reason = data.get("reason", "")
            else:
                # Try parsing the whole response as JSON
                data = json.loads(result)
                sentiment = data.get("sentiment", "neutral")
                score = int(data.get("score", 5))
                reason = data.get("reason", "")
        except (json.JSONDecodeError, ValueError):
            # Fallback: parse from text
            sentiment = "neutral"
            if "积极" in result or "positive" in result.lower():
                sentiment = "positive"
            elif "消极" in result or "negative" in result.lower():
                sentiment = "negative"

            score = 5
            score_match = re.search(r'(\d+)', result)
            if score_match:
                score = max(1, min(10, int(score_match.group(1))))

            reason = result[:100] if len(result) > 100 else result

        # Validate sentiment
        if sentiment not in ("positive", "neutral", "negative"):
            sentiment = "neutral"

        # Validate score
        score = max(1, min(10, score))

        return {
            "sentiment": sentiment,
            "score": score,
            "reason": reason,
        }
    ```

    3. 在 `services/processor-service/app/services/llm/anthropic_provider.py` 的 `AnthropicProvider` 类中实现相同的 `analyze_sentiment` 和 `generate_echo` 方法，调用逻辑与 OpenAIProvider 一致（复用相同的 prompt 和 _parse_sentiment_result）。
  </action>
  <verify>
    <automated>grep -c "async def analyze_sentiment" services/processor-service/app/services/llm/openai_provider.py && grep -c "async def generate_echo" services/processor-service/app/services/llm/openai_provider.py && grep -c "async def analyze_sentiment" services/processor-service/app/services/llm/anthropic_provider.py</automated>
  </verify>
  <done>OpenAI 和 Anthropic Provider 都实现了 analyze_sentiment 和 generate_echo 方法</done>
  <acceptance_criteria>
    - `base.py` 的 `LLMProvider` 包含 `analyze_sentiment` 和 `generate_echo` 抽象方法
    - `openai_provider.py` 的 `OpenAIProvider` 实现了 `analyze_sentiment` 和 `generate_echo`
    - `openai_provider.py` 包含 `_parse_sentiment_result` 方法，支持 JSON 解析和文本回退
    - `anthropic_provider.py` 的 `AnthropicProvider` 实现了 `analyze_sentiment` 和 `generate_echo`
    - `_parse_sentiment_result` 对 sentiment 进行白名单校验（positive/neutral/negative）
    - `_parse_sentiment_result` 对 score 进行 clamp（1-10）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 创建情绪分析和回响生成的 prompt 模板</name>
  <files>
    services/processor-service/app/services/llm/prompts/sentiment_prompts.py
    services/processor-service/app/services/llm/prompts/echo_prompts.py
  </files>
  <read_first>
    - services/processor-service/app/consumers/tag_consumer.py (_create_llm 函数模式)
    - RESEARCH.md §Code Examples (sentiment/echo prompt 模板)
  </read_first>
  <action>
    创建 `services/processor-service/app/services/llm/prompts/sentiment_prompts.py`：
    ```python
    SENTIMENT_SYSTEM_PROMPT = """你是一位情绪分析专家。请分析以下用户保存的记忆内容的情绪倾向。

要求：
1. 情绪分类（sentiment）：positive（积极）/ neutral（中性）/ negative（消极）
2. 情绪强度（score）：1-10 的整数
   - positive: 1=轻微积极, 10=非常积极
   - negative: 1=轻微消极, 10=非常消极
   - neutral: 一律为 5
3. 简短理由（reason）：20-50字，说明为什么这样判断

输出格式（严格JSON，不要其他内容）：
{"sentiment": "positive", "score": 7, "reason": "用户记录了一个愉快的周末活动，语气轻松"}

内容类型说明：
- text: 用户直接输入的文本
- link: 链接的标题和摘要
- file: 文件提取的文本内容
- weave: AI编织的文章内容
"""

    def build_sentiment_prompt(content: str, content_type: str = "text") -> str:
        return f"""{SENTIMENT_SYSTEM_PROMPT}

内容类型：{content_type}
内容：
{content[:3000]}

请分析以上内容的情绪倾向，输出JSON格式结果："""
    ```

    创建 `services/processor-service/app/services/llm/prompts/echo_prompts.py`：
    ```python
    ECHO_STYLE_PERSONAS = {
        "warm": "你是一位温柔体贴的老朋友，语气温暖、充满关怀，善于发现记忆中的美好。",
        "humorous": "你是一位幽默风趣的伙伴，善于用轻松调侃的方式让回忆变得有趣。",
        "concise": "你是一位洞察深刻的智者，用简洁有力的语言点出记忆的核心意义。",
        "poetic": "你是一位诗意文艺的观察者，用优美抒情的语言唤醒记忆的温度。",
    }

    def build_echo_prompt(memory_content: str, style: str, years_ago: int) -> str:
        persona = ECHO_STYLE_PERSONAS.get(style, ECHO_STYLE_PERSONAS["warm"])
        return f"""{persona}

你的任务：根据用户 {years_ago} 年前保存的这条记忆，生成一段温暖的回响语，让用户与过去的自己重逢。

要求：
- 长度：80-150个汉字
- 语气：根据选择的风格调整
- 内容：呼应记忆的主题，可以提出问题、分享感悟、或简单陪伴
- 不要总结记忆内容，而是以一种"对话"的方式与过去的用户交流

记忆内容：
{memory_content[:500]}

请直接输出回响语，不要加引号、标题或格式标记。"""
    ```
  </action>
  <verify>
    <automated>grep -c "build_sentiment_prompt" services/processor-service/app/services/llm/prompts/sentiment_prompts.py && grep -c "build_echo_prompt" services/processor-service/app/services/llm/prompts/echo_prompts.py</automated>
  </verify>
  <done>情绪分析和回响生成的 prompt 模板已创建，支持 4 种回响风格</done>
  <acceptance_criteria>
    - `sentiment_prompts.py` 存在，包含 `SENTIMENT_SYSTEM_PROMPT` 和 `build_sentiment_prompt`
    - `build_sentiment_prompt` 要求输出严格 JSON 格式
    - `echo_prompts.py` 存在，包含 `ECHO_STYLE_PERSONAS`（4 个风格）和 `build_echo_prompt`
    - `ECHO_STYLE_PERSONAS` 包含键：warm, humorous, concise, poetic
    - `build_echo_prompt` 限制记忆内容长度 `[:500]`
    - `build_sentiment_prompt` 限制内容长度 `[:3000]`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 创建 MoodConsumer 并集成到 processor-service 启动流程</name>
  <files>
    services/processor-service/app/consumers/mood_consumer.py
    services/processor-service/app/config.py
    services/processor-service/app/main.py
  </files>
  <read_first>
    - services/processor-service/app/consumers/tag_consumer.py (consumer 实现模式)
    - services/processor-service/app/consumers/base.py (RedisStreamConsumer 基类)
    - services/processor-service/app/config.py (enable_*_consumer 配置模式)
    - services/processor-service/app/main.py (consumer 启动模式)
  </read_first>
  <action>
    1. 创建 `services/processor-service/app/consumers/mood_consumer.py`：
    ```python
    import logging
    import redis.asyncio as redis
    from app.consumers.base import RedisStreamConsumer
    from app.clients.memory_client import MemoryServiceClient
    from app.services.llm.factory import LLMFactory
    from app.config import settings
    from app.crypto import decrypt

    logger = logging.getLogger(__name__)

    def _create_llm(fields: dict):
        """Create LLM provider with per-message overrides, decrypting API key if present."""
        protocol = fields.get("llm_protocol") or fields.get("llm_provider") or settings.llm_protocol or settings.llm_provider
        model = fields.get("llm_model") or settings.llm_model
        temp_raw = fields.get("llm_temperature")
        temperature = float(temp_raw) if temp_raw is not None else settings.llm_temperature
        api_key = None
        encrypted_key = fields.get("api_key")
        if encrypted_key:
            try:
                api_key = decrypt(encrypted_key)
            except Exception:
                api_key = encrypted_key
        base_url = fields.get("base_url")
        return LLMFactory.create(protocol=protocol, model=model, temperature=temperature, api_key=api_key, base_url=base_url)

    class MoodConsumer(RedisStreamConsumer):
        def __init__(self, redis_client: redis.Redis, memory_client: MemoryServiceClient):
            super().__init__(
                redis_client=redis_client,
                stream="mood:generate",
                group="processor-group",
                consumer="processor-mood-1",
                memory_client=memory_client,
                max_retries=3,
            )

        async def process_message(self, msg_id: str, fields: dict):
            memory_id = fields.get("memory_id", "")
            content = fields.get("content", "")
            content_type = fields.get("content_type", "text")

            if not content:
                raise ValueError("content is required for mood analysis")

            logger.info(f"[mood:generate] memory={memory_id} content_type={content_type} content_length={len(content)}")

            llm = _create_llm(fields)
            result = await llm.analyze_sentiment(content, content_type)

            logger.info(f"[mood:generate] memory={memory_id} sentiment={result['sentiment']} score={result['score']}")

            await self.memory_client.update_task_status(
                memory_id, "mood:generate", "completed",
                result={
                    "sentiment": result["sentiment"],
                    "score": result["score"],
                    "reason": result.get("reason", ""),
                }
            )
    ```

    2. 在 `services/processor-service/app/config.py` 的 `Settings` 类中添加：
    ```python
    enable_mood_consumer: bool = True
    ```

    3. 在 `services/processor-service/app/main.py` 中添加 MoodConsumer 启动（在 cover consumer 之后）：
    ```python
    from app.consumers.mood_consumer import MoodConsumer
    ```
    在 lifespan 的 consumer 启动部分添加：
    ```python
    if settings.enable_mood_consumer:
        mood_consumer = MoodConsumer(redis_client, memory_client)
        await mood_consumer.start()
        consumers.append(mood_consumer)
        print("Mood consumer started")
    ```
  </action>
  <verify>
    <automated>grep -c "class MoodConsumer" services/processor-service/app/consumers/mood_consumer.py && grep -c "enable_mood_consumer" services/processor-service/app/config.py && grep -c "MoodConsumer" services/processor-service/app/main.py</automated>
  </verify>
  <done>MoodConsumer 创建完成，配置和启动流程集成完毕</done>
  <acceptance_criteria>
    - `mood_consumer.py` 存在，包含 `class MoodConsumer(RedisStreamConsumer)`
    - `MoodConsumer` 的 stream 为 `"mood:generate"`
    - `MoodConsumer.process_message` 调用 `llm.analyze_sentiment` 并回写结果
    - `config.py` 包含 `enable_mood_consumer: bool = True`
    - `main.py` 导入 `MoodConsumer` 并在 `settings.enable_mood_consumer` 为 True 时启动
    - `main.py` 启动时打印 `"Mood consumer started"`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Processor Service -> LLM API | API key 加密传输，通过 HTTPS |
| Processor Service -> Memory Service | Bearer INTERNAL_API_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | LLM prompt injection | accept | 用户分析自己的内容，无第三方风险 |
| T-15-05 | Information Disclosure | sentiment result in logs | mitigate | logger 只记录 sentiment/score，不记录完整 reason |
| T-15-06 | Denial of Service | mood:generate backlog | mitigate | BaseConsumer 有 maxlen=5000 trim，pending recovery，指数退避重试 |
</threat_model>

<verification>
1. Python 语法检查：`python -m py_compile services/processor-service/app/consumers/mood_consumer.py`
2. Prompt 文件语法检查：`python -m py_compile services/processor-service/app/services/llm/prompts/sentiment_prompts.py`
3. 配置和启动文件语法检查
</verification>

<success_criteria>
- [ ] `base.py` 新增 `analyze_sentiment` 和 `generate_echo` 抽象方法
- [ ] `openai_provider.py` 和 `anthropic_provider.py` 实现两个新方法
- [ ] `_parse_sentiment_result` 支持 JSON 解析 + 文本回退，带校验
- [ ] `sentiment_prompts.py` 和 `echo_prompts.py` 创建完成
- [ ] `mood_consumer.py` 创建完成，继承 RedisStreamConsumer
- [ ] `config.py` 添加 `enable_mood_consumer`
- [ ] `main.py` 集成 MoodConsumer 启动
- [ ] Python 语法检查全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/15-mood-echo/15-02-SUMMARY.md`
</output>
