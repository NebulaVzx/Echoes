---
phase: 15-mood-echo
plan: 04
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - services/processor-service/app/main.py
  - services/processor-service/app/routers/generate.py
autonomous: true
requirements:
  - MOOD-09
must_haves:
  truths:
    - "Processor service exposes POST /api/v1/generate/echo endpoint"
    - "Endpoint accepts memory_content, style, years_ago and returns echo_message"
    - "Endpoint uses LLMFactory to call generate_echo on the appropriate provider"
  artifacts:
    - path: "services/processor-service/app/routers/generate.py"
      provides: "Echo generation HTTP endpoint"
      exports: ["POST /api/v1/generate/echo"]
    - path: "services/processor-service/app/main.py"
      provides: "Router registration"
      exports: ["app.include_router"]
  key_links:
    - from: "generate.py POST /generate/echo"
      to: "llm.generate_echo"
      via: "LLMFactory.create -> generate_echo"
      pattern: "llm.generate_echo"
---

<objective>
在 processor-service 中创建回响生成的 HTTP 端点，供 memory-service 调用。

Purpose: 为 memory-service 的 GenerateEcho 提供 LLM 生成服务
Output: POST /api/v1/generate/echo 端点 + router 注册
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-mood-echo/15-CONTEXT.md
@.planning/phases/15-mood-echo/15-RESEARCH.md

# Key interfaces from codebase

From services/processor-service/app/main.py (router registration pattern):
```python
app = FastAPI(...)
# Routers are registered in app/main.py or separate router files
```

From services/processor-service/app/services/llm/base.py:
```python
class LLMProvider(ABC):
    @abstractmethod
    async def generate_echo(self, memory_content: str, style: str, years_ago: int) -> str:
        pass
```

From RESEARCH.md (echo prompt):
```python
ECHO_STYLE_PERSONAS = {
    "warm": "你是一位温柔体贴的老朋友...",
    "humorous": "你是一位幽默风趣的伙伴...",
    "concise": "你是一位洞察深刻的智者...",
    "poetic": "你是一位诗意文艺的观察者...",
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 generate echo HTTP endpoint</name>
  <files>
    services/processor-service/app/routers/generate.py
    services/processor-service/app/main.py
  </files>
  <read_first>
    - services/processor-service/app/services/llm/factory.py (LLMFactory.create 签名)
    - services/processor-service/app/services/llm/base.py (generate_echo 签名)
    - services/processor-service/app/config.py (settings 访问模式)
  </read_first>
  <action>
    1. 创建 `services/processor-service/app/routers/generate.py`：
    ```python
    from fastapi import APIRouter, HTTPException
    from pydantic import BaseModel, Field
    from app.services.llm.factory import LLMFactory
    from app.config import settings

    router = APIRouter(prefix="/generate", tags=["generate"])

    class EchoRequest(BaseModel):
        memory_content: str = Field(..., min_length=1, max_length=2000)
        style: str = Field(default="warm", regex="^(warm|humorous|concise|poetic)$")
        years_ago: int = Field(default=0, ge=0, le=100)
        llm_config: dict = Field(default=None)

    class EchoResponse(BaseModel):
        echo_message: str

    @router.post("/echo", response_model=EchoResponse)
    async def generate_echo(request: EchoRequest):
        """Generate an echo message for a memory."""
        try:
            # Create LLM provider
            protocol = request.llm_config.get("llm_protocol") if request.llm_config else None
            model = request.llm_config.get("llm_model") if request.llm_config else None
            temperature = request.llm_config.get("llm_temperature") if request.llm_config else None
            api_key = request.llm_config.get("api_key") if request.llm_config else None
            base_url = request.llm_config.get("base_url") if request.llm_config else None

            llm = LLMFactory.create(
                protocol=protocol or settings.llm_protocol,
                model=model or settings.llm_model,
                temperature=temperature if temperature is not None else settings.llm_temperature,
                api_key=api_key,
                base_url=base_url,
            )

            echo_message = await llm.generate_echo(
                memory_content=request.memory_content,
                style=request.style,
                years_ago=request.years_ago,
            )

            return EchoResponse(echo_message=echo_message)
        except Exception as e:
            raise HTTPException(status_code=500, detail=f"Failed to generate echo: {str(e)}")
    ```

    2. 在 `services/processor-service/app/main.py` 中注册 router：
    - 在文件顶部添加：`from app.routers import generate`
    - 在 `app = FastAPI(...)` 之后添加：`app.include_router(generate.router)`
  </action>
  <verify>
    <automated>grep -c "class EchoRequest" services/processor-service/app/routers/generate.py && grep -c "generate_echo" services/processor-service/app/routers/generate.py && grep -c "generate.router" services/processor-service/app/main.py</automated>
  </verify>
  <done>generate echo endpoint 创建完成，router 注册到 main.py</done>
  <acceptance_criteria>
    - `services/processor-service/app/routers/generate.py` 存在
    - 包含 `EchoRequest` Pydantic model，字段：memory_content, style, years_ago, llm_config
    - style 有 regex 校验 `^(warm|humorous|concise|poetic)$`
    - 包含 `EchoResponse` Pydantic model，字段：echo_message
    - 包含 `@router.post("/echo")` 端点
    - 端点调用 `llm.generate_echo` 并返回 `EchoResponse`
    - `main.py` 导入 `generate` router 并 `app.include_router(generate.router)`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Memory Service -> Processor Service | Bearer INTERNAL_API_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Denial of Service | /generate/echo abuse | mitigate | memory_content max_length=2000, style regex validation |
</threat_model>

<verification>
1. Python 语法检查：`python -m py_compile services/processor-service/app/routers/generate.py`
2. FastAPI 启动测试（可选）：`cd services/processor-service && python -c "from app.main import app; print('OK')"`
</verification>

<success_criteria>
- [ ] `services/processor-service/app/routers/generate.py` 存在且语法正确
- [ ] `main.py` 注册 generate router
- [ ] POST /api/v1/generate/echo 端点接受 EchoRequest 并返回 EchoResponse
- [ ] Python 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/15-mood-echo/15-04-SUMMARY.md`
</output>
