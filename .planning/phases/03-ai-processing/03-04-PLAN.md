---
phase: 03-ai-processing
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - docker-compose.yml
  - .env.example
  - services/processor-service/Dockerfile
  - services/vectorizer-service/Dockerfile
  - services/processor-service/tests/test_imports.py
  - services/vectorizer-service/tests/test_imports.py
autonomous: true
requirements:
  - R5.4
  - R5.6
  - R2.5
  - R2.6
  - R2.7
must_haves:
  truths:
    - "Docker Compose includes all required env vars for Processor and Vectorizer"
    - "Processor and Vectorizer services start successfully with docker-compose up"
    - "End-to-end test creates a memory and verifies async processing completes"
    - "All services have health checks in docker-compose"
    - "Gateway does NOT route /api/v1/internal/* paths externally"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Updated with env vars, health checks, INTERNAL_API_TOKEN"
    - path: ".env.example"
      provides: "Template with all required env vars documented"
    - path: "services/processor-service/tests/test_imports.py"
      provides: "Basic import smoke test"
    - path: "services/vectorizer-service/tests/test_imports.py"
      provides: "Basic import smoke test"
  key_links:
    - from: "docker-compose.yml"
      to: "services/processor-service/Dockerfile"
      via: "build context"
    - from: "docker-compose.yml"
      to: "services/vectorizer-service/Dockerfile"
      via: "build context"
    - from: "Processor Service"
      to: "Memory Service"
      via: "MEMORY_SERVICE_URL + INTERNAL_API_TOKEN"
    - from: "Vectorizer Service"
      to: "Memory Service"
      via: "MEMORY_SERVICE_URL + INTERNAL_API_TOKEN"
    - from: "Gateway"
      to: "Memory Service internal routes"
      via: "MUST NOT route /api/v1/internal/* externally"
---

<objective>
Integrate all Phase 3 components: update Docker Compose with environment variables and health checks, ensure Dockerfiles are correct, add basic smoke tests, verify Gateway does not expose internal routes, and run end-to-end verification.

Purpose: Without proper Docker Compose configuration, the Processor and Vectorizer services cannot communicate with Redis and Memory Service. This plan wires everything together, verifies security boundaries, and validates the full async pipeline works.
Output: Updated docker-compose.yml, working containers, verified end-to-end flow, confirmed internal route isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-processing/03-CONTEXT.md
@.planning/phases/03-ai-processing/03-RESEARCH.md

## Research Note
RESEARCH.md Open Questions are RESOLVED as of this plan:
- Q1 (BGE-M3 dimension): Resolved — schema updated to VECTOR(1024) in 03-01
- Q2 (Internal API auth): Resolved — INTERNAL_API_TOKEN with Bearer middleware
- Q3 (Link content for vectorization): Resolved — Processor publishes derived text:vectorize task (implemented in 03-02)
- Q4 (Vector storage API): Resolved — internal API accepts result.vector, Memory Service handles pgvector SQL

## Key interfaces from existing code

From docker-compose.yml (current):
```yaml
  processor-service:
    build:
      context: ./services/processor-service
      dockerfile: Dockerfile
    container_name: echoes-processor
    restart: unless-stopped
    environment:
      - PORT=8003
      - REDIS_URL=redis://redis:6379/0
      - ENV=development
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - echoes-network

  vectorizer-service:
    build:
      context: ./services/vectorizer-service
      dockerfile: Dockerfile
    container_name: echoes-vectorizer
    restart: unless-stopped
    environment:
      - PORT=8004
      - REDIS_URL=redis://redis:6379/0
      - ENV=development
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - echoes-network
```

From services/processor-service/Dockerfile (assumed standard Python):
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8003"]
```

From services/vectorizer-service/Dockerfile (assumed standard Python):
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8004"]
```

## Required env vars (from plans 03-02 and 03-03)

Processor Service:
- REDIS_URL=redis://redis:6379/0
- MEMORY_SERVICE_URL=http://memory-service:8002
- INTERNAL_API_TOKEN (shared secret)
- LLM_PROVIDER (openai or anthropic)
- LLM_MODEL (e.g., gpt-4o-mini)
- LLM_TEMPERATURE (default 0.7)
- OPENAI_API_KEY
- ANTHROPIC_API_KEY
- ENABLE_LINK_CONSUMER=true
- ENABLE_TAG_CONSUMER=true

Vectorizer Service:
- REDIS_URL=redis://redis:6379/0
- MEMORY_SERVICE_URL=http://memory-service:8002
- INTERNAL_API_TOKEN (same shared secret)
- MODEL_NAME=BAAI/bge-m3
- DEVICE=auto
- ENABLE_VECTORIZE_CONSUMER=true

Memory Service:
- INTERNAL_API_TOKEN (same shared secret, for middleware validation)

## Gateway Internal Route Isolation

Per T-03-01 (threat_model in 03-01): Gateway must NOT route `/api/v1/internal/*` paths externally.
The Gateway service (services/gateway) routes requests to backend services. We must verify:
1. Gateway routing rules exclude `/api/v1/internal/*` from external access
2. If Gateway currently routes all `/api/v1/*` to Memory Service, a fix is needed
3. Internal routes should only be accessible within the Docker network
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Docker Compose with env vars and health checks</name>
  <files>
    docker-compose.yml
    .env.example
  </files>
  <read_first>
    - docker-compose.yml (current configuration)
    - services/processor-service/requirements.txt (to verify deps)
    - services/vectorizer-service/requirements.txt (to verify deps)
  </read_first>
  <action>
    **A. Update docker-compose.yml** — modify the processor-service and vectorizer-service sections. Keep all other services unchanged.

    Replace the `processor-service` section with:
    ```yaml
  # Processor Service (Python + FastAPI)
  processor-service:
    build:
      context: ./services/processor-service
      dockerfile: Dockerfile
    container_name: echoes-processor
    restart: unless-stopped
    environment:
      - PORT=8003
      - REDIS_URL=redis://redis:6379/0
      - MEMORY_SERVICE_URL=http://memory-service:8002
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN:-dev-internal-token}
      - LLM_PROVIDER=${LLM_PROVIDER:-openai}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o-mini}
      - LLM_TEMPERATURE=${LLM_TEMPERATURE:-0.7}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - ENABLE_LINK_CONSUMER=true
      - ENABLE_TAG_CONSUMER=true
      - ENV=development
    ports:
      - "8003:8003"
    depends_on:
      redis:
        condition: service_healthy
      memory-service:
        condition: service_started
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8003/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - echoes-network
    ```

    Replace the `vectorizer-service` section with:
    ```yaml
  # Vectorizer Service (Python + FastAPI + BGE-M3)
  vectorizer-service:
    build:
      context: ./services/vectorizer-service
      dockerfile: Dockerfile
    container_name: echoes-vectorizer
    restart: unless-stopped
    environment:
      - PORT=8004
      - REDIS_URL=redis://redis:6379/0
      - MEMORY_SERVICE_URL=http://memory-service:8002
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN:-dev-internal-token}
      - MODEL_NAME=BAAI/bge-m3
      - DEVICE=auto
      - ENABLE_VECTORIZE_CONSUMER=true
      - ENV=development
    ports:
      - "8004:8004"
    depends_on:
      redis:
        condition: service_healthy
      memory-service:
        condition: service_started
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8004/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    networks:
      - echoes-network
    ```

    Also update the `memory-service` section to add INTERNAL_API_TOKEN:
    ```yaml
  # Memory Service (Go + GORM)
  memory-service:
    build:
      context: ./services/memory-service
      dockerfile: Dockerfile
    container_name: echoes-memory-service
    restart: unless-stopped
    environment:
      - PORT=8002
      - DATABASE_URL=postgres://echoes_user:echoes_password@postgres:5432/echoes?sslmode=disable
      - REDIS_URL=redis://redis:6379/0
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN:-dev-internal-token}
      - VECTORIZER_SERVICE_URL=http://vectorizer-service:8004
      - PROCESSOR_SERVICE_URL=http://processor-service:8003
      - ENV=development
    ports:
      - "8002:8002"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - echoes-network
    ```

    **B. Create/update .env.example** at project root with all required variables:
    ```bash
    # Echoes (拾忆) - Environment Configuration
    # Copy this file to .env and fill in your values

    # JWT Secret (generate with: openssl rand -base64 32)
    JWT_SECRET=your-jwt-secret-here

    # Internal API Token for service-to-service communication
    INTERNAL_API_TOKEN=dev-internal-token

    # LLM Configuration
    LLM_PROVIDER=openai
    LLM_MODEL=gpt-4o-mini
    LLM_TEMPERATURE=0.7
    OPENAI_API_KEY=sk-your-openai-key-here
    ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

    # GitHub OAuth (optional)
    GITHUB_CLIENT_ID=
    GITHUB_CLIENT_SECRET=
    ```
  </action>
  <verify>
    <automated>grep -n "INTERNAL_API_TOKEN" docker-compose.yml | head -10</automated>
  </verify>
  <done>
    - docker-compose.yml has INTERNAL_API_TOKEN in processor-service, vectorizer-service, and memory-service
    - docker-compose.yml has healthcheck for processor-service and vectorizer-service
    - docker-compose.yml has ports mapping 8003:8003 and 8004:8004
    - .env.example exists with all required env vars documented
  </done>
  <acceptance_criteria>
    - `grep "INTERNAL_API_TOKEN" docker-compose.yml` returns at least 3 matches (one per service)
    - `grep "healthcheck:" docker-compose.yml` returns at least 2 matches (processor + vectorizer)
    - `grep "8003:8003" docker-compose.yml` returns 1 match
    - `grep "8004:8004" docker-compose.yml` returns 1 match
    - `.env.example` exists and contains `INTERNAL_API_TOKEN=`, `OPENAI_API_KEY=`, `ANTHROPIC_API_KEY=`, `LLM_PROVIDER=`, `LLM_MODEL=`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add smoke tests and verify Docker builds</name>
  <files>
    services/processor-service/tests/test_imports.py
    services/processor-service/tests/__init__.py
    services/vectorizer-service/tests/test_imports.py
    services/vectorizer-service/tests/__init__.py
  </files>
  <read_first>
    - services/processor-service/app/main.py
    - services/vectorizer-service/app/main.py
  </read_first>
  <action>
    **A. Create test directories and __init__.py files:**
    - services/processor-service/tests/__init__.py (empty file)
    - services/vectorizer-service/tests/__init__.py (empty file)

    **B. Create services/processor-service/tests/test_imports.py:**
    ```python
    """Smoke tests for Processor Service imports."""
    import pytest

    def test_import_main_app():
        from app.main import app
        assert app is not None

    def test_import_llm_factory():
        from app.services.llm.factory import LLMFactory
        assert LLMFactory is not None

    def test_import_scraper():
        from app.services.scraper import LinkScraper
        assert LinkScraper is not None

    def test_import_consumers():
        from app.consumers.link_consumer import LinkConsumer
        from app.consumers.tag_consumer import TagConsumer
        assert LinkConsumer is not None
        assert TagConsumer is not None

    def test_import_memory_client():
        from app.clients.memory_client import MemoryServiceClient
        assert MemoryServiceClient is not None
    ```

    **C. Create services/vectorizer-service/tests/test_imports.py:**
    ```python
    """Smoke tests for Vectorizer Service imports."""
    import pytest

    def test_import_main_app():
        from app.main import app
        assert app is not None

    def test_import_embedder():
        from app.services.embedder import BGEM3Embedder
        assert BGEM3Embedder is not None

    def test_import_consumer():
        from app.consumers.vectorize_consumer import VectorizeConsumer
        assert VectorizeConsumer is not None

    def test_import_memory_client():
        from app.clients.memory_client import MemoryServiceClient
        assert MemoryServiceClient is not None
    ```

    **D. Verify Docker builds** (if Docker is available):
    ```bash
    cd services/processor-service && docker build -t echoes-processor:test .
    cd services/vectorizer-service && docker build -t echoes-vectorizer:test .
    ```
    If Docker is not available in this environment, skip the build step and document it for the user.
  </action>
  <verify>
    <automated>cd services/processor-service && python -m pytest tests/test_imports.py -v</automated>
  </verify>
  <done>
    - processor-service/tests/test_imports.py exists and passes
    - vectorizer-service/tests/test_imports.py exists and passes
    - pytest is installed and runs successfully
  </done>
  <acceptance_criteria>
    - `ls services/processor-service/tests/test_imports.py` exists
    - `ls services/vectorizer-service/tests/test_imports.py` exists
    - `cd services/processor-service && python -m pytest tests/test_imports.py -v` exits 0 with all tests passing
    - `cd services/vectorizer-service && python -m pytest tests/test_imports.py -v` exits 0 with all tests passing
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: End-to-end integration verification + Gateway internal route check</name>
  <files>
    docker-compose.yml
    services/gateway/cmd/main.go
    services/gateway/internal/router/router.go
  </files>
  <read_first>
    - docker-compose.yml (updated from Task 1)
    - services/memory-service/internal/service/memory_service.go (publishTasks to verify stream names)
    - services/gateway/cmd/main.go (routing setup)
    - services/gateway/internal/router/router.go (if exists)
  </read_first>
  <action>
    **A. Verify Gateway does NOT route /api/v1/internal/* externally:**
    1. Read `services/gateway/cmd/main.go` and any router files
    2. Check if Gateway has a catch-all route like `/api/v1/*` -> Memory Service
    3. If Gateway routes ALL `/api/v1/*` to Memory Service, this is a security issue:
       - The internal API at `/api/v1/internal/*` would be exposed externally
       - Document this as a required Gateway fix
    4. If Gateway has explicit route matching (e.g., `/api/v1/memories/*`, `/api/v1/auth/*`), verify `/api/v1/internal/*` is NOT in the route list
    5. Add finding to the e2e verification output

    **B. Verify the full async pipeline** by checking that:
    1. All required env vars are present in docker-compose.yml
    2. Service dependencies are correct (processor/vectorizer depend on redis and memory-service)
    3. Stream names match between Memory Service publisher and consumers:
       - Memory Service publishes to: `link:fetch`, `text:vectorize`, `tag:generate`
       - Processor consumes: `link:fetch`, `tag:generate`
       - Vectorizer consumes: `text:vectorize`
    4. Internal API path is consistent:
       - Memory Service exposes: `PATCH /api/v1/internal/memories/:id/tasks`
       - Processor/Vectorizer call: `PATCH /api/v1/internal/memories/{memory_id}/tasks`
    5. INTERNAL_API_TOKEN is the same across all three services

    **C. Document the end-to-end test steps** in a verification script at `scripts/verify-phase3.sh`:
    ```bash
    #!/bin/bash
    # Phase 3 Integration Verification Script
    set -e

    echo "=== Phase 3: AI Processing Layer Verification ==="

    # 1. Start services
    echo "[1/6] Starting services..."
    docker-compose up -d postgres redis memory-service processor-service vectorizer-service
    sleep 10

    # 2. Check health endpoints
    echo "[2/6] Checking health endpoints..."
    curl -sf http://localhost:8002/health || exit 1
    curl -sf http://localhost:8003/health || exit 1
    curl -sf http://localhost:8004/health || exit 1
    echo "All services healthy"

    # 3. Verify Gateway does NOT expose internal routes
    echo "[3/6] Verifying Gateway internal route isolation..."
    # Try to access internal API through Gateway (should fail with 404 or 403)
    GATEWAY_INTERNAL_TEST=$(curl -s -o /dev/null -w "%{http_code}" \
      -H "Authorization: Bearer dev-internal-token" \
      http://localhost:8080/api/v1/internal/memories/00000000-0000-0000-0000-000000000000/tasks || echo "000")
    if [ "$GATEWAY_INTERNAL_TEST" = "404" ] || [ "$GATEWAY_INTERNAL_TEST" = "403" ] || [ "$GATEWAY_INTERNAL_TEST" = "000" ]; then
        echo "PASS: Gateway does not expose internal routes (HTTP $GATEWAY_INTERNAL_TEST)"
    else
        echo "WARNING: Gateway may expose internal routes (HTTP $GATEWAY_INTERNAL_TEST)"
        echo "ACTION REQUIRED: Add exclude rule for /api/v1/internal/* in Gateway routing"
    fi

    # 4. Create a test memory via Gateway (requires auth token)
    echo "[4/6] Creating test memory..."
    # This step requires a valid JWT token - document manual verification
    echo "Manual step: Create a text memory via frontend or API"
    echo "Expected: Redis Stream receives text:vectorize and tag:generate tasks"

    # 5. Check Redis streams
    echo "[5/6] Checking Redis streams..."
    docker-compose exec -T redis redis-cli XLEN text:vectorize
    docker-compose exec -T redis redis-cli XLEN tag:generate
    docker-compose exec -T redis redis-cli XLEN link:fetch

    # 6. Verify processing status
    echo "[6/6] Check memory processing_status in database..."
    docker-compose exec -T postgres psql -U echoes_user -d echoes -c "SELECT id, processing_status, metadata FROM memories ORDER BY created_at DESC LIMIT 1;"

    echo "=== Verification complete ==="
    ```
    Make the script executable: `chmod +x scripts/verify-phase3.sh`

    **D. Run a quick syntax check** on the updated docker-compose.yml:
    ```bash
    docker-compose config > /dev/null
    ```
    If Docker is not available, use an online validator or skip.
  </action>
  <verify>
    <automated>docker-compose config > /dev/null 2>&1 && echo "docker-compose valid" || echo "docker-compose validation skipped (no docker)"</automated>
  </verify>
  <done>
    - Stream names verified to match between publisher and consumers
    - Internal API paths verified consistent across services
    - INTERNAL_API_TOKEN consistent across all services
    - Gateway internal route isolation verified (or documented as requiring fix)
    - scripts/verify-phase3.sh exists and is executable
    - docker-compose.yml syntax is valid (or validation skipped with note)
  </done>
  <acceptance_criteria>
    - `grep "link:fetch" services/memory-service/internal/service/redis_queue.go` matches consumer stream names
    - `grep "text:vectorize" services/memory-service/internal/service/redis_queue.go` matches consumer stream names
    - `grep "tag:generate" services/memory-service/internal/service/redis_queue.go` matches consumer stream names
    - `grep "internal/memories" services/processor-service/app/clients/memory_client.py` matches handler path in memory_handler.go
    - `grep "internal/memories" services/vectorizer-service/app/clients/memory_client.py` matches handler path in memory_handler.go
    - `scripts/verify-phase3.sh` exists and contains Gateway internal route check
    - `docker-compose config` exits 0 (if Docker available)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External -> Gateway -> Memory Service | Untrusted user requests |
| Internal Docker network | Service-to-service communication |
| Redis | Internal message queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Information Disclosure | .env.example with placeholder secrets | mitigate | All values are placeholders/dev defaults. Document that production must use strong secrets. |
| T-03-16 | Information Disclosure | INTERNAL_API_TOKEN in docker-compose env | accept | Docker env vars are container-local. For production, use Docker secrets or K8s secrets. |
| T-03-17 | Denial of Service | Health check timeout during model load | mitigate | Vectorizer healthcheck has start_period: 60s. Processor has start_period: 30s. |
| T-03-18 | Information Disclosure | Gateway exposes /api/v1/internal/* | mitigate | Verified in e2e test. If Gateway routes all /api/v1/*, add exclude rule or separate port for internal API. Documented in verify-phase3.sh. |
</threat_model>

<verification>
1. `grep "INTERNAL_API_TOKEN" docker-compose.yml` returns 3 matches
2. `grep "healthcheck:" docker-compose.yml` returns 2+ matches
3. `docker-compose config > /dev/null` exits 0 (if Docker available)
4. Stream name consistency verified across all files
5. Internal API path consistency verified across all files
6. Gateway internal route isolation verified (or documented as requiring fix)
7. pytest tests pass for both processor and vectorizer
</verification>

<success_criteria>
- docker-compose.yml updated with all required env vars for processor, vectorizer, and memory services
- Health checks added to processor-service and vectorizer-service
- .env.example created with all required variables documented
- Smoke tests created and passing for both Python services
- Stream names and internal API paths verified consistent across all services
- Gateway internal route isolation verified — /api/v1/internal/* is NOT externally accessible
- End-to-end verification script created with Gateway security check
- docker-compose.yml syntax valid
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-processing/03-04-SUMMARY.md`
</output>
