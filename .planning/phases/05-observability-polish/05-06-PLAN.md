---
phase: 05-observability-polish
plan: 06
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-03
  - 05-04
files_modified:
  - docker-compose.yml
  - prometheus.yml
  - shared/grafana/provisioning/dashboards/dashboards.yaml
  - shared/grafana/provisioning/datasources/datasources.yaml
  - shared/grafana/dashboards/echoes-overview.json
  - .env.example
  - Makefile
autonomous: true
requirements:
  - R7.1
  - R7.2
  - R7.3
  - R7.4
  - R8.1
must_haves:
  truths:
    - Docker Compose includes prometheus, jaeger, and grafana services
    - Prometheus scrapes all 5 services every 15s
    - Jaeger accepts OTLP traces on port 4318
    - Grafana auto-imports Echoes dashboard via provisioning
    - Grafana shows QPS, latency P99, error rate, and memory processing status panels
    - All services are on the same Docker network (echoes-network)
  artifacts:
    - path: docker-compose.yml
      provides: prometheus, jaeger, grafana service definitions per D-07
    - path: prometheus.yml
      provides: Scrape config for all 5 services per D-06
    - path: shared/grafana/provisioning/datasources/datasources.yaml
      provides: Prometheus + Jaeger datasource auto-config per D-07
    - path: shared/grafana/provisioning/dashboards/dashboards.yaml
      provides: Dashboard provider config per D-07
    - path: shared/grafana/dashboards/echoes-overview.json
      provides: Echoes dashboard with QPS/latency/error panels per D-07
  key_links:
    - from: prometheus.yml
      to: all service /metrics endpoints
      via: Docker network DNS names (gateway:8080, user-service:8001, etc.)
      pattern: static_configs targets
    - from: docker-compose.yml
      to: shared/grafana/provisioning
      via: volume mount /etc/grafana/provisioning
      pattern: volumes: ./shared/grafana/provisioning
    - from: grafana
      to: prometheus
      via: http://prometheus:9090 datasource
      pattern: datasources.yaml url
---

<objective>
Add Prometheus, Jaeger, and Grafana to Docker Compose. Configure Prometheus to scrape all 5 services. Set up Grafana provisioning to auto-import the Echoes dashboard. Create the dashboard JSON with QPS, latency P99, error rate, and memory processing status panels.

Purpose: Satisfy R7.1 (metrics collection), R7.2 (HTTP metrics visualization), R7.3/7.4 (trace viewing), R8.1 (Docker Compose local dev).
Output: `docker-compose up -d` starts the full observability stack alongside application services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-observability-polish/05-CONTEXT.md
@.planning/phases/05-observability-polish/05-RESEARCH.md

# Key interfaces from codebase

From docker-compose.yml (current):
- Services: postgres, redis, minio, gateway, user-service, memory-service, processor-service, vectorizer-service, web
- Network: echoes-network (bridge)
- No observability services yet

From services/*/cmd/main.go (after Plan 01):
- Gateway port: 8080 (mapped to host 8088)
- User Service port: 8001
- Memory Service port: 8002
- Processor Service port: 8003
- Vectorizer Service port: 8004

From services/*/internal/observability/metrics.go (created in Plan 01):
- /metrics endpoint registered on each service
- Metrics: http_requests_total, http_request_duration_seconds

From services/*/internal/observability/trace.go (created in Plan 01):
- OTLP exporter endpoint: jaeger:4318

# Locked decisions from CONTEXT.md
D-07: Grafana dashboard JSON + Provisioning auto-import. Panels: QPS, latency P99, error rate, memory processing status.
D-06: Prometheus metrics dimensions (service, method, path, status_code). NO user_id.

# Dependencies
- Plan 01 complete (all services expose /metrics)
- Plan 03 complete (Go tracing setup)
- Plan 04 complete (Python tracing setup)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Prometheus and Grafana configuration files</name>
  <files>
    prometheus.yml
    shared/grafana/provisioning/datasources/datasources.yaml
    shared/grafana/provisioning/dashboards/dashboards.yaml
    shared/grafana/dashboards/echoes-overview.json
  </files>
  <read_first>
    docker-compose.yml
    .planning/phases/05-observability-polish/05-RESEARCH.md
  </read_first>
  <action>
    1. Create `prometheus.yml`:
       ```yaml
       global:
         scrape_interval: 15s
         evaluation_interval: 15s

       scrape_configs:
         - job_name: 'gateway'
           static_configs:
             - targets: ['gateway:8080']
               labels:
                 service: 'gateway'

         - job_name: 'user-service'
           static_configs:
             - targets: ['user-service:8001']
               labels:
                 service: 'user-service'

         - job_name: 'memory-service'
           static_configs:
             - targets: ['memory-service:8002']
               labels:
                 service: 'memory-service'

         - job_name: 'processor-service'
           static_configs:
             - targets: ['processor-service:8003']
               labels:
                 service: 'processor-service'

         - job_name: 'vectorizer-service'
           static_configs:
             - targets: ['vectorizer-service:8004']
               labels:
                 service: 'vectorizer-service'
       ```

    2. Create `shared/grafana/provisioning/datasources/datasources.yaml`:
       ```yaml
       apiVersion: 1

       datasources:
         - name: Prometheus
           type: prometheus
           access: proxy
           url: http://prometheus:9090
           isDefault: true
           editable: false

         - name: Jaeger
           type: jaeger
           access: proxy
           url: http://jaeger:16686
           editable: false
       ```

    3. Create `shared/grafana/provisioning/dashboards/dashboards.yaml`:
       ```yaml
       apiVersion: 1

       providers:
         - name: 'echoes-dashboards'
           orgId: 1
           type: file
           disableDeletion: false
           updateIntervalSeconds: 10
           allowUiUpdates: false
           options:
             path: /var/lib/grafana/dashboards
             foldersFromFilesStructure: true
       ```

    4. Create `shared/grafana/dashboards/echoes-overview.json`:
       - Dashboard title: "Echoes Overview"
       - UID: "echoes-overview"
       - Timezone: "browser"
       - Panels (per D-07):
         a. QPS (Queries Per Second):
            - Title: "QPS"
            - Type: Stat
            - Query: `sum(rate(http_requests_total[1m]))`
            - Unit: reqps
         b. Latency P99:
            - Title: "Latency P99"
            - Type: Graph (timeseries)
            - Query: `histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket[5m])) by (le, service))`
            - Unit: seconds
         c. Error Rate:
            - Title: "Error Rate"
            - Type: Graph (timeseries)
            - Query: `sum(rate(http_requests_total{status_code=~"5.."}[5m])) / sum(rate(http_requests_total[5m])) * 100`
            - Unit: percent (0-100)
         d. Request Rate by Service:
            - Title: "Request Rate by Service"
            - Type: Graph (timeseries)
            - Query: `sum(rate(http_requests_total[1m])) by (service)`
            - Unit: reqps
         e. Memory Processing Status:
            - Title: "Memory Processing Status"
            - Type: Stat or Pie chart
            - Query: `sum(memory_processing_total) by (status)`
            - Note: This metric is defined in Plan 01 but may need to be added if not yet created. If it doesn't exist yet, use a placeholder panel with a text note explaining it will show data once the metric is emitted.
       - Use Grafana schema version 36 (compatible with latest Grafana)
       - Each panel should be 6-12 units wide, 6-8 units tall
       - Layout: 2 columns (QPS + Error Rate top row, Latency + Request Rate middle, Memory Status bottom)
  </action>
  <verify>
    <automated>test -f prometheus.yml && test -f shared/grafana/provisioning/datasources/datasources.yaml && test -f shared/grafana/provisioning/dashboards/dashboards.yaml && test -f shared/grafana/dashboards/echoes-overview.json && echo "All config files exist"</automated>
  </verify>
  <done>
    - prometheus.yml exists with scrape configs for all 5 services
    - datasources.yaml exists with Prometheus and Jaeger datasources
    - dashboards.yaml exists with file provider config
    - echoes-overview.json exists with 4-5 panels (QPS, latency P99, error rate, request rate by service, memory processing status)
    - All config files have valid YAML/JSON syntax
  </done>
</task>

<task type="auto">
  <name>Task 2: Add observability services to docker-compose.yml</name>
  <files>
    docker-compose.yml
    .env.example
    Makefile
  </files>
  <read_first>
    docker-compose.yml
    Makefile
  </read_first>
  <action>
    1. Modify `docker-compose.yml`:
       - Add `prometheus` service:
         ```yaml
         prometheus:
           image: prom/prometheus:v3.0.0
           container_name: echoes-prometheus
           restart: unless-stopped
           volumes:
             - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
           ports:
             - "9090:9090"
           networks:
             - echoes-network
         ```
       - Add `jaeger` service:
         ```yaml
         jaeger:
           image: jaegertracing/all-in-one:1.76
           container_name: echoes-jaeger
           restart: unless-stopped
           environment:
             - COLLECTOR_OTLP_ENABLED=true
           ports:
             - "16686:16686"
             - "4317:4317"
             - "4318:4318"
           networks:
             - echoes-network
         ```
       - Add `grafana` service:
         ```yaml
         grafana:
           image: grafana/grafana:11.0.0
           container_name: echoes-grafana
           restart: unless-stopped
           ports:
             - "3001:3000"
           volumes:
             - ./shared/grafana/provisioning:/etc/grafana/provisioning:ro
             - ./shared/grafana/dashboards:/var/lib/grafana/dashboards:ro
           environment:
             - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin}
             - GF_AUTH_ANONYMOUS_ENABLED=true
             - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
           networks:
             - echoes-network
         ```
       - Update gateway service environment to include observability config:
         ```yaml
         gateway:
           environment:
             # ... existing env vars ...
             - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
             - LOG_LEVEL=info
             - SERVICE_VERSION=0.2.0
         ```
       - Similarly update all other services (user-service, memory-service, processor-service, vectorizer-service) with:
         ```yaml
         environment:
           # ... existing env vars ...
           - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
           - LOG_LEVEL=info
           - SERVICE_VERSION=0.2.0
         ```
       - Note: For Python services, the endpoint should be `http://jaeger:4318/v1/traces` (they use OTLP HTTP exporter directly). For Go services, `http://jaeger:4318` (OTel Go adds the path). To be safe, set the full path for all:
         - Go services: they use `otlptracehttp.WithEndpoint("jaeger:4318")` which the code in Plan 01 hardcodes. The env var is for flexibility but the code doesn't read it yet. Either update the code to read OTEL_EXPORTER_OTLP_ENDPOINT or just document that Go services use the hardcoded endpoint.
         - Actually, update the Go observability/trace.go to read from env:
           ```go
           endpoint := os.Getenv("OTEL_EXPORTER_OTLP_ENDPOINT")
           if endpoint == "" {
               endpoint = "jaeger:4318"
           }
           ```
         - Do this as part of this task (modify trace.go files)

    2. Update `.env.example` (or create if not exists):
       - Add observability env vars:
         ```
         # Observability
         GRAFANA_ADMIN_PASSWORD=admin
         LOG_LEVEL=info
         SERVICE_VERSION=0.2.0
         OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
         ```

    3. Update `Makefile`:
       - Add convenience targets:
         ```makefile
         .PHONY: observability-up observability-down logs-prometheus logs-jaeger logs-grafana

         observability-up:
         	docker-compose up -d prometheus jaeger grafana

         observability-down:
         	docker-compose stop prometheus jaeger grafana

         logs-prometheus:
         	docker logs -f echoes-prometheus

         logs-jaeger:
         	docker logs -f echoes-jaeger

         logs-grafana:
         	docker logs -f echoes-grafana
         ```
       - Update existing `dev-start` target to also start observability services (or document that they need to be started separately)
  </action>
  <verify>
    <automated>docker-compose config 2>&1 | grep -E "(prometheus|jaeger|grafana)" | head -10</automated>
  </verify>
  <done>
    - docker-compose.yml has prometheus, jaeger, grafana services
    - All app services have OTEL_EXPORTER_OTLP_ENDPOINT, LOG_LEVEL, SERVICE_VERSION env vars
    - Go trace.go files read OTEL_EXPORTER_OTLP_ENDPOINT from env
    - .env.example has observability variables
    - Makefile has observability convenience targets
    - docker-compose config validates without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Go trace.go to read OTLP endpoint from env</name>
  <files>
    services/gateway/internal/observability/trace.go
    services/user-service/internal/observability/trace.go
    services/memory-service/internal/observability/trace.go
  </files>
  <read_first>
    services/gateway/internal/observability/trace.go
  </read_first>
  <action>
    1. Modify each `services/*/internal/observability/trace.go`:
       - In `InitTracer()`, read endpoint from env:
         ```go
         endpoint := os.Getenv("OTEL_EXPORTER_OTLP_ENDPOINT")
         if endpoint == "" {
             endpoint = "jaeger:4318"
         }
         ```
       - Pass endpoint to `otlptracehttp.New()`:
         ```go
         exporter, err := otlptracehttp.New(ctx,
             otlptracehttp.WithEndpoint(endpoint),
             otlptracehttp.WithInsecure(),
         )
         ```
       - Note: otlptracehttp.WithEndpoint takes just the host:port, not the full URL path. The exporter adds `/v1/traces` automatically.

    2. Verify all 3 services still compile:
       ```bash
       cd services/gateway && go build ./...
       cd services/user-service && go build ./...
       cd services/memory-service && go build ./...
       ```
  </action>
  <verify>
    <automated>
      cd services/gateway && go build ./... 2>&1 | head -5 &&
      cd services/user-service && go build ./... 2>&1 | head -5 &&
      cd services/memory-service && go build ./... 2>&1 | head -5
    </automated>
  </verify>
  <done>
    - All 3 trace.go files read OTEL_EXPORTER_OTLP_ENDPOINT from env
    - Default fallback is "jaeger:4318"
    - All 3 services compile successfully
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal network | Prometheus, Jaeger, Grafana on echoes-network only |
| Host ports | 9090 (Prometheus), 16686 (Jaeger UI), 3001 (Grafana) exposed to host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Information Disclosure | Grafana anonymous access | mitigate | Anonymous role is Viewer (read-only). Admin password from env. In production, disable anonymous access. |
| T-05-18 | Information Disclosure | Prometheus UI exposed | accept | Low risk in local dev. Metrics contain no PII. Production: bind to internal network only. |
| T-05-19 | Denial of Service | Jaeger memory growth | accept | all-in-one has default retention. For dev, acceptable. Production: use Jaeger Collector + Elasticsearch. |
| T-05-20 | Tampering | Grafana provisioning read-only | mitigate | Provisioning files mounted read-only (:ro). allowUiUpdates: false prevents dashboard modification via UI. |
</threat_model>

<verification>
1. docker-compose config validation: `docker-compose config` — must pass
2. Config files exist:
   - `test -f prometheus.yml`
   - `test -f shared/grafana/provisioning/datasources/datasources.yaml`
   - `test -f shared/grafana/provisioning/dashboards/dashboards.yaml`
   - `test -f shared/grafana/dashboards/echoes-overview.json`
3. Go services compile with updated trace.go
4. Prometheus scrape targets are valid service names on echoes-network
5. Grafana datasource URLs point to correct internal hostnames
</verification>

<success_criteria>
- [ ] docker-compose.yml includes prometheus, jaeger, grafana services
- [ ] prometheus.yml scrapes all 5 services every 15s
- [ ] Grafana provisioning auto-configures Prometheus and Jaeger datasources
- [ ] Echoes dashboard JSON has QPS, latency P99, error rate, request rate by service panels
- [ ] All app services have OTEL_EXPORTER_OTLP_ENDPOINT, LOG_LEVEL, SERVICE_VERSION env vars
- [ ] Go trace.go reads OTLP endpoint from env with fallback
- [ ] .env.example documents observability variables
- [ ] Makefile has observability convenience targets
- [ ] docker-compose config validates
- [ ] All Go services compile with updated trace.go
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-polish/05-06-SUMMARY.md`
</output>
