---
phase: 05-observability-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/template.tsx
  - web/app/loading.tsx
  - web/app/(main)/page.tsx
  - web/app/(main)/memory/[id]/page.tsx
  - web/app/(main)/search/page.tsx
  - web/components/memory/memory-list.tsx
  - web/components/memory/memory-card.tsx
  - web/components/ui/skeleton.tsx
  - web/components/empty-state.tsx
  - web/app/globals.css
  - web/package.json
autonomous: true
requirements:
  - R6.5
  - R6.6
  - R6.7
must_haves:
  truths:
    - Page transitions show fade-in + slight upward slide (opacity 0->1, y 10->0, 200ms ease-out)
    - Memory cards enter with stagger effect (50ms interval between cards)
    - Loading states show shadcn/ui Skeleton components instead of "加载中..." text
    - Empty states have icon + text with fade-in animation (300ms)
    - Button interactions have scale feedback (scale 0.97, 100ms)
    - All animations respect Notion-like minimal aesthetic — no complex effects
  artifacts:
    - path: web/app/template.tsx
      provides: Framer Motion AnimatePresence page transition wrapper per D-05
    - path: web/app/loading.tsx
      provides: Global loading state with Skeleton components per D-05
    - path: web/components/memory/memory-list.tsx
      provides: Stagger container for memory card animations per D-05
    - path: web/components/ui/skeleton.tsx
      provides: shadcn/ui Skeleton component (or custom if not available)
    - path: web/components/empty-state.tsx
      provides: Animated empty state component per D-05
    - path: web/app/globals.css
      provides: Animation keyframes (fade-in, scale-feedback) per D-05
    - path: web/package.json
      provides: Framer Motion dependency (already installed v11.0.8)
  key_links:
    - from: web/app/template.tsx
      to: all page.tsx files
      via: Next.js App Router template.tsx auto-wraps children
      pattern: AnimatePresence mode="wait"
    - from: web/components/memory/memory-list.tsx
      to: web/components/memory/memory-card.tsx
      via: motion.div variants with staggerChildren
      pattern: variants={containerVariants}
    - from: web/app/(main)/page.tsx
      to: web/components/memory/memory-list.tsx
      via: <MemoryList memories={memories} />
      pattern: MemoryList
---

<objective>
Implement frontend polish: Framer Motion page transitions, staggered card animations, skeleton loading screens, animated empty states, and button scale feedback. Replace all plain "加载中..." text with proper skeleton screens.

Purpose: Satisfy R6.5 (page transitions), R6.6 (card entrance animations), R6.7 (loading states/skeleton screens).
Output: All main pages have motion animations, loading states use Skeleton, empty states are animated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-observability-polish/05-CONTEXT.md
@.planning/phases/05-observability-polish/05-RESEARCH.md

# Key interfaces from codebase

From web/package.json:
- framer-motion: ^11.0.8 (already installed)
- next: 14.2.0 (App Router)
- tailwindcss: ^3.4.1

From web/app/layout.tsx:
- RootLayout wraps children with ThemeProvider and AuthProvider
- No AnimatePresence here (layout.tsx doesn't remount on navigation)

From web/app/(main)/page.tsx:
- Uses useState for memories, isLoading
- Loading state: "加载中..." text (to replace with Skeleton)
- Empty state: static icon + text (to animate)
- Memory cards rendered in plain div with gap-5

From web/app/(main)/memory/[id]/page.tsx:
- Loading state: "加载中..." text (to replace with Skeleton)
- Error state: static text (to keep, but add fade-in)

From web/app/(main)/search/page.tsx:
- Loading state: "搜索中..." text (to replace with Skeleton)
- Empty state: static icon + text (to animate)

From web/components/memory/memory-card.tsx:
- Plain component, no animation wrapper
- Needs motion.div wrapper for stagger effect

From web/components/ui/toast.tsx:
- Already has animate-fade-in class (CSS animation)

# Locked decisions from CONTEXT.md
D-05: Framer Motion animations:
- Page transition: opacity 0->1, y 10->0, 200ms, ease-out
- Card entrance: stagger 50ms, same params
- Loading: shadcn/ui Skeleton component
- Empty state: icon + text fade-in 300ms
- Button: scale 0.97, 100ms
- NO complex animations (no drag, no 3D)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create animation infrastructure (template, skeleton, empty-state, CSS)</name>
  <files>
    web/app/template.tsx
    web/app/loading.tsx
    web/components/ui/skeleton.tsx
    web/components/empty-state.tsx
    web/app/globals.css
  </files>
  <read_first>
    web/app/layout.tsx
    web/app/globals.css
    web/package.json
  </read_first>
  <action>
    1. Create `web/app/template.tsx`:
       - 'use client' directive
       - Import { motion, AnimatePresence } from 'framer-motion'
       - Import { usePathname } from 'next/navigation'
       - Export default function Template({ children }: { children: React.ReactNode })
       - Use `const pathname = usePathname()`
       - Wrap children in `<AnimatePresence mode="wait" initial={false}>`
       - Wrap children in `<motion.div key={pathname} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -10 }} transition={{ duration: 0.2, ease: "easeOut" }}>`
       - Per D-05 and RESEARCH.md Pattern 6 + Anti-Pattern: MUST use template.tsx (not layout.tsx) for exit animations to work

    2. Create `web/app/loading.tsx`:
       - Server component (no 'use client')
       - Import { Skeleton } from '@/components/ui/skeleton'
       - Export default function Loading()
       - Return a skeleton layout matching the main page structure:
         - Header skeleton: h-14 bar with rounded corners
         - Content area: 3-4 skeleton cards (each ~120px tall) with gap-5
         - Use Tailwind classes for layout, Skeleton components for content blocks
       - This is used by Next.js App Router automatically when a page is loading

    3. Create `web/components/ui/skeleton.tsx`:
       - Check if shadcn/ui Skeleton is already available: look for existing skeleton component
       - If not exists, create a simple Skeleton component:
         ```tsx
         import { cn } from "@/lib/utils"
         function Skeleton({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) {
           return <div className={cn("animate-pulse rounded-md bg-gray-200 dark:bg-gray-700", className)} {...props} />
         }
         export { Skeleton }
         ```
       - If exists, skip creation

    4. Create `web/components/empty-state.tsx`:
       - 'use client'
       - Import { motion } from 'framer-motion'
       - Props: { icon: React.ReactNode, title: string, description?: string }
       - Wrap in motion.div with initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.3 }}
       - Centered layout with icon (text-gray-300 dark:text-gray-600), title (text-sm text-gray-400 dark:text-gray-500)

    5. Update `web/app/globals.css`:
       - Add `@keyframes fade-in` animation (if not already present)
       - Add `.animate-fade-in` class (if not already present)
       - Add button scale feedback utility:
         ```css
         .btn-scale {
           transition: transform 100ms ease-out;
         }
         .btn-scale:active {
           transform: scale(0.97);
         }
         ```
       - Ensure these don't conflict with existing Tailwind classes
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | grep -E "(template|loading|skeleton|empty-state)" | head -10</automated>
  </verify>
  <done>
    - template.tsx exists with AnimatePresence and motion.div
    - loading.tsx exists with Skeleton-based layout
    - skeleton.tsx exists as a reusable component
    - empty-state.tsx exists with fade-in animation
    - globals.css has btn-scale utility class
    - TypeScript compiles without errors related to these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MemoryList with stagger animation and update pages</name>
  <files>
    web/components/memory/memory-list.tsx
    web/components/memory/memory-card.tsx
    web/app/(main)/page.tsx
    web/app/(main)/memory/[id]/page.tsx
    web/app/(main)/search/page.tsx
  </files>
  <read_first>
    web/app/(main)/page.tsx
    web/app/(main)/memory/[id]/page.tsx
    web/app/(main)/search/page.tsx
    web/components/memory/memory-card.tsx
  </read_first>
  <action>
    1. Create `web/components/memory/memory-list.tsx`:
       - 'use client'
       - Import { motion } from 'framer-motion'
       - Import MemoryCard from './memory-card'
       - Import { Memory } from '@/lib/api'
       - Define containerVariants:
         ```
         hidden: { opacity: 0 }
         visible: { opacity: 1, transition: { staggerChildren: 0.05, delayChildren: 0.1 } }
         ```
       - Define itemVariants:
         ```
         hidden: { opacity: 0, y: 10 }
         visible: { opacity: 1, y: 0, transition: { duration: 0.2, ease: "easeOut" } }
         ```
       - Export function MemoryList({ memories }: { memories: Memory[] })
       - Return motion.div with variants={containerVariants} initial="hidden" animate="visible" className="flex flex-col gap-5"
       - Map memories to motion.div with key={memory.id} variants={itemVariants}
       - Each motion.div wraps <MemoryCard memory={memory} />

    2. Modify `web/components/memory/memory-card.tsx`:
       - Add 'use client' if not present
       - Wrap the outer <Link> in a motion.div with whileHover={{ scale: 1.01 }} whileTap={{ scale: 0.98 }} transition={{ duration: 0.1 }}
       - Keep all existing content and styling intact
       - The motion wrapper should not break the Link behavior

    3. Modify `web/app/(main)/page.tsx`:
       - Replace `import MemoryCard from '@/components/memory/memory-card'` with `import MemoryList from '@/components/memory/memory-list'`
       - Replace the loading state "加载中..." div with a Skeleton-based loading UI:
         - Use a div with 3-4 Skeleton blocks matching card height (~120px each)
         - Import { Skeleton } from '@/components/ui/skeleton'
       - Replace the empty state with `<EmptyState icon={...} title="还没有记忆，上方创建第一条吧" />`
       - Import EmptyState from '@/components/empty-state'
       - Replace the plain div wrapping MemoryCard with `<MemoryList memories={memories} />`
       - Add btn-scale class to interactive buttons (logout, settings link)

    4. Modify `web/app/(main)/memory/[id]/page.tsx`:
       - Replace loading state "加载中..." with Skeleton-based layout:
         - Skeleton header bar
         - Skeleton content blocks (title, text lines)
       - Import { Skeleton } from '@/components/ui/skeleton'
       - Wrap error state in motion.div with fade-in animation
       - Add whileHover/whileTap to delete button (scale feedback)

    5. Modify `web/app/(main)/search/page.tsx`:
       - Replace loading state "搜索中..." with Skeleton-based layout (3-4 skeleton cards)
       - Import { Skeleton } from '@/components/ui/skeleton'
       - Replace empty state with `<EmptyState icon={...} title="没有找到相关记忆，换个关键词试试？" />`
       - Import EmptyState from '@/components/empty-state'
       - Wrap results in MemoryList (or create SearchResultList if structure differs)
       - Note: search results have extra similarity score — may need a SearchResultCard wrapper or keep inline rendering with motion wrapper
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - MemoryList component exists with staggerChildren 50ms
    - MemoryCard has whileHover/whileTap motion wrapper
    - All three pages (main, memory detail, search) use Skeleton for loading
    - All three pages use EmptyState for empty states
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add button scale feedback and verify animations</name>
  <files>
    web/app/(main)/page.tsx
    web/app/(main)/memory/[id]/page.tsx
    web/app/(auth)/login/page.tsx
    web/app/(auth)/register/page.tsx
    web/components/memory/create-memory-form.tsx
  </files>
  <read_first>
    web/app/(auth)/login/page.tsx
    web/app/(auth)/register/page.tsx
    web/components/memory/create-memory-form.tsx
  </read_first>
  <action>
    1. Add scale feedback to all primary interactive buttons across the app:
       - In web/app/(main)/page.tsx: settings link, logout button — add `className="... btn-scale"`
       - In web/app/(main)/memory/[id]/page.tsx: delete button, back link — add `className="... btn-scale"`
       - In web/app/(auth)/login/page.tsx: submit button — add `className="... btn-scale"`
       - In web/app/(auth)/register/page.tsx: submit button — add `className="... btn-scale"`
       - In web/components/memory/create-memory-form.tsx: submit button — add `className="... btn-scale"`
       - Use Tailwind's `active:scale-[0.97]` as alternative to CSS class if preferred

    2. Verify Framer Motion is properly installed:
       - Check web/node_modules/framer-motion exists
       - If not, run `cd web && npm install` (package.json already lists it)

    3. Quick smoke test:
       - Run `cd web && npm run build` to verify no build errors
       - Note: This is a static analysis check, not a functional test
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All primary buttons have scale feedback (active:scale-[0.97] or btn-scale class)
    - Next.js build completes without errors
    - No "加载中..." plain text remains in any page component
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Next.js | Client-side code is untrusted but self-contained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service | Framer Motion animations | accept | Animations are lightweight CSS transforms. No network requests triggered by animations. |
| T-05-06 | Information Disclosure | Skeleton loading screens | accept | Skeletons show layout structure only, no data content. No PII risk. |
</threat_model>

<verification>
1. TypeScript compilation: `cd web && npx tsc --noEmit` — must pass
2. Next.js build: `cd web && npm run build` — must complete
3. Verify no plain loading text: `grep -r "加载中" web/app/ web/components/` — should return empty (except possibly in non-UI code)
4. Verify Framer Motion imports: `grep -r "framer-motion" web/app/ web/components/` — should show template.tsx, memory-list.tsx, memory-card.tsx, empty-state.tsx
5. Verify Skeleton usage: `grep -r "Skeleton" web/app/ web/components/` — should show loading.tsx, page.tsx files, search/page.tsx
</verification>

<success_criteria>
- [ ] template.tsx provides page transition animations (fade + slide)
- [ ] loading.tsx shows Skeleton-based loading state
- [ ] MemoryList component provides staggered card entrance (50ms interval)
- [ ] MemoryCard has hover/tap scale feedback
- [ ] EmptyState component has fade-in animation
- [ ] All pages (main, detail, search, login, register) have no plain "加载中..." text
- [ ] All primary buttons have active scale feedback
- [ ] Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-polish/05-02-SUMMARY.md`
</output>
